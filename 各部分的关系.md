# 各部分的关系

> **这份是「去哪干活」的路牌。** 新会话读完 `HANDOVER.md`（做到哪了）和
> `AGENTS.md`（规矩），第三份就读这个：**这件事该动哪个文件夹。**
>
> 手写文件，改了结构记得回来改它。

---

## 一、这个平台由哪几部分组成

一句话：**它是一个证据库**。中间是数据，四周是「往里放东西」和「从里面拿东西」的工具。

```
                        ┌─────────────────┐
        找 ──────────►  │                 │
      （补新文献）       │                 │
                        │    data/        │   ──────►  用
        读 ──────────►  │   证据库        │          （问答 / 比大小 / 找空白）
    （PDF → 结构化）     │  这是核心       │
                        │                 │
        管 ──────────►  │                 │
     （去重 / 标签）     └─────────────────┘
                                 ▲
                                 │  Zotero 是最重要的来源，
                                 │  也是精读成果的展示面 —— 但不是核心
```

| 部分 | 现在住在哪 | 干什么 | 什么会让它改 |
|---|---|---|---|
| **地基** | `shared/` | 路径、配置、日志、外部 API 封装、纯算法 | 几乎不改；外部世界变了才动 `adapters/` |
| **找文献** | `tools/discover`、`tools/getpdf`、`tools/askworld` | 搜全球 → 拿到正文 → 问全世界 | 需求变就变 |
| **读文献** | `tools/deepread`、`tools/extract`、`tools/digitize` | PDF → 中文精读 / 结构化字段 / 图表数值 | 需求变就变 |
| **用文献** | `tools/ask`、`tools/paperdb`、`tools/direction` | 问自己的库、筛数值、看方向版图 | 需求变就变 |
| **管书库** | `tools/library`、`tools/curate` | 查 Zotero、打标签、去重、同步 | 需求变就变 |
| **平台自身** | `host/` | 控制面板、体检、部署、MCP、精读监听服务 | 运维方式变了才改 |
| **双击入口** | `launch/` | 给人点的按钮，文件名就是按钮名 | 加了新入口才改 |
| **数据** | `data/` | 五层证据库（raw / curated / serving / state / logs） | 天天在变，但**布局**只由 `shared/kernel/paths.py` 说了算 |
| **档案** | `docs/` | 为什么 / 怎么做 / 事实 / 坑 | 每次改动都要记 |
| **通用工具** | `toolbox/` | 跟文献无关、任何项目都能用的：远程操作另一台机器、拆分工具的锻造间 | 各自独立演进，但**跟平台一起提交** |

---

## 二、谁能依赖谁（**这条有守卫强制，违反了 pytest 当场变红**）

```
host  ──►  tools  ──►  shared.domain / shared.adapters  ──►  shared.kernel
```

箭头方向 = 「可以 import」的方向，**反过来一律禁止**。四条硬规则：

1. **只有 `shared/adapters/` 能联网。** 这条守着的是「换掉 MineRU 只改一个文件」。
2. **`tools/*` 不许 import 别的 `tools/*`。** 要共用就沉到 `shared/`，
   要串起来就浮到 `host/`。
3. **`shared/domain/` 不许知道文件放在哪**（连 `paths` 都不许 import）。
4. **没人 import `host/`。** 换掉面板或换掉部署方式，不该波及能力层。

守卫在 `tests/test_architecture.py`，那张环表写的是**真实的顶层目录名** ——
以后搬动第一级文件夹，那张表要跟着改。

---

## 三、我要干这件事，该去哪个文件夹

| 要干的事 | 去哪 | 顺带要看的 |
|---|---|---|
| 改精读报告的样子 / 内容 | `tools/deepread/` | 提示词在 `tools/deepread/prompts/`，**只增不改** |
| 改抽取的字段 / schema | `tools/extract/` + `shared/domain/schema/` | schema 是结构性改动，先问用户 |
| 改「找文献」的排序、检索式 | `tools/discover/` | 联网部分在 `shared/adapters/openalex`、`crossref` |
| 换模型 / 改模型参数 | `shared/adapters/llm_client/` | 模型名一律走 `shared.kernel.config.get_model()` |
| 换 PDF 解析器（MineRU→别的） | `shared/adapters/pdf_parse/` | **只该改这一个文件夹** |
| 改数据目录布局 | `shared/kernel/paths.py` | 全系统只有这一个文件知道 `data/` 长什么样 |
| 改控制面板 | `host/panel/` | |
| 改体检项 | `host/doctor/health_check.py` | |
| 改精读监听服务（打标签自动跑） | `host/watcher/` | 改完要在 B 机重启任务，**部署≠换文件** |
| 加一个双击按钮 | `launch/` | 文件名就是按钮名，写人话 |
| 改给 AI 看的规矩 | `AGENTS.md` 或 `docs/howto/` | 改完跑 `python host/codegen/skills.py` |
| 连 B 机 / 部署 | `toolbox/remote-machine/` | 读 `two-machines` skill |

---

## 四、下一步（第二步）要变成什么样

第一步（2026-09-08）只做了改名、搬 `toolbox/`、补这份文档，**代码一行没搬**。

第二步打算把第一级从「给程序员看的分层」换成「看得懂的组成」：

```
literature-platform/
    README.md · 各部分的关系.md · AGENTS.md
    find/  read/  use/  keep/      ← tools/ 那 11 块按四条线拆开
    base/                          ← 现在的 shared/
    platform/                      ← 现在的 host/ + tests/
    launch/  data/  docs/  toolbox/
```

依赖规矩不变，只是环表里 `tools` 一格变成四格，四格之间互相不许 import。

### toolbox 为什么在仓库里，却不受五层规矩管

2026-09-08 用户拍板把它并进主仓库的版本管理（原话：「单独弄还是不方便实时更新」）。
它**不是平台的能力层**，所以刻意留在架构守卫的环表之外 ——
`remote-machine/remote.py` 要开 SSH 连接，放进 `tools/` 会违反「联网只在 adapters」。

但它是真代码、会被人改，所以静态检查照做：
`paths.SCANNED_ROOTS`（= `CODE_ROOTS` + `toolbox`）管这件事，
`pyproject.toml` 的 `testpaths` 也列了 `toolbox`，它的测试跟着平台一起跑。
**这两处缺一，那一片就静悄悄不再被覆盖**（踩坑 #83 的同一类事故）。

**还没定的三件事**（动手前必须先跟用户确认）：

1. `askworld` 归「找」还是「用」？`direction` 归「用」还是「找」？
2. `launch/` 要不要继续留在第一级？（用户最常点的就是它）
3. 第一级文件夹名用英文（`find/read/use/keep`）—— 中文目录名会踩编码的坑，
   所以是「英文目录名 + 中文门牌 README」。

**第二步会动 B 机**：那边 4 个开机自启的计划任务指向的路径会失效，
必须跟着一起改、一起验。挑一个不急着用平台的时间做。
