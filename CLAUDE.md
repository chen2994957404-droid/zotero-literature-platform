# 协作约定（给 Claude）

本项目是围绕 Zotero 的文献自动化科研平台。此文件规定 Claude 在本项目里的工作方式。

## 验证自主性（重要）

Claude 有 Windows MCP，可以直接在用户机器上跑进程验证，**无需每次都停下来问用户**。

**可自主执行、跑完直接报结果的：**
- 用 `mcp__Windows-MCP__PowerShell` 跑只读/验证类命令：`ollama list`、查文件、查日志、
  统计命中数、跑单篇抽取验证（如 `extract_structured.py <KEY>`）等
- A/B 对比实验（本地模型 vs 云端、改前 vs 改后），只要覆盖的是可还原的单篇/临时结果
- 读脚本、读数据、验证一处改动是否生效
- 跑完把结论直接汇报，不用事先请示

**仍需先问用户的（有副作用/不可逆/花钱）：**
- 全库重抽（177 篇 × API 调用，花钱且耗时）
- 覆盖或删除已有正确数据（先备份，或先确认）
- 改动会影响 Zotero 库、n8n 生产工作流的操作
- 方向性抉择（改 schema 结构、换技术路线）——这些是用户的领域判断

**原则**：验证性、可还原、零成本的 → 自己跑完报结果；
有副作用、花钱、不可逆、涉及方向判断的 → 先说清代价再问。

## 接手项目先读文档，别边探边拼

动手前先通读 `docs\`（架构总览/数据契约/结构化抽取说明/向量化问答说明），
建立完整认知再改。别走到哪读到哪、靠猜。

**分清「本会话产出」vs「既有资产」**：本会话刚做出来的东西是成果、不是待考古对象。
别把今天刚建的线当成用户早先的系统去揣摩来历（教训见踩坑记录 #14）。
- 既有资产：精读线、向量化问答线。
- 本会话新建：结构化抽取线（extract_library 粗层 / extract_structured 精层 / compare.md）。

## 日志纪律

每有一个值得记的技术发现/踩坑/决策，**当场追加**到 `docs\踩坑记录.md`
（编号 + 现象/根因/解法格式）。别攒着，别等用户提醒。
写中文用 Python `io.open(...encoding='utf-8')` 追加，避开 PowerShell 的 GBK 乱码。

## 单对话推进（设计+应用都在这一个对话里）

本项目在**同一个对话**里推进：既做架构搭建，也做日常使用中发现问题就顺手修。
不再分「设计侧/应用侧」两个并行对话（那样同步全靠人肉搬运，代价大于收益；
待平台稳定到基本不用维护，再考虑独立开使用对话）。

- **任何改代码/删数据/运维操作，当场记到 `docs\变更记录.md`**
  （日期·改了什么文件·为什么·关联踩坑编号）——这是项目的改动流水账，也是重开对话时的记忆锚点。
- **接手/重开对话时先读** `docs\变更记录.md` + `docs\踩坑记录.md` + 架构/数据契约，同步到最新再动手。
- 发现「需要改架构、还没做」的事 → 记进 `docs\待办与需求.md`（工单板），按需逐个处理。
- docs 是唯一事实源。

## 已知环境坑（详见 docs\踩坑记录.md）

- **PowerShell 控制台中文显示乱码**：是显示问题不是数据问题。
  验证真实内容用 Read 工具读文件，或让 Python 直接处理。
- **MCP 调用约 60 秒超时**：长任务（DeepSeek 精读、模型下载、全库抽取）会超时但后台继续，
  用「发起后轮询文件结果」的方式，别干等。
- **一切外部 API 先用真实数据实测**（从 GPT 版失败学到的最重要一条）。

## 两条抽取/检索线的模型分工（别搞混）

- **向量化/问答**：embedding 模型 `bge-m3`（只做文本→向量映射）
- **结构化抽取**：生成模型，需长上下文+强指令遵循。用云端 DeepSeek；
  本地 7B（qwen2.5）精度不够、会产生错配脏数据，只留给低精度场景。
