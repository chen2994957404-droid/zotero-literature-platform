# 本地化拆解规划 —— 每个步骤用什么模型、拆成什么样本地能扛（2026-09-20）

> 用户定的三条方向（同日）：**全部换本地模型** · **LLM 插进每个需要判断的步骤** · **模型按步骤挑，不必都用最大的**。
> 本文是动手前的总规划。每一条「建议档位」都标了依据：是**实测**还是**推断**；推断的必须先量再切。
> 硬件：主力机 RTX 3060 12 GB。可用模型见下表。

## 一、五个档位（以后说「这步用 T2」就指这里）

| 档 | 模型 | 适合的活 | 已有实测 | 速度 |
|---|---|---|---|---|
| **T0** | 脚本（正则 / 规则 / 表格解析） | 能写成规则的一律不请模型：切段、认图号、数字回查、格式硬修 | 全项目在用 | 毫秒 |
| **T1** | `gemma3:1b`（0.8 GB） | **封闭判断题**：给定段落 + 给定名单，问「哪个数是哪个样品的什么」；是/否 | chunk_pass 比武：抽 40 条、接地 94%、样品合法 80%，错的两类脚本都能当场判掉 | 3.5 s/段 |
| **T2** | `qwen3.5:4b`（3.4 GB） | **分类 / 短 JSON / 逐句判**：这篇是不是综述、这张图是主图还是辅图、这句原文支不支持 | **未实测** | 待量（推断 5–8 s/次） |
| **T3** | `qwen3.5:latest` 9.7B（6.6 GB，262k 窗口，有 vision） | **中文写作、长材料理解**：精读各栏、要点清单、问答 | 精读金标 40 篇 90.6 分、0 编数 | 约 16 s/次，一篇精读 4 min |
| **T4** | 云端（只在面板点名时） | 目前只剩图表数字化，待 T3 的 vision 重测后决定 | 踩坑 #15/#16（7B 老模型编数据） | — |

选档的规矩：**同一批样本三档都跑，取达标里最小最快的**。达标线由各步的验收指标定（见每节）。
T1/T2 之所以敢用，全靠 T0 的把关层 —— 小模型犯的错必须是脚本能判掉的那种（数不在段里、样品不在名单里、格式不合形）。

## 二、逐条流水线拆解

### 1. 精读 deepread（已本地，只补步骤、换小模型）

| 步骤 | 输入 → 输出 | 现状 | 建议 | 依据 / 验收 |
|---|---|---|---|---|
| 骨架（节、图、表） | 全文 md → outline | T0 | 不变 | — |
| **是不是综述** | 标题 + 刊名 + 骨架节名（<1k 字） → 是/否 | T0 硬猜（标题关键词 / 刊名表 / 无合成节且 ≥8 图） | **T2 判断题** | 推断。验收：范文 774 篇里综述 8% 左右，人工标 40 篇算准确率 |
| **阅读计划（新）** | 摘要 + 结论 + 全部图注（≈6–10k 字）→ JSON：类型、主角材料、核心主张、图分级（主/辅/补）、Q2 问句 | 没有 | **T3 一次调用**（理解类，T2 先试） | 推断。验收：图分级与范文里各图篇幅的相关性；阅读计划后的金标分 |
| 术语表 | 全文 → 「全称(缩写)」+ 样品编号 | T0 | 不变；查不到中文名的缩写可加 **T2** 给译名 | 领域术语表已有 |
| 导读 + 引言 | 摘要/引言/结论 ≈ 12k 字 → 450 字 + 2 段 | T3 | 不变 | 金标 |
| 实验 + Q1 | 方法节 + 表 + SI ≈ 20k 字 → 三段 + 7 条 | T3 | 不变。无标题论文的方法段识别改 **T1 逐段判「这段是不是方法」**（现在关键词捞，#179） | 推断。验收：IDY9U372 那类论文的实验栏数字覆盖 |
| 逐图两段 | 图注 + 讨论段 ≈ 7k 字 → 100 + 300 字 | T3；篇幅按图数平分 | 篇幅改按阅读计划的图分级分（主图 450、辅图 250、补图 120） | 推断。验收：篇幅比、数字覆盖 |
| 收尾四栏 | 摘要 + 结论 + 各图深解 → Q2/总之/通俗理解/标题 | T3 | 不变 | 金标 |
| 数字回查 / 缩写介绍 / 译名 / 格式 | 产出 vs 原文 | T0 | 不变 | — |

### 2. 审稿 review（已本地，**拆三个用途、各自选型**）

| 步骤 | 输入 → 输出 | 现状 | 建议 | 依据 / 验收 |
|---|---|---|---|---|
| 逐句判 ok/distorted/unsupported/skip | 一栏材料（≤20k 字）+ 25 句 → 25 条判定 | REVIEW 用途，T3 | **拆成 REVIEW_CLAIMS**，T2 先试，不达标退 T3 | 云端基线：误报 6.3%、塞错查全 84%。达标：误报 ≤10%、查全 ≥80% |
| 整篇复核（切片漏判） | 检索出的段落 + 被标句子 → 判定 | 同上 | 同上 | slice_miss 数量 |
| 要点清单 | 摘要 + 结论 + 图注 → 6–12 条英文要点 | T3 | **REVIEW_POINTS**，留 T3（理解 + 取舍） | 推断。验收：要点在范文里的命中率 |
| 覆盖判定 | 要点 + 精读全文（≤40k 字）→ covered/partial/missed | T3 | **REVIEW_COVER**，T2 试 | 推断 |
| 回炉 | notes → 重写那一栏 | 精读的 T3 | 不变 | — |

现在主力机跑着 T3 的本地校准（同一批 5 篇）；跑完接 T2 同批，选完写进路由表。

### 3. 结构化抽取 extract（**本地化的主战场**：整篇一次抽是本地做不好的形状）

现状：`hierarchical_body` 把 5 万字压到 9k 字一次喂 T4/T3 抽 30+ 字段 → 本地只抽到云端 1/4 的数字（2026-08-28）。
根因跟精读一样：**不是模型笨，是没看见 + 一次要的太多**。已有 `chunk_pass` 证明了拆法可行。

| 步骤 | 输入 → 输出 | 建议 | 依据 / 验收 |
|---|---|---|---|
| 表格扫描 | 全文表 → 数值条 | T0（已有 `scan.scan_tables`） | 已在用 |
| **正文分段数值** | 有数字的段（≈1k 字）+ 封闭样品名单 → 「这段哪个数是哪个样品的什么性能」 | **T1**（`chunk_pass` 正式化，全文每段都过，不再压缩） | 实测：1b 接地 94%、脚本把关后可用 |
| 样品名单 | 表格 + 全文 → 样品编号集合 | T0（术语表那套 `_CODE`）+ 遗漏时 **T2** 补 | 验收：名单覆盖表格里全部行名 |
| **定性字段**（动态键类型、机理、应用场景、材料体系） | 摘要 + 结论 + 引言末段（≈5k 字）→ 封闭选项 JSON | **T2 分类**，每个字段族一次调用，选项封闭（schema 里的枚举） | 推断。验收：对着 39 篇精层已有记录算一致率 |
| **合成条件** | SI 的合成节 + 正文方法节（≈10k 字）→ 投料、配比、温度、时间 | **T3**（自由文本→结构，本地最难的一步），拆成「每个样品一次」 | 推断。验收：347 个已核对数字的召回 |
| 数字核对 | 每个数 + 原文窗 → 对那个样品是不是这么说的 | Jev（用户定不换） | 终态 347 yes / 8 no |
| 自评估重抽环 | 抽出 vs 原文 → 反馈重抽 | 现云端自评；本地跳过 | **换成 T0 把关 + T1 判断**（数字在不在原文、样品对不对），不再让模型评模型 | — |
| 出对比表 / 概念矩阵 | 记录 → md | T0 | 不变 | — |

### 4. 问答 ask / 问世界 askworld

| 步骤 | 输入 → 输出 | 建议 | 依据 |
|---|---|---|---|
| 问句转英文 + 扩展检索式 | 一句 → 5 句 | **T2**（短输出、格式简单） | 推断，验收：向量检索命中率不降 |
| 向量检索 | bge-m3 | T0/EMBED | 不变 |
| 综合作答（中文 + 来源） | 检索到的 5–10 段 ≈ 6k 字 → 一段 | **T3** | 路由表已标 meh：小模型综合多来源易漏引 |
| 答案自检（有没有引到来源、数在不在段里） | 答案 vs 材料 | 现有 `_answer_ok` T0 | 不变 |

### 5. 找文献 discover / 方向 direction / 期刊 journalwatch

| 步骤 | 输入 → 输出 | 建议 | 依据 |
|---|---|---|---|
| 检索式拆解 | 主题 → 分面 + 同义词族 | **T3**（要领域知识） | 推断 |
| 候选与库的相关性 | 摘要 vs 库向量 | T0（向量） | 不变 |
| 重排 | 摘要 + 主题 → 0–4 分 | Jev（用户定不换） | — |
| 方向层四元组 | 一篇摘要 → 固定字段 JSON | 已本地 T3；**T2 试** | 推断，验收：20 篇抽查 |
| 期刊雷达 | Crossref | T0 | 无模型 |

### 6. 图表数字化 digitize

| 步骤 | 建议 | 依据 |
|---|---|---|
| 曲线 → X-Y 数据 | **T3 vision 重测**：拿 `curves` 里已有的云端结果当对照，算点误差；误差可接受才切 | #15/#16 是 7B 老视觉模型的结论，不能直接套 qwen3.5 |
| 读数前先判「这张图是什么类型、几条曲线、坐标范围」 | **T2/T3 vision 判断题**，把「读数」拆成「认图 → 定轴 → 逐条读」三步 | 推断：跟精读一样，拆小了才稳 |

### 7. 落地 ingest / 公众号导入 wechat_import / 库房 curate

| 步骤 | 建议 |
|---|---|
| MineRU 解析、骨架、向量化 | T0 + 外部服务，无模型；不变 |
| 推文分块（正文 / 图 / 图注） | 现 T0；块类型拿不准时 **T1** 判 |
| 自动打标签 | 已弃用（707 个标签事故），不动 |

## 三、脚本「硬猜」→ 小模型判断的清单（按收益排）

| 现在硬猜什么 | 在哪 | 换成 | 档 |
|---|---|---|---|
| 是不是综述 | `sectioned.is_review_doc` | 标题 + 摘要 + 节名 → 是/否 | T2 |
| 每张图写多长 | `sectioned._one_fig` 按图数平分 | 阅读计划里的图分级 | T3（一次）|
| 哪段是方法 | `sectioned._methods_paragraphs` 关键词 | 逐段「是不是实验方法」 | T1 |
| 哪张表是配比表 / 性能表 | `_tables` 全要 | 表头 → 类型 | T2 |
| 缩写的中文名 | 领域术语表查不到就不给 | 给全称 → 中文名 | T2 |
| 推文里哪块是图注 | wechat_import 规则 | 块 → 类型 | T1 |

## 四、顺序与验收

```
A. 审稿拆三用途 + T2/T3 同批校准选型          ← 校准跑着，今天出数
B. 抽取拆解：chunk_pass 正式化(T1) → 定性字段分类(T2) → 合成条件按样品(T3)
   验收：347 个已核对数字的召回 ≥ 云端的 80%；定性字段与精层记录一致率 ≥ 85%
C. 阅读计划步骤 + 图分级篇幅 + 综述判定换 T2   验收：金标同批分数不降、篇幅比进 0.8–1.3
D. 问答 / 检索式 / 四元组的小活换 T2           验收：各自的抽查
E. 看图：T3 vision 对照 curves 重测            验收：点误差；不达标继续留云端
```

每一步：先在同一批样本上跑候选档位 → 写进 `docs/变更记录.md` 的实测数字 → 再改路由表默认值。
**没有实测数字的档位不许写成默认。**

## 五、第一轮实测已经给的答案（2026-09-20 17:21，审稿 T3 本地校准，同批 5 篇范文）

| | 云端（deepseek） | 本地 T3（qwen3.5 9.7B） |
|---|---|---|
| 干净范文误报率 | 6.3% | **22.1%** |
| 塞错查全率 | 84% | **67%** |
| 漏判（模型没给判定） | 0 | 23/99 句（一批 25 句时后面的不答） |

误报逐条看，三类占了大头，**都是任务形状问题，不是模型笨**：
1. **材料没找对**：范文的药品清单、表征方法在 SI 里；本地窗口小，材料靠「共享数字/英文词」挑段落，
   而「实验药品包含二水氯化钙、乙醇…」这种中文句子一个英文词都没有 → 挑不到段 → 判成编造。
   **改法：用 bge-m3（本地、跨语言）按句子做语义检索挑段落**，不再靠词面重合。这是 T0 层的活，免费。
2. **引言里的常识**（「传统水凝胶易失水」）在范文整篇一栏时没有「引言可宽容」的规则。改法：范文也按段落位置分栏。
3. **一批 25 句太多**，后半批不答。改法：本地一批 10 句。

这轮数字说明：**同一个提示词、同一个流程，云端能扛的本地扛不住 —— 本地化 = 重做任务形状，不是换个模型名。**
上面每一节的「建议」都要按这个标准再过一遍。

## 六、要提前说的风险

- **显存**：T3 6.6 GB + 24k 窗口的 KV 缓存接近 12 GB 上限；T1/T2 与 T3 不能同时驻留，Ollama 会来回换模型（每次换约 5–10 s）。一条流水线里尽量把同档的调用排在一起。
- **速度**：全本地后一篇「精读 + 审稿 + 抽取」估 8–12 分钟；批量靠夜里跑，watcher 已经是排队的。
- **T2 完全没实测**：上面所有「T2」都是推断，第一轮校准就是给它定性。
- **审≠写**做不到：审稿和精读都是 T3。把逐句判换成 T2 之后这条自然满足。
