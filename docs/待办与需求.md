# 待办与需求 · 只放还没做的

> **2026-09-22 压缩**：原来 1300 行的工单板（含大量已做完、已结案的条目）整份移进
> `docs/待办_历史存档.md`，一个字没删；查「当时为什么这么定」去那里。
> 本页只列**还没做的事**，做完就从这里删掉、写进 `docs/变更记录.md`。
>
> 规矩：
> - 每条一行起头：`- [ ] 标题 —— 一句话为什么`，细节放存档或规划文档，这里只留指针
> - 做完**删掉**，不打勾留着（打勾留着正是上一版长到 1300 行的原因）
> - `HANDOVER.md` 的「待办」一节从这里的 `## ` 标题抓
> - 引用【启发式】「不为架构而架构」推迟一件事时，推迟的是什么、代价是什么写在这里

## 正在做：本地化拆解（主线）

依据与实测数字：`docs/explain/本地化拆解规划.md`。

- [ ] **审稿收尾（已达标，不急）**：实验清单句逐项核；图注句带 SI 同号图注；把核定过的「范文问题句」存成排除表，尺子复用（规划 §十五补三）
- [ ] **审≠写** —— 精读与审稿都是 qwen3.5 9.7B，自己审自己有共同盲区；审稿拆成小模型逐句判之后可自然满足
- [ ] **抽取候选范围** —— 数值事实候选上限 76%、动作 75%，漏的都在「脚本在哪找」而不在模型；图注 / 引言里的处理条件由类型画像决定收不收
- [ ] **样品归属剩三类错** —— 基底 / 试剂当样品、对照样、GLiNER 垃圾短语（§十一）
- [ ] **老整篇抽取的尾巴**：`compare.md` 对比表仍只读 structured/*.json（新文献进不去）—— 改成从 paperdb 出；`chunk_pass` 与 `verify`（Jev 核老记录的数）是否退役待定
- [ ] **单元研究收尾** —— cause 类拆解定义、role / attribute 覆盖低（<35%）怎么推导（规划 B 组 5、6）
- [ ] **类型画像**：按类型统计范文各类单元的选取权重（要先把全部范文的画像跑出来）
- [ ] **精读改「单元 → 按类型选取 → 成文」**：数值事实与动作已接；其余类型待单元库有了再接；实验栏拆四次、收尾拆两次；SI 切错的栏用小模型逐段判、图表栏按图分块
- [ ] **阅读计划步骤**（类型 / 主图分级 / 核心主张）+ 综述判定换小模型
- [ ] **看图 digitize 本地重测** —— qwen3.5 vision 对照 curves 算点误差；#15/#16「本地编数」是 7B 老模型的结论；读数拆成 认图 → 定轴 → 逐条读；先做挑图判据（只送真曲线）
- [ ] **问答 / 检索式扩展 / 方向四元组换小模型**，各自抽查
- [ ] **记录每次模型调用的输入输出** —— 攒聚类 / 微调的料

## 平台与运维

- [ ] **路由表：8 个用途还在按模型名猜通道** —— 面板里逐个指定（能跑，但主力机 `.env` 老名字曾导致单元库 0 条，踩坑 #183）
- [ ] **「待人看」清单进面板 / HANDOVER** —— `review.json` 里 `needs_human=True` 的
- [ ] **骨架：无标题老版式**（Nature 2012 前后）整段判成「摘要」—— 按段落位置切（踩坑 #179）
- [ ] **体检加「取件浏览器能不能开网页」** —— 2026-09-16 浏览器挂死却报「学校没订」
- [ ] **`zotero_client` / `embed` / `llm_client` 自测在编程机上会红** —— 分清「本机没服务」与「代码坏了」
- [ ] **取全文真跑一次实测** —— 取 PDF 的耗时记录一条都没有；`fulltext` 一次 3 篇的上限待定

## 找文献与方向层

- [ ] **期刊分级** —— 横切 discover / direction / extract / getpdf；**先问用户认哪套**（中科院 / JCR / 他自己的表）
- [ ] **雷达库补摘要** —— OpenAlex 按 DOI 批量补（Wiley/ACS/Nature 覆盖 95%+），补齐后再做相关度筛选
- [ ] **盯人：关注的课题组** —— OpenAlex 作者 ID，用户列十几个组
- [ ] **`discover --新方向` 的远 / 近阈值写死 0.5** —— 按实测分布定
- [ ] **B 机只读查询口 / 数据库→Zotero 双向流水线**（核心归属路线第 3、4 步）

## 等用户拍板

- [ ] **抽取 / 审稿要不要允许 flash 兜底** —— 2026-09-22 审查：flash 非高峰约 5 分 / 篇；用户定继续本地，理由是要做与模型无关的流程优化。本地达不到验收线时再议
- [ ] **提示词「只增不改」要不要废** —— 架构准则第八节已列为待推翻；待做一次消融
