# 待办与需求 · 两侧工单板

> **这是应用侧 ↔ 设计侧之间的需求传递通道。**
> 用户在两个对话间搬运信息：应用侧用平台时发现「需要架构性改动」的问题就记这里，
> 设计侧接手时先扫这份，挑能做的实现，做完改状态。
>
> **和另外两份日志的区别：**
> - `踩坑记录.md` = 已诊断清楚的问题 + 根因 + 解法（知识沉淀）
> - `变更记录.md` = 已经做完的改动（交接流水账）
> - `待办与需求.md`（本文件）= **还没做、需要另一侧处理的事**（工单）
>
> ## 怎么用
> - **应用侧**发现「用不顺、需要改架构」→ 加一条：现象 + 期望 + 提出方 + 状态`待处理`。
>   （小 bug 应用侧自己修并记变更记录即可，不用进这里；这里只放需要设计侧动的。）
> - **设计侧**接手先读本文件 → 实现后把状态改 `已解决`，注明改了哪些文件 / 关联踩坑编号。
> - **用户**是两侧传声筒：把应用侧的诉求带到设计侧对话，让设计侧看这个文件接单。
>
> 格式：`- [状态] 标题 · 现象 · 期望 · 提出方/日期 · (解决说明)`
> 状态取值：`待处理` / `处理中` / `已解决` / `搁置`

---

## 工单列表

- [已解决] **代码框架化：批量迁移 40+ 脚本到标准模板**
  · 解决：2026-08-11 完成（见变更记录 · 框架化第二步）。36 个脚本全部按标准模板统一
    （标准开头 + core/cli + core/config + 函数化），体检与迁移前基线一致
    （8 通过/4 警告/1 失败，1 失败为缺密钥）。modules/ 各 selftest 开头统一为可选优化。
  · 现象：框架化已立规范（`docs/howto/代码规范_标准脚本模板.md`）+ 第 17 块积木 `core/cli`，
    试点 `找新文献/find_papers.py` 已改；但当时所在 DSH 环境无 Python/网络，
    剩余脚本不能在无验证能力的环境改（违反「自测不过就是没改完」）。
  · 期望：在有 Python 的环境按「规范 + 本清单」批量统一，每批跑验证命令 + health_check + git 提交。
  · 提出方：设计侧 / 2026-08-11

  **迁移清单**（每批：改完跑该文件夹验证命令 → health_check → git commit；只改写法不改行为）：
  1. `core/cli/selftest.py` 首跑：`python core/cli/selftest.py` 必须 6 组全过
  2. `找新文献/find_papers.py` 实跑：`python 找新文献/find_papers.py "polyborosiloxane" 8`
  3. `库内问答/`：ask.py、ask_world.py、vectorize.py、vectorize_library.py
     （验证：`python 库内问答/ask.py "聚硼硅氧烷剪切硬化机理"`）
  4. `数据抽取/`：extract_batch.py、extract_library.py、extract_structured.py、filter_domain.py
     （涉及 API 的只做语法/体检级验证，不实抽）
  5. `找新文献/` 其余：collect.py、discover.py、import_by_doi.py、search_global.py、
     brainstorm.py、zotero_add_thesis.py
     （验证：`python 找新文献/search_global.py "polyborosiloxane" 3`，其余以体检+语法为准）
  6. `库房维护/`：auto_sync.py、autotag.py、backfill_meta.py、delete_junk.py、list_junk.py、
     tag_to_nested.py、zotero_rename.py（涉及写 Zotero 的操作只做只读项验证）
  7. `文献精读/`：deepread_batch.py、deepread_v4.py、merge_summary.py、mineru_parse.py、
     refresh_summary_file.py、rerun_pro.py、si_batch.py、si_deepread.py、upload_summaries.py、
     zotero_upload_attachment.py、watchdog.py、zotero_watcher.py
     （服务常驻最谨慎：每文件只做「标准开头 + cli 取参 + 函数化」最小改动，体检后人工抽验）
  8. `平台管理/`：health_check.py、panel.py、交接.py（改动最小化；panel 有子进程内联 import，
     改完必须体检 + 面板启停实测）
  9. modules/ 各 selftest.py 的 dirname 写法可顺手统一为 walk-up 标准开头（现有写法正确，可不改）
  · 底线：`归档_旧版本/` 一律不动。

- [待处理] **deepread 密钥注入应支持 .env 配置**
  · 现象：`deepread_v4.py` 的 DeepSeek key 只从命令行第5参传入、不读环境变量，
    且无 fallback；watcher 从环境变量读，读不到就传空 → 精读 401。启动时环境变量
    继承很脆弱（Start-Process 起 .bat 时 set 的变量可能传不到子进程）。
  · 期望：让 watcher/deepread 支持从 `.env` 或统一配置文件读 key，不再靠环境变量继承。
  · 提出方：应用侧 / 2026-07-24（见变更记录 · 认知2）

- [已解决] **watcher 加心跳日志 + 看门狗，防静默卡死**
  · 解决：watcher 每轮写 watcher_heartbeat.txt + 打「[心跳]」日志；新增 scripts/watchdog.py
    监控心跳，超 300s 无更新自动杀掉重启。2026-07-24（变更记录）
  · 现象：watcher 是普通后台进程、非自启；观察到会静默卡死（日志十分钟无轮询痕迹），
    卡死后要手动杀掉重启，期间新打「待精读」标签的文献不会被处理。
  · 期望：加轮询心跳日志便于判断存活；或做成任务计划自启服务 + 看门狗自动重启。
  · 提出方：应用侧 / 2026-07-24（见变更记录 · 运维观察3）

- [已解决] **综述(review)应与研究论文分流，不进结构化对比表**
  · 解决：SCHEMA 加 doc_type 字段；build_compare_table 分流综述到 compare_reviews.md，
    主表仅研究论文。重建后：研究论文 149 篇、综述 5 篇分流。2026-07-24（变更记录）
  · 现象：全库至少 7 篇综述（Self-stratifying…A review、Polyurea…A Review、超临界…研究进展 等）
    被当研究论文抽了 schema。综述是「综合几十上百个体系」，没有单一合成条件/性能数值，
    硬套 schema → 要么全 N/A（如 X7YQNGQ9 全 N/A，占行零信息），要么摘个例子以偏概全（更糟）。
  · 期望：加「文献类型」判断（review/overview/advances/进展 等标题特征 + 可选正文特征），
    结构化抽取时跳过综述或标 item_type=review 单列；综述仍进向量库（问答优质语料）、可精读。
    即「按类型分流」而非排除：综述进问答/精读，不进对比表。
  · 提出方：设计侧 / 2026-07-24（PBS 精抽时发现）

- [已解决] **extract_library.py --rebuild 应保护精层结果**
  · 解决：加 protected 集合，--rebuild 时跳过 source≠coarse 的精层记录。2026-07-24（变更记录 · 改动1）
  · 现象：`--rebuild` 无条件重抽全部，会把 DeepSeek 精层高质量结果（如 2T6H4S3D
    的 104.6 MPa 力学数据）用本地 qwen 粗层覆盖降级。目前只能靠事先备份 + 事后还原。
  · 期望：给 --rebuild 加「仍跳过 source≠coarse 的精层记录」保护，或拆分粗/精重抽命令。
  · 提出方：设计侧 / 2026-07-24（关联踩坑 #16）

- [已解决] **find_pdf 正文/SI 识别应优先信任 Zotero 规范化命名**
  · 解决：find_pdf（watcher + extract_batch 两处）改为优先选 title=="Full Text PDF" 的规范正文，
    正则仅作兜底。CABSSMLA/BMJEJ4ZY 验证正确选中正文。2026-07-24（变更记录）
  · 现象：`find_pdf` 靠文件名正则 + 附件 title 判断正文/SI（踩坑#15 已加 MOESM/ESM 补丁），
    但仍是「靠命名特征猜」的治标做法。Springer 的 MOESM、其他期刊的非常规 SI 命名会逃逸，
    且当 SI 恰好比正文大时选错（CABSSMLA 即此）。
  · 期望：改为优先信任 Zotero 附件的规范化 title（Full Text PDF / SI）——这是 `zotero_rename.py`
    已能产出的可靠信号；正则仅作未规范化条目的兜底。前提是 zotero_rename 先跑过全库。
    这是「正文识别策略」的架构判断（是否把 zotero_rename 纳入精读前置步骤），请设计侧定夺。
  · 提出方：应用侧 / 2026-07-24（关联踩坑 #15、变更记录 · 改动1）


## watcher 重复实例（面板发现，2026-08-06）

一天内第二次出现 2 个 zotero_watcher 并存。看门狗 `restart_watcher()` 先 taskkill 再启动，
但仍有漏网 —— 疑似「任务计划自启的那份」与「看门狗启的那份」互不相识。
影响：两份都在轮询，会抢同一篇文献的处理权，可能重复精读/重复上传。
建议解法：给 watcher 加**单实例锁**（启动时抢一个 lock 文件，抢不到就退出），
比让看门狗去杀更可靠 —— 从源头杜绝而不是事后补救。
面板已能一眼看出该问题（重复实例会标红）。


---

## 2026-08-26 · 架构重构 v2 的后续阶段

总体设计见 `docs/explain/架构重构_v2总体设计.md`。已完成阶段 0（安全网）、阶段 1（core/paths）。

- **阶段 1 剩余**：`core/log.py`（替掉 zotero_watcher 里劫持 `print` 的写法）、
  `core/errors.py`（异常分类，让调用方能分辨「外部服务挂了」和「输入不对」）
- **阶段 2**：`modules/*` 按「是否碰网络/硬盘」拆进 `core / domain / adapters`；
  删掉 zotero_watcher 里重复的 `zget`/`find_pdf`/`SUPP_PAT`；chromadb 包进 `adapters/vectordb`
- **阶段 3**：`pipelines/` + `core/jobs`（SQLite 状态库）。watcher 不再 subprocess 拉脚本。
  这一步收益最大也最需小心，要新旧并行比对产物
- **阶段 4**：统一 `lit` CLI；panel.py 1012 行拆开；MCP 服务重新指向 pipelines
- **阶段 5**：`架构总览.md` 改为自动生成；`归档_旧版本/` 移出仓库；两份大流水账封存

### 单独的小活

- `文献精读/rerun_pro.py` 疑似失效：它引用的 `workflow_data/zotero_work`、
  `workflow_data/summary` 在现行目录布局下都不存在。确认后要么修、要么删。


### 2026-08-26 · 阶段 2 完成后新增的小活

- `库房维护/zotero_rename.py` 里的 `SUPP` 正则与 `adapters/zotero_client.SUPP_PAT`
  是**两套不同的补充材料判断**（前者含中文词、用于改名，后者用于找正文 PDF）。
  暂未合并 —— 合并会改变改名行为，需要真实 Zotero 库验证。确认后再决定合还是分。
- `adapters/snowball` 现在建在 `adapters/openalex` 之上，但它做的是引文网络遍历，
  性质更接近 pipeline。等阶段 3 建好编排环再考虑挪位置。
- 阶段 2 已完成，下一步是**阶段 3（编排环 + 状态库）**——收益最大也最需小心。
  必须在**有真实文献数据的机器上**做：新旧并行跑同几篇，逐字节比对产物。


### 2026-08-26 · 分工落地后的待办

- **金样本**：从主力机拷 2~3 篇的 `parsed/`（一篇有 SI、一篇图多、一篇纯文字）到
  编程端 `workflow_data/library/`。没有它，精读相关的改动在编程端**无法做回归验证**，
  阶段 3 的「新旧并行逐字节比对」也做不了。体检会一直提示这一条。
- ~~**`ZOTERO_USER_ID` 一个配置项在干两件事**~~ → **2026-08-27 已拆**：
  新增 `ZOTERO_WEB_USER_ID`（写回用），没配则沿用旧值，主力机无需改配置。
  仍需在主力机上确认一次写回正常。
- **Zotero Web API 客户端有 9 份**（9 个文件各自拼 base URL 和鉴权头）。
  应该收进 `adapters/zotero_client.zwrite()`，那样机器角色守卫也只需要一处。
  没在这轮做，是因为写路径在编程端无法验证 —— 要在主力机上验证后再合并。


### 2026-08-27 · 阶段 3 上半完成后的待办

- **主力机实测精读端到端**（编程端做不了）：打一次「待处理」，确认
  日志走到 `[正文精读完成]`、Zotero 标签换成「正文精读/全文精读」、
  `workflow_data/state.db` 里有 parse / main_summary 的记录。
  这是本次改动唯一没被自动化覆盖的部分。
- **阶段 3 下半**：把结构化抽取（`数据抽取/extract_structured.py`）、
  向量化与问答（`库内问答/`）也做成 pipelines 里的函数。
  抽取那条要配 `schema_ver`，才能实现「加字段后只重抽缺该字段的文献」。
- **回写 Zotero 收进 adapters**：`zotero_upload_attachment.py` /
  `upload_summaries.py` / watcher 里的 `set_state_tag` 是三份 Zotero 写实现。
  收成一处，机器角色守卫也只需要一处（与上面「9 份 Web API 客户端」是同一件事）。
- **控制面板显示真进度**：状态库已经有数据了（`jobs.summary()` / `jobs.running()`），
  面板还在看日志尾巴。接上就能显示「3/12 篇，当前在解析」。
- **体检的「无弹窗」检查是文本匹配**，文档里引用一句 `subprocess.run(...)` 就会误报
  （本次撞上过一次，靠改写文档绕开）。应改成 AST 判断真实调用。


### 2026-08-27 · 端到端跑通后发现的两个小问题

- ~~合并步骤没有幂等判断~~ **2026-08-27 已修**：产物比两个输入都新就跳过（`_up_to_date`）。
- ~~DOI/标题从解析文本里猜~~ **2026-08-27 已修**：编排层把 item 的 title/DOI
  传给 `read_main`，正则只作兜底。真实复验：精读的「文献信息」栏已带上
  `10.1038/nature11409`（此前是空的）。


### 2026-08-28 · 密钥这条线暴露出来的待办

- **面板保存密钥时要检测同名环境变量**：加载顺序是「环境变量 → 凭据库 → .env」，
  环境变量里若有旧值，用户在面板里填的新值**会被静默盖住**（踩坑 #66 的连带发现）。
  面板应该当场提示「检测到环境变量 X 会覆盖此处设置，是否清除」。
- **主力机的 Zotero API key 需要用真实账号重建**（旧的已 403）。
  注意别把编程端那把复制过去 —— 那是测试账号（21027809）的，
  主力机是 16078117，复制过去照样 403。
- MineRU token 没有「查额度」的接口，只能查有效性。用量得去 mineru.net 网站看。


### 2026-08-28 · 数据质量：下一阶段（数据库方向）的起点

**用户下一个对话要专门做数据库这块。动手前先读这三条 —— 它们是同一条线，顺序不能反。**

#### ~~① 结构化抽取**根本没读 SI**~~ → **2026-08-28 已做**（踩坑 #68）

代码已改：抽取把 `si_parsed/full.md` 一起喂给模型。**但主力机上的 175 条还是旧的** ——
待重抽清单：`python 数据抽取/extract_structured.py --si-pending --list`（只看，不花钱），
去掉 `--list` 才重抽（每篇一次 DeepSeek 调用，不再解析 PDF）。

原文如下：


`pipelines/extract.extract_one()` 只读 `paths.fulltext(key)`（正文），
**完全没碰 `library/<key>/si_parsed/full.md`**。而「精确投料量、原料分子量、
溶剂配比、反应温度时间」这些恰恰是 SI 的内容 —— 这正是精层记录里
`synthesis_conditions` 只有 36% 有值率的原因。

有 SI 精读的那些篇可以直接重抽（每篇一次 DeepSeek 调用）。
做法：抽取时把正文 + SI 全文拼起来喂给模型（注意 `hierarchical_body` 的预算）。

#### ~~② 对比表里 78% 是粗层数据，而且没有标出来~~ → **2026-08-28 已标**

`compare.md` 新增「来源」列（精+SI / 精层 / 粗层）+ 开头印各档次有值率，
并按档次排序。**还没做的是决策部分**：哪些粗层值得花钱升成精层（要用户按方向筛）。

原文如下：


2026-08-28 实测（主力机 175 条）：

| 字段 | 精层(39篇) | 粗层(136篇) |
|---|---|---|
| material_system | 100% | 60% |
| dynamic_bond_type | 87% | 44% |
| **synthesis_conditions** | 36% | **5%** |
| structure_property | 100% | 57% |

粗层 = `数据抽取/extract_library.py` 从 Zotero 全文索引抽的（没有 MineRU 全文）。
两档混在同一张 `compare.md` 里且**看不出区别**，后果是：
用户竖着比字段找规律时，**空白格分不清是「这篇本来就没有」还是「粗层没抽到」**。

至少要在表里标出来源档次；再决定哪些粗层值得升级成精层（要花钱，可按方向筛）。

#### ~~③ 然后才是 structured → SQLite 查询库~~ → **2026-08-28 已建**

`pipelines/paper_db` + `数据抽取/查询库.py`，性能数值已能比大小。
**主力机上还没建**：那边要先 `--si-pending` 重抽，再 `python 数据抽取/查询库.py --rebuild`。

原文如下：


有了①②，「175 个 JSON + 一张 md 表」才值得升成可查询的库：
`papers` 表 + 字段 + 来源档次 + schema 版本。那时才能回答
「所有含硼、拉伸强度 > 10 MPa 的体系，按动态键类型分组」这类问题，
`compare.md` 退化成从库里生成的一个视图。

**顺序理由：先把数据变准，再谈怎么查它。**反过来做，等于给糙数据修了一座漂亮的索引。


## 2026-08-28 · 数据库方向收尾后剩下的三件事

三条起点（①读 SI ②标来源档次 ③SQLite 查询库）**代码全部做完并在主力机上验过**。
剩下的都不是「写代码」，而是「跑」与「判断」：

- **还有 21 篇没重抽**（28 篇里云端跑了 7 篇，用户觉得贵就停了）。
  双击 `重抽缺SI的文献.bat`：[1] 云端最准 · [2] 云端省钱（不自检）· [3] 本地免费。
  建议本地跑完这 21 篇（约 54 分钟，零花费），PBS 方向的少数几篇留给云端。
- **哪些粗层值得升级成精层**（136 篇粗层，要花钱，按方向筛）——
  这是用户的领域判断，不是技术判断。查询库已经能按字段/数值筛出候选。
- **本地抽的那些以后要不要升级**：`python 数据抽取/extract_structured.py --upgrade-local --list`
  就是清单。等哪天余额充足、或者某篇要写进论文时再升。

### 顺手记下的、还没做的小事

- `has_value` 现在把「N/A (原文没给…)」判为没值，但**这类句子还留在 JSON 里**，
  对比表里会显示一长串废话。可以在 make_record 时归一成 `N/A`。
- `core/jobs` 有 `cost` 字段但一直没人写。现在 `llm_client.usage_snapshot()`
  能拿到 token 数了，可以把每篇的花费真正记进状态库，
  于是「这个月花了多少、花在哪一步」变成一句 SQL。
- 面板的服务卡片可以显示「这个服务是什么时候启动的」——
  今天两次栽在「进程还是旧的」（旧代码 / 旧环境变量），
  而界面上完全看不出来。


---

## 【专家层】窄带完备图 · 路线（2026-08-29 立，用户拍板"先做窄带"）

> **目标**：不是做大模型，是做**大模型能调用的库**。最终形态：接上 MCP 后，
> 问"这方向最近什么动态"，agent 能像资深导师那样回答，且每句有出处。
>
> **核心判断（写在最前面，别忘）**：
> **"大"是错的轴。** 专家和 RAG 的差别不是读得多，是**有判断**；
> 而判断不来自更多论文，来自**对已有论文做计算**（数值分布 / 跟进厚度 /
> 引用语境 / 主路径 / 团队图谱）。堆一百万篇 PDF 进向量库，一条也得不到。
>
> **完备性是有停止判据的**：窄带的相关文献是**有限集合**（实测外推 1000~3000 篇），
> 雪球滚到 K 轮不再冒新文献即完备。这跟 OpenScholar 的 4500 万篇是不同的游戏 ——
> 他们要广（只能拿开放获取的），我们要**深且全**（一条窄带里做到完备）。

### ⚠ 架构要求：窄带必须是**参数**，不是硬编码

用户明确说了「我有别的感兴趣方向可以再专门做」。所以第二条窄带必须是
**加一份配置**，不是改代码。当前 `core.paths.direction_db()` 是单例，**要改成按窄带分库**：

```
workflow_data/direction/<band>/map.db      每条窄带一个库
workflow_data/direction/<band>/band.json   窄带定义（关键词/种子DOI/边界判据）
```

### 三条做不到的（天花板，提前写下来免得反复讨论）

1. **付费墙外的全文** —— 只能覆盖用户合法拥有的那些。全世界都一样。
2. **失败与阴性结果** —— 几乎不发表。资深导师最值钱的「我们试过，不行」
   文献里没有。**任何系统都补不了这一条。**
3. **没喂过的方向** —— 窄带外退回普通大模型。这是「做窄才有优势」的另一面。

### 顺序（按 价值/成本 排，早期就要能用起来）

| # | 事项 | 依赖 | 成本 | 状态 |
|---|---|---|---|---|
| B1 | 窄带定义 + 三路取种子（现有地图相关簇 / Zotero 库 / OpenAlex 检索） | — | 半天，免费 | 待处理 |
| B2 | **双向雪球到收敛**（前向是关键，目前完全没有）；多轮直到 K 轮不出新文献 | B1 | 1 天 + 跑几小时，免费 | 待处理 |
| B3 | **跟进厚度**：逐年被引轨迹、跟进者是谁、有无独立课题组跟进 | B2 | 1 天，纯计算 | 待处理 |
| C2 | **团队图谱**：谁在做、路线差异（OpenAlex authorships 自带） | B2 | 半天，免费 | 待处理 |
| D | **MCP 暴露**：有 2~3 个工具就先接上，早接早用 | B3/C2 | 1 天 | 待处理 |
| C1 | **主路径**：这想法从哪来的 | B2 | 1 天 | 待处理 |
| C3 | **数值分布 / reality_check**：同一指标跨论文的分布，判断一个数字是否离群 | 扩结构化抽取到几百篇 | **要花钱，只能在主力机跑** | 待处理 |
| C5 | **空白格**：机理 × 体系 × 性能交叉表 | C3 | 半天 | 待处理 |
| C4 | **矛盾检测**：某结论有没有被后人反驳 | 全文 + LLM 逐句 | **最贵，放最后** | 待处理 |

**为什么 B2 排第一**：没有前向雪球，B3/C1/C2/C4 全都做不了 —— 现在库里只有
「种子引了谁」，没有「谁引了种子」，而后者才是跟进厚度与引用语境的来源。

**为什么 D 排在中间而不是最后**：有 2~3 个工具就该接上 MCP 用起来。
早用早知道哪里不对，比一次做完七个再发现方向错了强。

### 参考的外部现状（2026-08-29 查证，别凭印象推翻）

- **OpenScholar**（Ai2+UW，Nature）：45M **开放获取**论文；8B 模型正确性超 GPT-4o 5%；
  GPT-4o 编造引用 78~90%，它的引用准确率接近人类专家。
  **但它的语料就是开放获取** —— 最强的系统天生带着用户抱怨的那个偏倚。
- **PaperQA2**（FutureHouse）：LitQA2 上超过博士/博后；**开源包，能跑在自己的 PDF 上**。
  ContraCrow 做矛盾检测：随机生物论文平均每篇 **2.34 条陈述被其他文献反驳**。
- **GraphRAG**（微软）：RAG 在面向整个语料的**全局问题**上结构性失效
  （那是查询聚焦摘要，不是检索任务）。我们的引用网络聚类是同一思路的另一种实现。
- **领域专用模型不要做**：LLaMat 用 4M 论文 / 30B token 才超过通用模型；
  而 Darwin 1.5-7B、ChemLLM-7B **反而不如通用大模型**（输在指令跟随）。
- **开放获取偏倚实测**：各家大模型引用里 77.3%~91.8% 指向 OA 文献；
  而化学工程领域 OA 占比**不到 10%**。

### ⚠ 窄带边界（2026-08-29 当天修正过一次，别用旧的）

**用户更正**：方向不是聚硼硅氧烷，是**弹性体抗冲**；PBS 只是当下在试的一个体系
（聚脲混聚硼硅氧烷）。原先按 PBS 画的边界**作废**。

**实测发现（这条决定了整个方案）：高分子抗冲是两个几乎不相往来的社群。**

```
                            OpenAlex 量级   公众号覆盖   发在哪
剪切硬化胶 / PBS 抗冲         59 篇          几乎没有     小众
聚脲 防爆 / 防护涂层          ~1,000         0            IJIE / Composite Structures
聚合物弹道防护                ~1,400         0            Composites B / Materials & Design
剪切增稠流体防护              ~8,200         2 篇         流变学 + 工程刊
冲击硬化超分子                ~1,800         有           AM / AFM / JACS
能量耗散网络 / 强韧弹性体     ~4,600         有           AM / AFM / JACS
仿贝壳层状抗冲                ~1,200         7 篇         Science / Nat Mater
```

现有 754 篇种子里抗冲相关 94 篇（12.5%），但**散在 8 个以上的簇里，没有任何一个簇是「抗冲」**；
而「剪切增稠/硬化」只命中 2 篇、「防护装备」只命中 3 篇 ——
**公众号只覆盖化学半边，工程半边零覆盖。**

**边界定义**：

```
核心必全   剪切硬化胶 / 剪切增稠抗冲（59 篇，全部精读）
工程侧     聚脲防爆防弹涂层、弹道与防护材料
化学侧     冲击硬化超分子、动态键强韧弹性体、能量耗散网络
结构侧     仿贝壳层状抗冲
```

去重后估 **3000~6000 篇**，需全文的 300~500 篇。比最初估的大，但仍可收敛。

**用户本人站在两个社群的接缝上**：聚脲是工程侧的明星材料，
聚硼硅氧烷/剪切硬化胶是化学侧的小众核心。他在做的正是把两边缝起来。

### 因此方案有三处改动

1. **种子必须三路取，工程侧是必须项**：公众号（化学半边）+ OpenAlex 按检索式
   直接拉（工程半边）+ 用户 Zotero 库。**只用公众号会得到一张缺一半的图。**
2. 窄带边界按上表重定。
3. **新增一等产出 B4「社群桥接分析」**：两个社群到底互相引不引？
   若不引，那道缝就是用户的位置，而且能用数据说清楚而不是靠感觉。
   排在 B3 之后、C2 之前。

| # | 事项 | 依赖 | 成本 |
|---|---|---|---|
| B4 | **社群桥接分析**：化学侧 ↔ 工程侧的引用往来密度、谁是少数的桥接者 | B2 | 半天，纯计算 |

### ⚠ 2026-08-29 再次修正：这是**方向层**，不是围绕用户课题的库

**用户原话**：「不用很聚焦我的课题……我想了解的是真正的行业动态和方向。
比如抗冲击这块，有哪些人做了什么，用了什么方法，能实现哪些效果和应用，
什么新的结构或方法可以实现什么性能。」「不要细化到我具体做什么怎么做，那是实验方案的东西。」

这把范围拉回到用户最早说的两层分工：**方向层管行业动态，细节层管怎么合成怎么表征。**
本条线只做方向层。B4「社群桥接」保留但降级 —— 它是一个发现，不是组织原则。

**知识的最小单元因此变了**：不是「论文」，而是

```
策略 / 结构  →  实现的性能（带数值）  →  应用场景  →  谁做的
```

### ★ 决定性实测：方向层不需要全文，因此**不受开放获取偏倚影响**

拿我们语料里 200 篇真实论文（多为 AM / AFM / Angew 这类付费刊）实测：

```
开放获取        60 (30%)
付费墙         140 (70%)
有摘要         200 (100%)
★付费墙里有摘要 140 / 140 = 100%
```

**OpenAlex 对付费墙论文同样提供摘要。** 开放获取偏倚咬的是**全文**，不是元数据和摘要。

后果很大：
- 方向层可以覆盖**整个领域**（含付费墙论文），合法、免费、无需 PDF
- 前面担心的「AI 工具只看得见开放获取那一小撮」，**在方向层不成立**
- 全文只在细节层才是瓶颈 —— 而细节层用户明确说先不做

### ⚠ 同时修正一条我自己说过的话：「窄带是有限集合」需要更严格的定义

关键词检索的命中数会**严重高估**：`impact protection` 在 OpenAlex 命中 200 万，
`impact resistance polymer` 命中 27.9 万 —— 那是相关性检索，不是领域边界。

**所以边界不能用关键词枚举来定，要用「从核心出发的引用网络闭包」来定**：
以几个紧核心为种子（剪切硬化胶 59 篇 / 聚脲防爆 ~1000 / 冲击硬化超分子 ~1800 /
仿贝壳抗冲 ~1200 / 弹道防护 ~1400），雪球扩展 + 相关性门槛，
滚到 K 轮不出新文献为止。**收敛才是边界，关键词只是起点。**

### MCP 工具重排（直接对着用户那句话来）

| 用户会问 | 工具 | 数据 |
|---|---|---|
| 抗冲这块有哪些人做了什么 | `who_and_what` | 团队图谱 + 策略标签 |
| 要实现某个性能有哪些方法 | `strategies_for` | 性能 → 策略反查 |
| 某个策略能做到什么效果、用在哪 | `performance_landscape` | 策略 → 性能分布 + 应用 |
| 最近冒出什么新结构 / 新方法 | `emerging` | 时间序列 + 首次出现 |
| 这方向最近什么动态 | `direction_trend` | 已建 |
| 这结果靠不靠谱 | `follow_up_depth` | 跟进厚度（B3） |

### 唯一要花钱的一步

从**摘要**里抽「策略/结构 → 性能 → 应用」四元组，量级是**上万条摘要 × 一次小模型调用**。
这是整条线唯一的实质成本。**动手前先实测几十条算出单价再报给用户**，不要凭印象估。

### ⚠ 2026-08-30 动手前的现成方案调研（宪法【零号判据】，此前漏做了一次）

**背景**：`domain/bibliometrics` 是我直接开写的 —— Louvain、模块度、文献耦合全部手写，
**没有先查有没有现成的**。用户问「是不是都在重新写」时才补做这次调研。
教训不在已经写的那部分（沉没成本），在于**后面三项动手前必须先查**。

| 计划项 | 现成方案 | 判断 |
|---|---|---|
| **B3 跟进厚度** | `cdindex`（CD/颠覆指数，C 后端，Russell Funk 本人写；另有 novelpy / fast-cdindex）+ sleeping beauty / beauty coefficient | **用现成，而且比原设计好**（见下） |
| **C1 主路径** | **`pybibx`**：内置 Main Path Analysis，支持 SPC / SPLC / SPNP 三种搜索路径法（2026-06 还在更新） | **用现成** |
| **B4 社群桥接** | `networkx`：betweenness_centrality、Burt 结构洞（constraint / effective_size） | **用现成** |
| 聚类 | networkx 3.6 内置 Leiden | **不换**（见下） |
| OpenAlex 客户端 | `pyalex` | **不换**（见下） |

全部可安装（实测 PyPI）：litstudy 1.0.6 · cdindex 1.0.20 · pyalex 0.21 ·
networkx 3.6.1 · leidenalg 0.12.0 · python-igraph 1.0.0 · novelpy 1.4 · pybibx 5.9.5

**① CD 指数比「数跟进篇数」强在哪**（这是本次调研最大的收获）：
它测的是**后来引用这篇的人，是「引它而不再引它的前驱」，还是「连它前驱一起引」** ——
前者说明这项工作**改变了路线**，后者说明它只是**巩固了既有路线**。
这比单纯的被引数或跟进篇数更接近用户想要的「这个结果到底成不成」。
⚠ 但它**需要前向引用网络**（谁引了它、那些人还引了谁），所以 **B2 前向雪球仍是前提**。
⚠ 且网络不完整时 CD 有偏，覆盖度要先报出来。

**② 为什么不换 Leiden**：Leiden 的卖点是修 Louvain「社区内部可能断开」的毛病
（原论文实测最多 25% 连接差、16% 断开）。**在我们的图上实测：47 个簇 0 个断开**。
因为我们的图很稠密（3720 节点 / 317,818 条非零边，平均每点 85 条边），
Louvain 的失效场景是稀疏图 + 反复迭代。**换了不改变结果，所以不换。**

**③ 为什么不换 pyalex**：`adapters/openalex` 现在编码了两条用血换来的知识 ——
踩坑 #76（批量静默丢 30% 数据）与 #77（2026-02 起按量计费 + 免费单条端点兜底）。
换客户端要重新确认它有没有处理这两件事，收益不抵风险。

**判据（补进宪法的实践部分）**：
> **「先查现成的」这条，最容易在「我知道这个算法怎么写」的时候被跳过。**
> 恰恰是这种时候最该查 —— 因为能写出来不等于该自己写，
> 而且现成实现里往往带着你想不到的更好指标（本例：CD 指数之于「跟进厚度」）。


---

## ✅ 2026-08-30 · R2 窗留下的两处 `tools` 调 `tools`（**R7 窗已解决**）

REBUILD.md 第三节硬规则 2 写着「`tools/*` 不许 import 别的 `tools/*`」，
但 R2 窗搬完之后有两条真实的业务闭环横跨两个工具：

| 位置 | 调用 | 为什么它在那儿 |
|---|---|---|
| `tools/deepread/watcher.py` `process_item()` | `tools.extract.run(key)` | 「打个标签就全自动」这条闭环本来就是精读**加**抽取。而 REBUILD.md 又明确要求 watcher 住在 `tools/deepread/` |
| `tools/extract/batch.py` `_rebuild_index()` | `paperdb.rebuild()` | paperdb 是 `structured/*.json` 的**索引**，抽完不刷新它，用户查到的就是旧数据 |

两处都做成了**延迟 import + 就地写明理由**，第二处还做成了失败不致命。

**R7 窗要做的选择**（守卫写出来之前必须先决定，否则守卫一加就红）：

- **选项 A**：把 `watcher.py` / `watchdog.py` 挪去 `host/` —— 它们本来就是
  「让平台活着的东西」而不是一项能力，而 `host/` 可以 import 一切。
  代价：REBUILD.md 第四节的映射要改一行。
- **选项 B**：把跨工具的那一步做成**调用方传进来的回调**
  （`deepread.run(..., after=extract.run)`），谁组合谁自己负责。
- **选项 C**：承认「刷新索引是抽取的一部分」，把 paperdb 降级成 `shared/` 里的一块。
  代价：它有自己的 CLI 和 MCP 暴露，不太像共用件。

**R7 窗的落地（2026-08-31）：选了 A，第二处用了 C 的判据但没降级 paperdb。**

· `watcher.py` / `watchdog.py` → `host/watcher/{service,watchdog}.py`。
  判据是 host 的定义本身：不是能力，是让平台活着的东西；硬规则 4 又明写
  host 可以 import 一切。理由写在 `host/watcher/__init__.py`。
· 索引刷新**不再由抽取方负责**，改成 `paperdb.query()` 每次比一眼时间戳、
  比源 JSON 旧就自己重建（秒级、不花钱）。没有降级 paperdb ——
  它有自己的 CLI 与 MCP 暴露，确实不像共用件。
  **真正的毛病不在那行 import，而在责任放错了地方**：让写入方负责，
  就得每个写 JSON 的人都记得刷一次，漏一个用户就查到旧数据且不报错。
· `wizard._index_stats()` 改成直接调 `shared.domain.schema.coverage`，
  与 `paperdb.stats()` 同一口径、同一个函数，不是复制一份。

## 2026-08-30 · R2 窗没改的运行时标识（低优先级）

搬家之后这些名字与新位置对不上了，但它们连着**磁盘上已有的**日志、锁、状态库记录：

- 日志名 `zotero_watcher`（`workflow_data/logs/zotero_watcher.log`，面板下拉框里也写死了）
- 单实例锁名 `zotero_watcher`（面板 `_TASK_LOCKS` 靠它重启服务）
- `main_text.PRODUCER = 'deepread_v4'` / `si.PRODUCER = 'si_deepread'` /
  `extract.PRODUCER = 'extract_structured'`（状态库里已有记录用的就是这些名字）

改名本身是一行的事，但要连带改面板 HTML、诊断报告、以及决定旧记录怎么办。
**不在重构窗里顺手做。**

## ✅ 2026-08-30 · R3 窗留给 R7 的三件事（**R7 窗已全部处理**）

1. **依赖方向守卫还写着旧环名**（`tests/test_architecture.py` 的
   `RINGS = {'core':0,'domain':1,'adapters':1,'pipelines':2,'apps':3}` 与
   `_PROJECT_PKGS`）。这四个顶层目录 R1 窗起就不存在了，所以这条守卫
   **一直在空转**（同一个病见踩坑 #83；本窗只当场修好了两条联网守卫）。
   R7 窗重写守卫时，`_ring_of` 要改成按**路径前缀**判环
   （`shared/kernel` < `shared/domain`=`shared/adapters` < `tools` < `host`），
   import 侧也要把 `shared.kernel.paths` 映射回 `shared/kernel`。
   **每加一条守卫都要故意违反一次确认它会红。**

2. **`docs/` 里还有大量旧路径**（`库内问答/ask.py`、`找新文献/discover.py`、
   `pipelines/...`）。R7 窗做 docs 归类时一并 `grep -rn` 修掉。
   本窗只修了会被人真正照着敲的两处入口：`CLAUDE.md` 与 `README.md`。

3. **`tools/curate/sync.py` 用子进程跑 `tools.ask.vectorize` 与 `tools.extract.batch`**。
   这不构成 import 边（所以硬规则 2 拦不到它），但它确实是「一个工具驱动另外两个工具」。
   与上面 R2 窗那两处 `tools` 调 `tools` 是同一个问题的两种形态，
   R7 定夺时请一起考虑：**定时同步这件事本身像不像 `host/` 的活？**

**R7 窗的落地（2026-08-31）**：
1. ✅ 依赖方向守卫改成按路径前缀判环，并当场故意违反验红。
   顺带发现引导脚本守卫的 `_PROJECT_PKGS` 也是老名字（同样空转六个窗），
   改成 `set(paths.CODE_ROOTS)` —— **能取的就别抄第二份**。
   还发现「domain 不许有 I/O」那条一直在 `skip`（找的是根目录 `domain/`）：
   **skip 和空转是一回事**。
2. ✅ docs 归类 + 全项目 `grep` 修引用（43 个文件），
   shared/tools 各 CLAUDE.md 里的 `core.paths` / `平台管理/` / `workflow_data`
   一并改对（26 个文件）。
3. ✅ 答案是「像」——`tools/curate/sync.py` → `host/autosync/service.py`。
   它驱动两个工具、还顺手救活后台服务，那是平台的活；
   走子进程躲过守卫是**形式上的规避，不是本质上的合规**。
- [✅ R7 完成] 每个工具还缺 `tests/` 与 `evals/` 骨架 · 现象：R4 只补了五件套，
  REBUILD 第二节的工具包形状还差 `tests/`（只有 deepread 有）与 `evals/` ·
  期望：R5 建 evals 骨架、R7 的守卫要求每个工具七件齐 · 设计侧/2026-08-31
- [✅ R5 完成] `tool.toml` 的 `prompts` 字段目前只有 deepread 填了 `main@v2` ·
  现象：其余工具的提示词还硬编码在代码里 · 期望：R5 窗把提示词迁进
  `tools/<t>/prompts/v<N>.txt` 后回填这个字段 · 设计侧/2026-08-31
- [待处理] B 机的 MCP 配置指向老路径与老工具名 · 现象：R4 把服务入口改成
  `host/mcp/server.py`，工具名加了前缀（`search_items`→`library_search`），
  B 机上 DSH / Claude Code 的现有注册会失效 · 期望：合并回 main 时重新注册，
  同时更新 `.dsh` 的 profile · 设计侧/2026-08-31

- [✅ 2026-09-01 完成] **精读质量指标里百分数几乎永远不被计入** · 现象：`tools/deepread/evals/scorers/quality.py`
  的数值正则以 `` 收尾，而 `%` 后面跟的通常是中文标点或空格，构不成词边界，
  于是「800 %」这类不算数值。实测「拉伸强度 12 MPa，断裂伸长率 800 %」只数出 1 处。
  · 期望：修正单位表的边界处理（`%` 不该要求词边界）。**修的时候要一并想清楚历史快照怎么办** ——
  口径一变，`evalset.json` 里已存的旧快照就跟新算的不可比了，要么重算全部，要么给快照打口径版本号。
  · 提出方：设计侧 R5 窗 / 2026-08-31（搬家窗不改逻辑，先钉住现状：
  `tools/deepread/tests/test_evals.py::test_快照能算且不存在的返回None` 里有断言与注释）

- [部分完成 2026-09-01：paperdb 与 library 已填实，余七个仍空] **九个工具的 evals/ 还是空骨架** · 现象：R5 窗给
  ask/askworld/curate/digitize/direction/discover/extract/library/paperdb 建了
  `evals/{golden,scorers}/ + thresholds.toml + README.md`，但**一条金标、一个评分器都没有**。
  · 期望：按各自 README 里写明的判据逐个填。**优先级建议**：
  ① `digitize`（本地 7B 会编假数据，肉眼看不出来，最该有已知真值的图）
  ② `curate`（改名/删条目不可逆，错了要人工收拾）
  ③ `paperdb` / `library`（纯确定性查询，最容易先做起来，能立刻见效）
  · 提出方：设计侧 R5 窗 / 2026-08-31

- [✅ R7 完成] **「块」有两处枚举口径，会对不上** · 现象：`paths.block_dirs()` 认「有 `__init__.py` 的子目录」，
  `host/codegen/handover.py` 画结构树用 `os.listdir`+`isdir` 认「目录在不在」。
  删包时只 `git rm` 会让目录以「只剩 `__pycache__`」的形态活着，结构树照样画它（踩坑 #87）。
  · 期望：R7 窗重写守卫时合并成一处，结构树也走 `block_dirs()`。
  · 提出方：设计侧 R5 窗 / 2026-08-31


## 2026-08-31 · R7 窗留下的（重构收尾之后）

- [已备好 2026-09-01：双击 launch/更新平台.bat 会自动修正，等用户按一次] **B 机要重新注册两个自启任务的路径** · 现象：R7 把 watcher/看门狗搬到
  `host/watcher/`（`python -m host.watcher.watchdog`）、把定时同步搬到
  `host/autosync/`（`python -m host.autosync`），B 机上 `ZoteroLiteratureWatcher`
  与 `LiteratureAutoSync` 两个计划任务的命令行还指着老模块路径。
  **合并回 main 之前必须先改，否则 B 机上这两条线会静默停摆**（不报错，只是不再触发）。
  MCP 注册也要一起更新（R4 留下的那条待办）。· 设计侧 R7 窗 / 2026-08-31

- [✅ 2026-09-01 完成：digitize 补了 digitize_paper()，豁免作废] **`shared/domain/figure_crop` 是下沉规则唯一的逐块豁免** · 现象：按硬规则 1
  只有 1 个代码使用者（deepread），但 digitize 的对外契约（SKILL/README/MCP 提示词）
  明写「先用它裁图再逐张数字化」，只是那一步今天由调用方手工完成、没有代码调用。
  · 期望：给 digitize 加一个「从解析目录直接数字化」的入口
  （`digitize_parsed(key)`：裁图 → 逐张 digitize），豁免自动消失。
  **这是加功能，不该在重构窗里做。** · 设计侧 R7 窗 / 2026-08-31

- [✅ 2026-09-01 处理：目录名改对、迁移路线标成历史；不删，15 处代码注释指着它] **`docs/explain/架构重构_v2总体设计.md` 只剩历史价值** · 现象：它描述的
  四环结构（`core/domain/adapters/pipelines/apps`）已被按工具切片取代，
  文件顶上加了「别照它施工」的提醒，但正文里还有 24 处老目录名。
  · 期望：要么把仍然有效的判据摘进 `docs/explain/架构宪法_第一性原理.md` 后归档它，
  要么整份重写。**不要留着一份半对半错的设计文档** —— 那比没有更容易误导。
  · 设计侧 R7 窗 / 2026-08-31

- [待处理] **运行时标识仍是老名字**（R2 窗记过，R7 仍未改）· 现象：日志名
  `zotero_watcher` / `auto_sync`、锁名 `zotero_watcher`、`PRODUCER = 'deepread_v4'`
  等，与新模块路径对不上。· 期望：**继续别改** —— 它们连着磁盘上已有的日志、锁、
  状态库记录，改名要连带处理历史数据。这里只是把「知道它对不上」这件事记着。
  · 设计侧 R7 窗 / 2026-08-31

## 2026-09-01 · 这一轮之后还剩的

- [大部分完成 2026-09-01 下午] ~~七个工具的 evals 仍是空骨架~~ ·
  六个已填（curate / discover / extract / direction / ask / askworld），
  **`digitize` 仍是空的**。当初判断「标准答案都得用户来定」是**错的** ——
  这些工具有大块确定性逻辑，金标完全能造。

- [待处理] **`digitize` 的评测**（唯一完全空着的，而且最该有）· 现象：本地模型会编出
  看似合理的假数据，**肉眼看不出来**，这块又没有任何自动检查
  · 需要什么：几张自己画的、**已知真值**的图（曲线/散点/柱状各一张就够），
  再花钱调一次云端模型比对 X-Y 误差。图和「可以花这笔钱」都得用户给
  · 提出方：设计侧 2026-09-01

- [待处理] **各工具评测「还缺的那一半」**，都需要用户的数据或判断：
  `extract` 要几篇人工核对过的 `structured/<KEY>.json`；
  `ask`/`askworld` 要「已知答案在哪一篇」的问题；
  `discover` 的天然金标是「用户后来实际导入了哪几篇」（在主力机上）；
  `direction` 要真实窄带上「这两篇确实是一支吗」的判断
  · 提出方：设计侧 2026-09-01

- [待处理] **Elsevier / docx 的 SI 命名没能核实** · 现象：合并 SI 判据时想顺带
  加上 `mmc\d`（Elsevier）与裸 `esm.docx`，但 A 机测试库里没有这类附件，
  **不能凭记忆断言外部世界怎么命名**（零号判据）· 期望：在主力机上
  `grep` 一遍真实附件文件名，对得上就加进 `zotero_client.SUPP_PAT`
  · 提出方：设计侧 2026-09-01

- [待处理] **B 机上的精读评价快照要重算一次** · 现象：指标口径升到 v2
  （百分数终于被数进去了），但已存的评价是 v1 算的，两把尺子的数不能直接比。
  A 机上那条的精读 HTML 不在本机，重算不了 · 期望：在主力机跑
  `python -c "from tools.deepread import evals; evals.recompute()"`（零成本，
  精读文件还在盘上）。面板的评价卡片上会提示还剩几条 · 提出方：设计侧 2026-09-01

- [待处理] **精读的「废品线」3000 字是拍出来的** · 现象：`MIN_OK = 3000`
  是当初凭手感定的，没有数据支持。低于它就判废、不写盘 —— 这个闸门很关键，
  但阈值本身没被验证过 · 期望：评测集样本够了之后，用「好/差」两组的实际字数
  分布反推一个有依据的值 · 提出方：设计侧 2026-09-01

## 2026-09-01（傍晚）· 放开花钱入口之后剩的

- [待处理] **实际用几次，看弹窗够不够清楚** · 现象：弹窗里显示的是工具名 + 参数，
  代价写在工具描述里 —— 但**没人验过用户看到的到底长什么样**。
  · 期望：用户在主力机上真让 AI 调几次 `ask_library` / `digitize_figure`，
  看「这次要花钱」这件事有没有真的传达到。看不清就把代价写进参数描述
  · 提出方：设计侧 2026-09-01

- [待处理] **askworld 要不要也放开** · 现象：它一次要打 Sciverse + 大模型，
  比 ask 贵一档，这次保守留在 prompt · 期望：`ask_library` 用顺手之后再定
  · 提出方：设计侧 2026-09-01

- [待处理] **MCP elicitation（服务端主动问）没做** · 现象：现在弹窗里只有工具名和参数；
  elicitation 能让服务端自己弹「这次大约花 X，继续吗」，信息量完全不同。
  Claude Code 完全支持（已查证）· 阻碍：要把 `host/mcp/stdio.py` 从「一问一答」
  改成「能在处理中嵌一次服务端→客户端往返」，并提协议版本
  · 提出方：设计侧 2026-09-01

- [待处理] **`extract_one` 可能超 60 秒没实测过** · 现象：靠推断（长文过一次大模型），
  返回文本里写了「超时也不要紧，幂等，再调一次」的自救说明，但**没真的撞过一次**
  · 期望：主力机上真抽一篇看耗时 · 提出方：设计侧 2026-09-01

## 2026-09-03 · B 机通路：等它开机才能验的几件事

- [✅ 2026-09-03 已验证] **`remote.py check` 端到端** —— 一次通过，MAC 也记下了
  · 顺带：它会自动记下 B 的 MAC，`wake` 要用 · 提出方：设计侧 2026-09-03
- [待验证] **`wake` 到底管不管用** · 要求 B 的网卡与 BIOS 开了唤醒、且是有线连接。
  **更省事的正路是让主力机别睡** —— 它本来就跑着精读监听和每小时同步
  · 提出方：设计侧 2026-09-03
- [✅ 2026-09-03 已证实] **`task` 能绕过「SSH 读不到密钥」** · 证据：watcher（计划任务）当天完整跑完一篇含付费模型调用的精读+抽取，而同时 SSH 里读 key 是空串 · 原推断：计划任务的登录会话能读
  凭据库（watcher 就是这么跑花钱作业的）。**验之前不许当结论**
  · 验法：远程触发一次 `LiteratureAutoSync`，然后看日志里有没有「拿不到密钥」
  · 提出方：设计侧 2026-09-03
- [待处理] **B 机那两个计划任务的命令行仍然指着搬走前的模块**（R7 遗留）
  · 现在可以远程修了：`python host/deploy/remote.py deploy`
  （`update.py` 的第 4 步会自动改正），**等 B 开机**
  · 提出方：设计侧 2026-09-03

- [待用户拍板] **B 机还在 `main`（`fef2e35`，重构前的代码）** · R1~R7 全在 `rebuild`
  分支上，B 从没见过。它现在跑得很好（当天刚精读完一篇），所以**不急** ——
  但合并是个方向性决定：合并后 B 的目录布局、模块路径、计划任务命令行全要跟着变
  （`remote.py deploy` 会自动改任务，但那是合并之后的事）
  · 提出方：设计侧 2026-09-03

- [待处理] **A/B 不在同一网络时仍然连不上** · 现象：A 是笔记本，连过 5+ 个网络；
  B 固定在一处。今天是用户手动把 B 连到同一个 WiFi 才通的 · 用户已选「装组网软件」
  · **2026-09-03 已查**：Tailscale/ZeroTier 的中继都在境外，国内要稳得自建服务器 → 排除；
  国产两个可选：**蒲公英**（3 客户端免费、P2P 不限速、转发限 2M、中文界面）
  与 **EasyTier**（开源、无设备限制、有公共节点、称对称 NAT 突破率 98%）。
  **这些是文章说法，不是实测** —— 装上跑一次才算数。
  `remote.py` 已支持多候选地址，装完只要加一个环境变量，不用改代码
  · 提出方：设计侧 2026-09-03


## B 机上重构前的老目录还没清（2026-09-03 记）

切到新代码后，`git pull` 删掉了老目录里**被版本库跟踪的**文件，
但每个目录都还剩一些 git 管不到的零散文件，所以目录本身还在：

`core/ modules/ pipelines/ adapters/ domain/ MCP服务/ 平台管理/ 找新文献/ 文献精读/ 归档_旧版本/`

**为什么当时没顺手删**：`归档_旧版本/`（8 个文件）按名字就是用户自己存的东西，
其余目录里剩的多半是 `__pycache__` 与临时产物，但**没有逐个看过**。
删一个可能有用的文件，比多留十个没用的目录代价大得多。

**下次处理时的判据**：逐目录列出剩余文件再决定，别整目录 `Remove-Item -Recurse`。
另外 `adapters/` 与 `domain/` 这两个名字跟 `shared/adapters`、`shared/domain` 撞车，
留在项目根有「哪天被误 import」的隐患 —— 优先清这两个。

顺带：`workflow_data/`（1.22 GB 原数据）也还留着，等新布局跑顺一两天再删。


## A↔B 的连接会间歇性断，而设计好的兜底地址至今没配（2026-09-04）

**现象**：一晚上断了两次，每次几分钟，都发生在没人操作 B 的时候；
用户去动一下 B 就恢复。一次精读作业因此丢了结果。

**已经排除的（都有证据，不是推测）**：
- 不是睡眠 —— B 连续运行 133 小时，两次查电源事件都是零条
- 不是无线网卡省电 —— 设备管理器那个开关是关的，电源计划里的
  「无线适配器节能模式」交流/直流都已是 0（最高性能）
- 不是 A 机代理抢路由 —— 断线当时实测：ping 网关 1ms 正常，
  `Find-NetRoute` 显示到 B 走 WLAN 直连，没进隧道
- 不是 IP 变了 —— B 的 192.168.123.216 一直在 WLAN 上

**还没查清**：无线日志里只有每 15 分钟一次的常规重新认证
（id 11004/11005/11010），看不出几分钟级的断连。
B 上同时有 singbox_tun、cfw-tap、以太网（公网网关 211.83.153.254）
四条跃点为 0 的默认路由，网络栈很乱，怀疑但**没有证据**。

**该做的（早就设计好了，只是没装）**：commit 9208860 已经把
`remote.py` 改成支持多候选地址，`B_HOSTS` 逗号分隔、局域网优先、
组网地址兜底 —— **但组网软件一直没装，所以兜底地址是空的**。
装上蒲公英或 EasyTier，两台机器各拿一个虚拟 IP，设一个环境变量即可，
代码一行不用改。装之前那条研究结论（见该 commit）**仍未实测**。

**已经缓解的部分**：作业改用 `remote.py job ... --async` 交出去就不管，
连接断了不影响 B 那边干活，结果落盘等重连再取。今晚正是靠这条才拿到精读结果。


## 组网兜底：Tailscale 试过了，**跟用户的代理共存不了**（2026-09-04 结案）

**做过什么**：两台各装 Tailscale 1.102.3（签名验过），登录、拿到 100.x 地址、
SSH 实测通、故障演练也过（把局域网地址换成死地址，命令自动绕到虚拟网络跑通）。

**为什么撤掉**：用户的 VPN 随后连不上，**卸掉 Tailscale 才恢复**。
根因是同一层的东西在抢 —— Tailscale 的虚拟网卡会改默认路由与 DNS，
而这两台机器都靠代理上网。**「能用」和「能共存」是两件事，我只验了前一件。**

**教训**：给一台正在用代理的机器加组网软件，验证清单里必须有一条
「装完之后原来的上网方式还正常吗」。我当时只验了「新路通不通」。

**如果以后还要做**，先解决共存，别直接装：
- 在代理的分流规则里放行 `100.64.0.0/10`（Tailscale 的地址段）走直连
- 或选不接管默认路由的方案（纯 WireGuard 点对点、只加一条明细路由）
- 或干脆不做 —— 见下面

**为什么「干脆不做」也是合理选项**：真正的痛点是「作业跑到一半断线就丢结果」，
而那个问题**已经用 `job ... --async` 解决了** —— 作业交给 B 的计划任务，
连接断了它照跑，重连后取结果。组网只是让「我能随时看见 B」更舒服，
不是让系统更可靠。**B 的无线一周天天在抖（08-31 有 114 条断连事件），
连不上是常态而不是异常**，系统本来就该按这个前提设计。

**还没做的收尾**：B 机上的 Tailscale 还装着（用户只卸了 A 机的），
下次 B 在线时问一句要不要一起卸 —— 它在 B 上同样有抢路由的风险，
而 B 是主力机，那里的代理断了后果更大。


## ⚠ B 机的 SSH 暴露在公网，且密码登录是开着的（2026-09-04 发现）

**怎么发现的**：给 A→B 加宽带兜底地址时才注意到，`211.83.153.16` 是**公网地址**。

**问题**：`C:\ProgramData\ssh\sshd_config` 里
`#PasswordAuthentication yes` 这行是**注释掉的** —— 注释掉等于取 OpenSSH 的默认值，
而**默认就是 yes**。防火墙规则 `OpenSSH SSH Server (sshd)` 的 Profile 是 `Any`。
合起来就是：**任何能路由到那个公网地址的人，都可以对着 B 的 22 端口试密码。**
B 上有 Zotero 写权限、几个付费 API 的密钥、以及全部研究数据。

**两个修法（都不影响我们自己用公钥连）**：
1. `sshd_config` 里显式写 `PasswordAuthentication no`，重启 sshd —— 最直接
2. 或把防火墙规则的 RemoteAddress 收窄到家里网段 + 校园网段

**为什么记在这里而不是直接改**：改 sshd 有把自己锁在门外的风险
（虽然 B 就在用户手边，物理可达），属于要用户点头的那一档。

**顺带一条教训**：我第一次写这段注释时，顺手写下「密码登录已确认关闭」——
**根本没验过**。写完自己看着不对才回去查，一查正好相反。
今天第六次同类问题：**没验过的话不许写进注释、文档或结论**。


## 精读提示词的「加强版」草稿：验过有效，用户决定先不动（2026-09-04）

**背景**：为了验 Gemini 能不能替 DeepSeek 做精读，顺手做了一版加强提示词，
实测 `deepseek-v4-flash` + 这版 = **89 秒 / 21654 字**（现役 v2 是 67 秒 / 15067 字），
图注完整照译、每个子图各成段落、把幂律模型的式子和物理含义讲透了，抽查未见编造。
**Gemini 用同一版会编数字**（三处，见踩坑 #111），所以这版只对 DeepSeek 成立。

**用户当时的决定：先不动。** 现有 15000 字的精读够用，不为多 6500 字引入不确定性。

**要捡起来的话，草稿就是在 `main@v2` 末尾追加下面这段**（新建 `prompts/main_v3.txt`，
提示词只增不改；同时 `main_text.PROMPT_VER` 与 `tool.toml` 的 `prompts` 一起 +1）：

```
## 篇幅与密度的硬性下限（不达标即判为不合格，必须重写而不是缩写）
- 中文正文总量**不少于 12000 字**。这是下限不是目标，超出没有任何问题。
- **讨论**一栏至少占全文一半篇幅。每张图：先把原文图注**完整照译成中文**（不许概括、不许只留要点），
  再对每个子图（图Xa、图Xb…）**各写成完整段落**，说清做了什么、看到什么、数值是多少、说明什么。
  **禁止**把子图解读写成一行一条的要点清单 —— 那是缩水，不是精读。
- 凡原文出现的数值一律照搬：分子量、模量、频率、浓度、温度、时间、效率、误差棒、样本数 n、
  公式与方程编号、拟合参数、坐标轴范围。**宁可重复也不许省略。**
- 实验药品必须写全：中文名 + 英文名/缩写 + 厂商 + 纯度/规格/货号（原文有就写，没有就不写，不许编）。
- 遇到原文的推导、模型、判据（如幂律关系、无量纲数、拟合模型），要把式子和它的物理含义讲出来，
  不能只说"用了某模型拟合得很好"。
- 原文没有的事实一个字都不许添。**加长只能靠把原文讲透，不许靠编。**
```

**上线前必须做的两件事**（踩坑 #111 的教训）：
1. 再验 2–3 篇不同类型的文献（纯合成的、图特别多的），每篇都**回原文核数值**；
2. 核对要专挑**原文本来就没给具体值的地方**（定性描述、"harsh conditions"、"room temperature"）——
   填充式编造正是发生在那里。
3. 考虑给提示词补一条**出口**：允许写"原文未给出"。逼模型把格子填满，就是在发编造许可证。

**还有一笔账**：换 v3 会让状态库把全库 175 篇判为「该重跑」。是否重跑是另一个决定（花钱）。

## 期刊分级（用户 2026-09-05 提出，未做）

**用户原话的意思**：MDPI 的文章基本不看也不想引，可信度不高；
开放获取的刊里「除了一些比较好的期刊，其他都不看」。**他打算后面专门做一套期刊分级。**

**为什么值得做**：他是要发文章的人，参考文献的质量直接影响自己稿件的说服力。
一份把低可信度刊和顶刊混着排的清单对他不是「多给了选择」，是**噪音** ——
他还得自己再筛一遍，等于工具没干活。

**会影响到谁**：`discover` 的排序、`direction` 的版图、`extract`/`paperdb` 往对比表里
收不收、`getpdf` 值不值得为它费劲去取全文。所以分级这件事一旦做，
是**横切多个工具的**，得先想清楚放哪层（大概率是 `shared/domain/` 的一张表 + 一个判分函数）。

**⚠ 先问用户认哪套**：中科院分区 / JCR 分区 / 他自己心里那张表 ——
这是**他的领域判断**，不是我能替他挑的。别自作主张选一个就开做。

**注意**：判据是**刊的档次**，不是开不开放获取。
Nature Communications、Science Advances 这类开放获取他是看的。


## 已评估但不采纳：crawl4ai（2026-09-05）

**结论：不装。**[crawl4ai](https://github.com/unclecode/crawl4ai)（8.1万星，Apache 2.0，
当时 0.9.3）是「网页 → 干净 Markdown」的抓取库，底层 Playwright，附带 LLM 结构化抽取
和 Docker 服务。做 RAG 语料的人很爱用。

**为什么不适合我们**（三条，任一条都足以否掉）：
1. 我们取全文的难点是**付费墙**，不是「网页太脏」。`getpdf` 走的是**借用户已登录的真实
   浏览器**（机构订阅 cookie 在里面）；crawl4ai 自己另起一个干净浏览器，正好卡在最难那步。
   九家出版商已真机验完 95% 覆盖，这条路是通的。
2. 我们要 **PDF 原件**（后续 MineRU 解析 + 裁图给精读）。出版商网页版常缺图缺 SI，
   Markdown 正文不够用。
3. 全家桶依赖与已有的 `llm_client` / `pdf_parse` 重复，违背宪法「少依赖」。

**唯一可能翻案的场景**：将来要抓**开放网页** —— 预印本站、课题组主页、出版商 HTML 全文、
公众号以外的中文来源。它「渲染完再转 Markdown」比自己写解析靠谱，届时可作为
`shared/adapters/` 里一个可换的取数口。**眼下没有这个缺口**（`askworld` 走 Sciverse
取原文片段已够），所以不做。

**下次别再重评一遍。** 要翻案，先确认上面那个「开放网页」缺口是不是真出现了。

## 2026-09-06 · 三层数据库之后的路线（用户拍板：先搭好，暂时不用）

三层（论文 → 样品 → 测量）已落地（见 `docs/变更记录.md` 同日条目）。
**代码这一步做完了，剩下的是「跑」与「判断」**，按顺序：

1. **修精读全文没进向量库那个 bug**（不花钱，现在就在拖问答质量）：
   42 篇有 `parsed/full.md`，精层却只有 276 块 —— 粗层先入库，
   `deep_one` 的 `existing_keys()` 判重把精层全跳过了。
2. **精层那几十篇按 v2 重抽**（要花钱）：先跑 3 篇报单价给用户再说全量。
   清单 = `jobs.stale('extract', schema_ver=2)`，或 `python -m tools.extract --stale --list`。
   验收看 `python -m tools.paperdb --prov`：located 那一栏从 0 涨起来才算真做到。
3. **digitize 落库**（现在抠完的曲线**根本没存**，每次都丢）：
   建 `curves` 表 + 把曲线的极值派生成 `measurements`（`method='curve'`）。
   动手前先确认 digitize 该往哪写 —— 真相仍是文件，库只是索引。
4. **方向层四元组进同一个三层**（策略 → 性能 → 应用，上万条摘要，唯一的实质成本）：
   先测 50 条算单价。落库时 `section='abstract'`、出处标「摘要」，
   于是方向层的空白格能直接指出哪些值得升成细节层。
5. **期刊分级**（用户 2026-09-05 提的，仍未做）：进 papers 表当一列，
   与 located 一起构成「这条数据有多可信」。

**别忘的约束**：不做单位换算；没有出处的数字只能当线索；
粗层维持 v1（Zotero 全文索引里本来就没有表号图号）。


---

## 【下一个对话的正事】脚本优先的抽取流水线（2026-09-07 立，用户拍板）

> 用户原话：「这个工作是不是对大模型的智力要求不高……**能用脚本做的应该先用脚本实现**。」
> 以及：「**尽量把每步都做到最优**。」
>
> 本节是下个对话的施工图。**动手前先读完，它每一句都有当天的实测数字撑着**，
> 不要重新论证，也不要凭印象改判据。

### 为什么要改（三个实测事实，都可复现）

1. **模型只看见了 18% 的正文。**42 篇实测：全文平均 49995 字符，
   `schema.hierarchical_body()` 截到平均 9243 字符就停。42 篇无一例外被砍掉一半以上。
   → 它不是「乱编」，是**没看见**。
2. **脚本扒表格完胜模型。**同样 37 篇：脚本 `schema.scan.scan_tables()` 直出
   **1063 条测量，其中 1051 条带样品名 + 带出处**；云端模型只给了 302 条、175 条带出处。
   表格里四样东西同时在场（行首=样品 / 表头=性能+单位 / 交叉点=数值 / 表前=标题），
   而**脚本抓的出处不可能是编的**。
3. **「本地模型行不行」是问错了的问题**，该问「这个模型配不配这道题」。
   同一天同一个 `gemma3:1b`（0.8 GB）：拆段任务接地 94%，摘要任务接地 3%（踩坑 #132）。

### 比武结果（`python -m tools.extract.bench_chunk`，8 段真实正文，五个指标全自动）

| 模型 | JSON | 抽出 | 数字接地 | 样品合法 | 召回 | 速度 |
|---|---|---|---|---|---|---|
| `qwen3.5-plus`（云）| 8/8 | 31 | 100% | 100% | 88% | **4.1s** |
| `gemma3:1b`（本地 0.8G）| 8/8 | **40** | 94% | 80% | 75% | **3.5s** |
| `qwen3.5:4b`（本地 3.4G）| 5/8 | 10 | 100% | 100% | 88% | 78.7s |
| `qwen3.5:latest`（本地 9.7B）| 6/8 | 13 | 100% | 100% | 88% | 104.1s |

**读法**：本地模型越大越慢越沉默（抽得少、JSON 还崩），1b 快且敢答但会犯错 ——
而它犯的那两种错（数字不在这段里、样品名不在名单里）**脚本能当场判掉**。
所以「0.8 GB 模型 + 脚本校验 ≈ 云端质量、零成本」。云端 flash 档这道题约 0.001 元/段，
全库 42 篇约 500 段 ≈ **五毛钱** —— 别为了省这五毛把架构搞复杂。

### 三步走（每步单独可验，做完一步就有用，别攒着）

#### 第一步 · 表格直接进库（不花钱、收益最大）

- `schema.scan.scan_tables()` 已经写好并验过，产出格式与 `iter_measurements` 一致
  （`method='script'`）。缺的是**入库那一环**。
- 落点选择（**先想清楚再写**）：产物是「派生数据」还是「真相」？
  建议**当派生**：`tools/paperdb` 重建时现扫现进，不落第三份 JSON ——
  真相仍是 `parsed/full.md`，扫一遍 42 篇不到十秒。若实测慢了再改成落盘。
- **必须先洗三样**（都是脚本活，实测样本里都出现了）：
  1. 名字里的 LaTeX：`$\Delta H_c$` → `ΔHc`；`$M_{n}$` → `Mn`
  2. **转置表**：有些表第一列是性能、表头是样品（`d_w (μm) | 3 wt% | 0.39`）——
     判据：第一列的格子大多能被 `PROPERTY_ALIASES` 认出来 → 那就是转置的
  3. 一格多值：`PD 1.68 1.28` → 拆开或丢弃，别硬塞
- **要分开配方与性能**：`KH550 1.0 wt%`、`Antioxidant 0.2 wt%` 是**投料量**，
  该进样品层的 `composition`，不该进测量层。这是判断题 → 词表先做一版，
  认不出的留给第二步的小模型。
- 验收：`python -m tools.paperdb --prov` 里 `located` 从 175 涨到 **1000+**；
  `python -m tools.extract.audit` 不出现新的「挂在不存在样品上」。

#### 第二步 · 拆段抽正文里的数字（1b 本地 或 云端 flash，都便宜）

- 拆段规则已写好：`bench_chunk.split_chunks()`（按标题切、没有数字的段直接不喂模型）。
- 每段的提问也已写好：`prompts/chunk_v1.txt` + `bench_chunk.build_prompt()`，
  **样品名单作为封闭选项传进去**（名单来自第一步的表格 + 已有记录）。
- **必须加脚本校验层**（这是 1b 能用的前提）：
  - 数字不在这一段里 → 丢
  - `sample_id` 不在名单里且不是 `unknown` → 丢
  - 与第一步表格里的同名同值重复 → 去重（表格那条优先，它带出处）
- 模型怎么选：先用 `gemma3:1b` 全跑（免费），把被校验丢掉的比例统计出来；
  丢得太多再换云端 flash（全库五毛钱）。**别用 4b/9.7b 跑量，实测 80~100s/段。**
- 验收：全库数值条数与 `located` 再涨一截；`audit` 的接地率保持 100%。

#### 第三步 · 论文级字段单独抽（唯一真需要「智力」的一步）

- 只抽 `material_system / dynamic_bond_type / structure_property / key_finding /
  limitation / application`，**料只给摘要 + 结论段**（不给全文，省 90% 输入）。
- 用云端好模型，每篇一次。42 篇 ≈ 几毛钱。
- 验收：这些字段的有值率不低于现在（`python -m tools.paperdb --stats`）。

### 不要做的事（都踩过）

- **别把示例数字写进提示词**（踩坑 #132）：小模型会照抄。自测已有守卫，别绕过它。
- **别用大本地模型跑量**：4b/9.7b 在 3060 上 80~100s/段，几千段等于几周。
- **别信「本机测试通过」**（踩坑 #131）：编程端几乎没数据，隔离漏了也发现不了。
  加数据源就去 `tools/paperdb/tests/conftest.py` 的 `SOURCES` 里加一行。
- **别在 `run` 通道发长作业**：300 秒就断，进程会被杀。用 `job ... --async`。
- **别忘了百炼的免费额度按模型各算**：一个用完换下一个（`quadruples.py` 里
  已有 `is_quota_exhausted` + 模型轮换，抽取线还没有，可以照搬）。

### 现状快照（2026-09-07 收工时，主力机实测）

```
2194 篇（摘要 2006 / 粗层 146 / 精+SI 30 / 精层 12）
2571 个样品 · 3287 条数值（能比大小 2421 · 性能种类 1477）
有出处 175 · 有测试条件 712 · 挂到具体样品 560
期刊分级：顶刊 140 / 一流 81 / 常规 163 / 一般 47 / 慎用 66；1697 篇待分级（跑一次 curate journals 即可，免费）
向量库 13906 块（精读全文 2873 · SI 615 · 粗层 10418）
曲线 0 条（digitize 落库通道已通，还没批量跑）
```

**今天全部花费：0 元**（百炼免费额度，四个模型接力）。

### ✅ 第一步（表格直接进库）已完成 —— 2026-09-07

B 机实测：数值 3287 → 3599，**有出处 175 → 487**，悬空数值 0，接地率仍 100%。
脚本直扫 359 条全部带出处。详见 `docs/变更记录.md` 同日两条。

**修正施工图里的两处基线**（下次别照抄上面那节的数字）：
- 「脚本直出 1063 条」复现不出来 —— 同样 42 篇实测 428 条，洗完 371 条
- 于是 located 到不了 1000+，实际 487

**施工图漏判、实测才发现的脏**（已全部处理）：
表头里的 LaTeX（150/428 条）、误差棒被当成单位（37 条）、整行塌进表头的列。
而「转置表」和「一格多值」第一遍探针都报 0 —— **那是探针瞎了**，
词表认不出带 `$` 的性能名（踩坑 #133）。洗完 LaTeX 再问，两样都真实存在。

**下一步就是施工图的第二步**（拆段抽正文里的数字，1b 本地或云端 flash）。


## 2026-09-07 宪法分级后留下的三件事

1. **【消融·要花钱】证伪「提示词只增不改」** —— 该规矩的直接后果是提示词只会越来越长，
   而加进去的每一条从未被证伪过。做法：用现成的比武台（`launch/比一比两个模型.bat`
   那套五指标），对同一批输入跑「带某段约束」vs「删掉它」，看输出指标变不变。
   删掉没差别的，就该删。**需用户批准花费后再跑。**
2. **【收敛】【零号判据】现住三处** —— 宪法 / AGENTS.md / research-first skill，
   改一处另两处漂移。跟「别落第三份 JSON」同病。定一处为源，其余由生成器产出或只留指针。
3. **【守卫】分级标记要能机器校验** —— 在 `tests/` 加检查：宪法与 AGENTS.md 里
   规范性语句没带【事实】/【硬约束】/【启发式】标记的报警。不然过两个月又长回去。
   （动 .py 前先读 code-redlines skill。）


### 2026-09-07 更新：上面三件事完成了两件

- ✅ **【收敛】零号判据三处 → 一个源**：宪法为源，`handover.py` 的
  `sync_zero_criterion()` 抄进 AGENTS.md 与 research-first skill，
  三份副本纳入漂移守卫（commit eb44476）。
- ✅ **【守卫】分级标记可机器校验**：`tests/test_constitution.py`，5 个守卫，
  故意违反 5 次全红（commit eb44476）。
- ⬜ **【消融·要花钱】证伪「提示词只增不改」** —— 仍未做，等用户批准花费。
  这是三个靶子里最值钱的一个：现在「删掉那 50 行哲学没有损失」只是我的内省，
  没有证据；消融是唯一能把这类判断变成证据的手段。


---

## 2026-09-07 · 核心归属定下来之后的路线（第 2 步已做，3/4 待做）

用户拍板的体系：**数据库是这个领域的公共账本（跑在 B 机，唯一写入口），
Zotero 是他个人的阅读桌；两边靠 DOI 对账，靠标签与推荐来回流动。**

- [x] **第 1 步 改口径**：写进宪法第四节 + `AGENTS.md`。
- [x] **第 2 步 松开身份证**：见 `docs/变更记录.md` 同日条目。
- [ ] **第 3 步 B 机开只读查询口**：控制面板加一层只读查询，让别的设备能查库但写不了。
      ⚠ **只绑局域网，不开公网** —— 一开公网就要处理鉴权与被扒库，那是另一个量级，
      动手前先跟用户确认。
- [ ] **第 4 步 双向流水线**：数据库→Zotero（定期推「你没有但该看的 N 篇」）；
      Zotero→数据库（新收藏自动当种子扩图）。**Zotero 插件放最后，可做可不做** ——
      现在的「打标签 → watcher 接活」已经是零维护的等价物，
      插件只多给一个右键菜单，却换来「Zotero 一升级就可能坏」的长期负担。

**接下来最该做的仍是那件老事**：精读全文没进向量库（42 篇有全文，精层只有 276 块）。
它跟归属无关，但一直在拖问答质量。


---

## 2026-09-07 · 核对：精读全文进向量库那件事**已经做完了**（不是待办）

`AGENTS.md` 与本文件都还写着「精层几乎是空的、只有 276 块」，今天在 B 机实测，那个描述**已经过期**：

| 项 | 实测 |
|---|---|
| 向量库总块数 | 13906（粗层 10418 + 精层 2873 + SI 615）|
| 有 `parsed/full.md` 的篇数 / 已进精层的篇数 | 42 / 42 |
| 有 SI 原件却没进库的 | 0 |
| 同一篇两档并存的 | 0（精层入库时粗层已退场）|

代码 2026-09-06 修好（踩坑 #126，提交 ee7c23c），当时的提交信息写着
「主力机上还要跑一次 `--deep` 才算真修好」—— **那一次已经跑过了**，只是没人回来改文档。
所以问答现在检索的确实是精读产出的高质量全文。

**教训**：把「还没做的事」写进多份文档、做完了却只改代码不改文档，
下一个对话就会照着过期的账重做一遍。这次白花的是几十分钟，
换成花钱的作业就是白花钱。**账要么自动生成，要么做完当场改。**


---

## 2026-09-07 · extract 的命令行会把不认识的参数当成「跑全库」（踩坑 #138）

`python -m tools.extract --stale --list` 里两个参数**都不存在/不被那条分支认**，
于是落进 else 走了全库抽取。这次因为记录都已是 v3、逐篇跳过，没花钱（实测 0 文件改写），
但这是**钱的量级**的隐患，且待办里那句「清单 = `--stale --list`」本身就是错的。

待做三件（都不花钱）：
1. **未知参数一律拒绝**并打印用法（`shared.kernel.cli` 已有 `wants_help`，缺的是「不认识就停」）。
2. `--list` 升成全局开关：任何分支下都只列不做。
3. 把 `--stale` 真接上 `jobs.stale('extract', schema_ver=N)`。

⚠ 顺带核实到的账（同日实测，B 机）：**「精层那几十篇按 v2 重抽」已经做完了** ——
`papers` 表里精层 12 篇 + 精+SI 30 篇全是 schema v3，
`--prov` 的 located 一栏：精+SI 261/377、精层 227/252（待办原文的验收标准是「从 0 涨起来」）。


---

## 2026-09-07 · 看图（digitize）的下一步：先挑图，再全抠

跑通了，账也算清了（`docs/reference/看图的账.md`：292 张约 2.2–4.4 美元）。
**卡点不是钱，是命中率** —— 实测 5 张里 2 张是图谱/多联版面，读回 0 条曲线。

- [ ] **挑图判据**：只送真曲线（应力应变 / 模量 / 效率随循环）进模型，
      排除示意图、SEM/TEM、XRD、NMR、光谱、多联版面。
      判据放哪要想清楚：图注关键词是最便宜的一档，不够再考虑先让模型看一眼缩略图。
- [ ] 判据做好后再问用户要不要全库抠（那时的账才是准的）。
- [ ] `curves` 的数值目前**没有样品名**（`sample_id` 取自图例，如 `"free" carbonyl`），
      跟正文抽出来的样品对不上号。要不要归一，等有更多真实数据再判断。
