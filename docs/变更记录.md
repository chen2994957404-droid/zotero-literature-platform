# 变更记录 · Changelog（跨会话交接日志）

⚠ **本文件时间正序、体积很大。要看最近做了什么请读文件末尾（tail），或直接看 `HANDOVER.md` 的摘要。**

> 用户同时在两个对话里推进本项目：
> - **「运用」侧**（挂 n8n-literature-workflow 文件夹）：日常**使用**平台，用中发现问题就顺手修。
> - **「设计」侧**：搭建/架构性改动。
>
> 两个对话无法互通，唯一的连贯机制是**共享的落盘文件**（代码 + docs）。
> 因此：**任何改动都当场记到这里**（谁·哪侧·改了什么文件·为什么），
> 交接班的对话先读这份 + 踩坑记录，就能同步到最新状态。
> 格式：日期 · 侧 · 改动摘要 · 涉及文件 · 关联踩坑编号。

---

## 2026-07-24 · 运用侧

**背景**：用户在本对话用平台辅助解决合成问题（聚联硼硅氧烷封端程度/摩尔投料比），
过程中给一篇目标文献 CABSSMLA 打「待精读」，暴露并修复了两个问题。

### 改动 1：修复 find_pdf 误选补充材料（SI）去精读
- **文件**：`scripts/zotero_watcher.py`（find_pdf 函数）
- **问题**：Springer/Nature 系 SI 命名 `..._MOESM1_ESM.pdf` 逃逸了 SUPP_PAT 正则，
  且该 SI 恰好比正文大，被误选去精读。用户在 Zotero 改附件 title 为 "SI" 无效，
  因 find_pdf 读的是磁盘 filename 而非 title。
- **修法**：① SUPP_PAT 增加 `moesm|_esm\b|electronic.?supplementary`；
  ② 同时把附件 Zotero title 纳入判断（title 命中正则 或 title=="SI" 即判 SI），
  尊重用户手动标注。零副作用（只会让更多 SI 被正确排除）。
- **验证**：直接调 find_pdf("CABSSMLA") → 正确选中正文 IL4MTXUJ。✓
- **关联**：踩坑记录 #15。

### 认知 2：deepread_v4 的密钥注入机制（未改代码，仅记录踩坑）
- `deepread_v4.py` 的 DeepSeek key 靠命令行第5参数传入，**不读环境变量**，
  且唯独它没有硬编码 fallback key。上游 watcher 从环境变量 `DEEPSEEK_KEY` 读，
  读不到就传空 → 精读 401。
- **正确启动**：在同一 PowerShell 会话 `$env:DEEPSEEK_KEY="..."` 后再起 watcher，
  确保环境变量继承到 python 子进程。用 `Start-Process cmd /c 启动Zotero闭环.bat`
  时 set 的变量可能传不到子进程。
- **潜在改进建议（留给设计侧决定）**：可让 watcher 或 deepread 也支持从
  `.env` 文件或统一配置读 key，避免每次靠环境变量继承。

### 运维观察 3：watcher 会卡死 + 非自启
- watcher 是普通后台进程，**非开机自启服务**；且观察到进程会卡死（日志十分钟无轮询痕迹），
  卡死后需杀掉重启。
- **潜在改进建议（留给设计侧）**：给 watcher 加轮询心跳日志/看门狗，
  或做成真正的自启服务（任务计划），避免静默卡死。

### 数据操作 4：CABSSMLA 用正文重跑
- 删除了基于 SI 的旧结果（library/CABSSMLA 目录 + structured/CABSSMLA.json），
  标签重置「待精读」，用修复后的 find_pdf 重新用正文精读。

### 结果 5：CABSSMLA 正文精读成功
- 修复后重跑，find_pdf 正确选中正文（IL4MTXUJ），MineRU 解析正文、7张正文图、DeepSeek 精读 9112 字、summary.html 6.2MB 生成。对照之前误选 SI（14张SI附图）已纠正。
- 观察：watcher 进程曾卡死（日志十分钟无轮询），杀掉带 key 重启后正常捞取。

## 2026-07-24 · 设计侧

### 大操作挂记（并发纪律）：全库粗层 --rebuild 进行中
- **正在跑**：extract_library.py --rebuild（本地 qwen2.5，零成本），用修复后的 schema 重抽全库。
- **原因**：修复 key_properties 字段太窄（踩坑 #12），让 174 篇粗层用上放宽字段。
- **涉及**：覆写 workflow_data/structured/ 全部 *.json + compare.md。当前进度约 91/177。
- **已备份**：structured_bak_before_schema_fix/（重抽前快照，可还原）。
- **给运用侧的提醒**：CABSSMLA 是两侧都动的 key——本重抽是粗层，收尾时设计侧会把
  CABSSMLA 从备份/精层还原，不让粗层结果覆盖你的正文精读成果。你那边暂勿动 structured/。
- **中间效果**：key_properties=N/A 从 61% 降到约 41%，字段修复全库有效。
- **关联**：踩坑 #12（字段修复）、#16（--rebuild 会覆盖精层，收尾需还原）。

### 设计侧最新进展（仍在跑 119/177）· 供运用侧同步
- **schema 修复**：key_properties 字段太窄导致漏数值（踩坑 #12），已放宽为「任何量化表征结果」。
  全库粗层 --rebuild 中，N/A 率从 61% 降到约 41%，字段修复全库有效。
- **新建工单板** docs/待办与需求.md：两侧「待办/需求」通道。已归入运用侧提的两条建议
  （deepread 支持 .env、watcher 加心跳看门狗）+ 设计侧的 --rebuild 保护精层（#16）。
  ★ 运用侧以后发现需改架构的问题，往这个文件加一条即可。
- **定了并发纪律**（见 CLAUDE.md）：两侧可同时跑，但不许同时写同一文件；
  各写各领地、共享日志追加前先读编号、大操作先挂记号。
- **进展同步约定**：每轮有实质进展就当场写一条到本文件，让另一侧读到即同步最新。
  ★ 运用侧也请照做：你那边做了什么、跑到哪，写一行这里，好让设计侧知道。
- **收尾待办**：重抽跑完后，设计侧会把 CABSSMLA 等精层篇从备份还原、重建 compare.md。
  在此之前 ★ 运用侧暂勿动 workflow_data/structured/。


### 运用侧同步（响应设计侧 --rebuild）· 2026-07-24
- 已读设计侧全部进展，遵守并发纪律：**暂停一切对 workflow_data/structured/ 的操作**，
  直到设计侧收尾还原精层。
- **已停掉 watcher 进程**（PID 3028）：防止 CABSSMLA 精读后自动触发 extract_structured.py
  精层抽取，撞上设计侧正在跑的全库 --rebuild（两侧写同一目录）。收尾后我再按需重启。
- CABSSMLA 正文精读成果（library/CABSSMLA/summary.html 6.2MB）完好，等设计侧从备份/精层还原。
- 本会话运用侧成果小结：修复 find_pdf 误选 SI（踩坑#15）、CABSSMLA 正文精读、
  用平台辅助解决聚联硼硅氧烷(PDBS)封端/投料比问题（结论：同原料文献 CABSSMLA 实证
  1:0.55 为最优基准，交联为主+悬挂端并存）。

## 2026-07-24 · 合回单对话推进

**决定**：不再分设计侧/应用侧两个并行对话（同步全靠人肉搬运，代价大于并行收益）。
回到单对话推进：架构搭建 + 日常使用修 bug 都在一处。待平台稳定到基本不用维护，
再考虑独立开使用对话。已删 CLAUDE.md 里「跨侧话术接力」「并发写入纪律」两节，
「两个对话交接」改为「单对话推进」。三份日志（踩坑/变更/待办与需求）保留，作项目记忆。

**本轮收尾（schema 修复全库重抽）**：
- 全库粗层 --rebuild 完成：key_properties=N/A 从 61% 降到 47%（字段修复有效；
  剩余 N/A 多为文献本身无数值的正确空值）。compare.md 已重建（178 篇）。
- 踩坑 #16 验证属实并已修复：--rebuild 把 3 篇精层（2T6H4S3D/AZJRUG6P/RBYA8XMB）
  用本地 qwen 粗层覆盖降级，已全部从 structured_bak_before_schema_fix/ 还原成精层版。
- CABSSMLA 澄清：它本就只有粗层结构化结果（无精层 JSON），未被降级；其 6.2MB 正文
  精读 summary.html 属精读线、完好未动。无需处理。
- 备份 structured_bak_before_schema_fix/ 保留（还原源，暂勿删）。

## 2026-07-24 · 修 #16 + 清粗层脏数据

### 改动1：extract_library.py --rebuild 保护精层（修踩坑 #16）
- 加 protected 集合：启动时扫描 structured/，把 source!='coarse' 的精层 key 收集起来，
  循环里 `if key in protected: skip`——即使 --rebuild 也绝不重抽/覆盖精层。
- 验证：protected 正确识别 3 篇精层（2T6H4S3D/AZJRUG6P/RBYA8XMB）。
- 工单 docs/待办与需求.md 中该条可标「已解决」。

### 改动2：清洗粗层 key_properties 结构漂移（本地 qwen 脏数据）
- 现象：175 篇粗层中 66 篇 key_properties 漂成 dict（本地7B 把数值塞进键或值，
  含 {'N/A':'N/A'}、{'Mn:xx':'N/A'} 键值倒错等，见踩坑 #13）。
- 修法：写清洗逻辑把 dict 拍平成 list[str]、剔除纯 N/A、丢弃>60字的叙述型脏项；
  synthesis_conditions 若漂 dict 也拍平成分号串。数据未丢，仅规整结构，无需重抽。
- 结果：清洗 66 篇，残留 dict 结构 0 篇，compare.md 重建（178 篇）。
- 备份：structured_bak_before_clean/（可还原）。

## 2026-07-24 · 新增 extract_batch.py（精层批量抽取，补自动 MineRU 衔接）

**背景**：extract_structured.py 缺 full.md 时直接跳过，不会自动解析。精读线的 watcher
虽有「有则复用、无则调 MineRU」逻辑，但精抽脚本没接上。补这一环。

**新脚本 scripts/extract_batch.py**：
- 对给定 key 列表：ensure_fullmd(key) → 有 full.md 复用；无则 find_pdf 定位本地正文 PDF
  （复刻 zotero_watcher.find_pdf 稳定逻辑，含 #15 的 SI 排除补丁）→ mineru_parse 落地
  到 library/<key>/parsed/ → 再走 extract_structured 的精层抽取（DeepSeek pro）。
- 解析结果与精读线共享同一 full.md（数据契约），谁先跑谁生成，另一方复用，不重复解析。
- 产物 source 无标记=精层，受 #16 保护，--rebuild 不会覆盖。
- 用法：python extract_batch.py KEY1 KEY2 / --file keys.txt

**模型选择**：精抽是「输入重、输出轻」的活（输出仅十几字段的 JSON），
用 deepseek-v4-pro——pro/flash 主要差在输出价，输出轻则 pro 几乎不增成本却更准。
（对照：精读线输出 9000 字长文，用 flash 省钱。原则：输出少上 pro，输出多上 flash。）

**首次应用**：PBS 方向 12 篇精抽（探针 5NIJVLMA 链路验证通过：
粗层 N/A → 精层抽出「内部损伤减少65%、损伤面积减少70%」等定量数值）。
MineRU 每日 500 篇免费额度，解析零成本；仅 DeepSeek 精抽花费几元。

## 2026-07-24 · Zotero 库去重 + 本地孤儿数据清理（保证数据源正确）

**背景**：用户反馈 Zotero 有重复。查重发现 191 篇里 17 组重复（某篇 Mouthguard 达5副本）。

**去重（Zotero Web API 删除）**：
- 规则：每组重复保留「有本地 full.md/summary/structured 数据」评分最高的副本，删其余。
- 删除 26 篇冗余副本，191 → 165 篇，零残留。删前逻辑保住了已精抽/精读的正确副本。

**清理孤儿数据**：
- 删文献后，其 structured JSON / library 目录成孤儿。扫描「structured 里但 Zotero 已无」，
  清理 24 个孤儿 JSON + 2 个孤儿 library 目录，移到 workflow_data/_orphan_trash/（可还原）。
- 附带清出 SXULKSWY/UDXDLNEY 两个更早清理遗留的历史孤儿。
- compare.md 重建：154 篇。最终核对：孤儿 0 个，本地与 Zotero 完全一致。

**新增工具**：scripts/upload_summaries.py（批量回写精读到 Zotero，补 deepread_batch 缺的回写）。

**遗留**：BMJEJ4ZY「Flexible Carbon Fiber…Polyborosiloxane Network」用户确认有正文PDF，
但 find_pdf 判定找不到——疑似 find_pdf 定位 bug，待查（未影响本轮，单列）。

## 2026-07-24 · 清掉三个非 API 工单（综述分流 / find_pdf / watcher看门狗）

用户要求：把工单板里除 API/密钥类外的待办都做掉（deepseek 密钥类留给用户自己弄）。

### #3 综述分流（已解决）
- SCHEMA 加 doc_type 字段（research/review）；build_compare_table 加 _is_review 判断
  （优先 doc_type，兜底标题特征词），综述分流到 compare_reviews.md，主表仅研究论文。
- 重建后：研究论文 149 篇进主表，5 篇综述（原全 N/A 噪声）分流。改善「看不到有用信息」。

### #5 find_pdf 优先信任 Zotero 规范命名（已解决）
- zotero_watcher.py + extract_batch.py 两处 find_pdf：优先选 title=="Full Text PDF"
  的规范正文（最可靠），文件名正则仅作未规范化条目的兜底。
- 验证：CABSSMLA（#15 踩坑篇，SI比正文大）、BMJEJ4ZY 均正确选中正文。

### #2 watcher 心跳 + 看门狗（已解决）
- zotero_watcher 主循环每轮写 workflow_data/logs/watcher_heartbeat.txt + 打「[心跳]」日志。
- 新增 scripts/watchdog.py：监控心跳，超 300s 无更新判定卡死→杀掉重启，带 180s 启动宽限。
- 验证：新鲜心跳判活、600s 旧心跳判死触发重启，逻辑通过。
- 用法：python watchdog.py 常驻（建议放任务计划/开机自启）。

**未动**：deepread 密钥 .env 配置（API/密钥类，用户自行处理，不提醒）。

## 2026-07-24 · 阶段1 模块化启动：建 modules/ + 首个基础件 zotero_client

**方向**（用户愿景）：每个功能模块做成「可独立成 GitHub 项目的基础件」，
复杂系统由基础件拼装。LLM 再进化，本质也是组合这些稳定基础操作。
路线：先轻量化重构（项目内理成独立目录+接口+README+自测），验证边界后再谈真拆仓库。

**首个样板 adapters/zotero_client/**（Zotero 接口基础件）：
- __init__.py：单一实现 find_pdf/get_fulltext/zget，配置从环境变量读（带默认，便于独立用）。
- README.md：接口/配置/规则说明。selftest.py：3 篇验证 find_pdf 选中正文，3/3 通过。
- 消除技术债：extract_batch.py 的 find_pdf 拷贝已删，改为 from adapters.zotero_client import find_pdf，验证正常。

**未动（下轮再收敛，降风险）**：zotero_watcher.py 的 find_pdf（返回2值、是生产关键脚本，
本轮不动）。技术债「find_pdf 拷贝」从 3 处→剩 watcher 1 处待收敛。

**样板形态**：modules/<件名>/{__init__.py 接口, README.md 说明, selftest.py 自测}。
后续模块照此推广（解析层 mineru / 精读 / 抽取 / 向量问答）。

## 2026-07-24 · 引入 Git 版本管理 + 立架构宪法

### 架构宪法（第一性原理）—— 平台最高纲领
- 新增 docs/架构宪法_第一性原理.md：定义「公理→定理→工作流」的有序构筑法。
- 三条铁律：识别不可再分的公理层、严格单向依赖永不循环、每层可独立验证。
- 立意：让不懂开发的主导者在「公理层+推导层」掌控系统，实现真正可控的 vibe coding。
- 维护哲学（用户定，要坚持）：后期维护=优化公理 + 优化公理到定理的推导过程。

### Git 版本管理
- git init + .gitignore（只管代码/文档/配置，忽略数据/备份/密钥/缓存）。
- 首次 commit f3c9a00：64 文件，作为「体系理顺后」的历史起点。
- 意义：替代手动 _bak 备份；每次改动可追溯、可回退，对「靠 LLM 维护」是根本保障。
- 后续（等体系拆干净）：再考虑推 GitHub、按模块拆独立仓库（呼应基础件拼装愿景）。

### 节奏
用户放缓推进、去读论文。设计侧后台按宪法逐步沉淀公理件（下一步：mineru→pdf_parse 基础件）。

## 2026-07-24 · 沉淀第二个公理件 adapters/pdf_parse

- 把 mineru_parse.py 的逻辑沉淀成公理件 adapters/pdf_parse（PDF→full.md+图坐标）。
  接口 parse_pdf(pdf, out_dir)（已解析则复用）、is_parsed()。MINERU_TOKEN 改从环境变量读（不硬编码）。
  README + selftest（不耗额度，验证接口契约），自测 3/3 通过。踩坑#1/#2 固化在注释里。
- extract_batch.py 改为 import 公理件（parse_pdf + find_pdf），不再 subprocess 调老脚本。
  验证：parse_pdf 来自 adapters.pdf_parse、find_pdf 来自 adapters.zotero_client。
- 公理层进度：Zotero定位 ✅ + PDF解析 ✅。下一个候选：LLM调用（散在各脚本，待收敛）。

## 2026-07-24 · 沉淀第三个公理件 adapters/llm_client（核心公理层成形）

- LLM 调用此前散在 9 脚本、6 函数各写各的（deepseek/ollama/call_llm/deepseek_json/
  ollama_json/llm_json），是全项目重复最重处 + 密钥坑#17 根源。收敛成公理件。
- adapters/llm_client：chat(纯文本) + chat_json(强制JSON)，支持 deepseek/ollama 切换，
  密钥/模型从环境变量读、可参数覆盖。含 think段过滤 + JSON 容错解析。
  自测 3/3（本地 Ollama 零成本验证 chat/chat_json + 缺key报错）。
- extract_structured.py 的 deepseek_json/ollama_json/llm_json 改为调公理件（保留名字兼容
  extract_batch 的 import）。端到端验证：真实精抽 5NIJVLMA 成功。
- **核心公理层成形**：Zotero定位 ✅ + PDF解析 ✅ + LLM调用 ✅。三条定理最依赖的原子能力已齐。
- 下一步候选：文本→向量(embed)、裁图(figure_crop)。其余脚本的 LLM 调用逐步接入公理件（不急，顺手改）。

## 2026-07-24 · 沉淀第四个公理件 adapters/embed；向量问答线接入

- embed/strip_references/chunk 此前在 vectorize.py + vectorize_library.py 各一份重复。
  收敛成公理件 adapters/embed（文本→向量 + 去参考文献 + 切块）。自测 3/3
  （本地 bge-m3 零成本验证 embed 返回1024维向量 + chunk + strip_references）。
- vectorize.py / vectorize_library.py 改为 import 公理件；vectorize_library 的 get_fulltext
  也改用 zotero_client 公理件。验证：vectorize 实跑正常（向量库9105块），语法/导入均OK。
- **公理层四块齐**：Zotero定位 ✅ + PDF解析 ✅ + LLM调用 ✅ + 文本向量化 ✅。
- 技术债：vectorize.py 是「import 即运行」老式写法（无 if __name__ 保护），以后顺手加。
- 剩余公理候选：裁图(figure_crop，在 deepread_v4 内)。之后精读线接入。

## 2026-07-24 · 沉淀第五个公理件 domain/figure_crop；精读线接入（公理层全齐）

- 裁图（精读线最复杂公理，踩坑#7）从 deepread_v4 沉淀成 domain/figure_crop。
  接口 crop_figures(parsed_dir)→图列表。自测：真实文献裁出6张完整图，b64 数据齐。
  踩坑#7 四个子问题的解法全固化在注释里（坐标裁图/page_size基准/合并三类块/纵向聚类不依赖题注）。
- deepread_v4.py 的 call_llm 改为调 llm_client 公理件（低风险接入；裁图散代码块暂未替换，记技术债）。
  端到端验证：真实精读 2IC4NWF4 生成 2.8MB HTML（含裁图+LLM）正常。
- **公理层五块全齐**：Zotero定位 ✅ PDF解析 ✅ LLM调用 ✅ 文本向量化 ✅ 裁图 ✅。
  三条定理线（精读/抽取/向量问答）均已开始接入公理件。第一性原理地基完成。
- 技术债：deepread_v4 的裁图散代码块可进一步替换成 crop_figures 调用（下次专门重构精读时做）。

## 2026-07-24 · 收尾：零散脚本接入公理件

- ask.py（问答）：embed→公理件、deepseek→llm_client。端到端实测：问"聚硼硅氧烷动态交联机制"
  答案准确（Si–O–B共价键+羟基交换）并附正确来源。日常问答功能正常。
- brainstorm.py：embed→公理件（chat 是多轮 messages 形式，接口不同暂保留，记技术债）。
- autotag.py：tag_llm→llm_client.chat_json。
- 至此项目主要 LLM/embedding 调用基本收敛到公理件，重复大幅消除。

**技术债汇总（不急，顺手还）**：
- deepread_v4 裁图散代码块可换成 figure_crop 调用。
- brainstorm 多轮 chat 接口需公理件支持 messages 形式后再接。
- vectorize.py 无 if __name__ 保护（import 即运行）。
- zotero_watcher.py 的 find_pdf 拷贝待收敛到 zotero_client。

## 2026-07-24 · 中间数据改回原生英文（用户校正：数据给LLM看，应英文）

**原则确立**：处理的是英文文献。中间数据（结构化抽取、图表数据）主要给 LLM/机器用，
应保持原生英文——避免"英译中"的精度损失和中英混杂噪声。只有终端给人看的
（精读HTML、问答答案）才用中文。

**改动**：
- extract_structured.py：SCHEMA 字段说明、SYS、prompt 全改英文，要求"值用英文"。
  验证：单篇 CQSKX3BB 重抽，输出纯英文且数值单位准确（weight loss 6.9 wt% 等）。
- chart_digitize：prompt 改全英文（图表数据纯机器数据）。
- 保持中文：deepread（精读，给人读）、ask（问答答案，给人看）。

**待办**：现有 154 篇结构化数据是旧的中文版，与新英文版会混在 compare 表里。
全库重英文重抽需花钱（154篇×DeepSeek），留待用户要用结构化分析时决定。已单篇更新的
CQSKX3BB/5NIJVLMA 是英文，其余仍中文——不影响功能，是一致性问题。

## 2026-07-24 · 图表数字化积木验证成功（本地视觉模型零成本）

- 下载本地视觉模型 qwen2.5vl:7b（适配 3060 12GB，专长图表/文档）。
- chart_digitize 端到端验证：真实 FTIR 图正确识别 line chart、坐标轴、3个数据系列及数据点，
  输出原生英文。本地跑，零 API 成本——适合几万篇批量。
- 补全 README + selftest（自测通过）。质量：本地7B是合理采样（找趋势/粗筛够用），
  精确逐点需更强模型，接口已支持换（底层可换上层不动）。
- 第6个公理件成形：chart_digitize（图表图片→数据点）。
- 云端高精度备选见 docs/视觉模型选择_参考.md（建议通义千问 qwen-vl，用户自弄key）。

## 2026-07-24 · llm_client 接入硅基流动（视觉对照实验准备）

- 为验证图表数字化"是模型太小还是方法问题"，接入硅基流动云端视觉模型。
- chat_vision 加 provider='siliconflow'（endpoint api.siliconflow.cn，key 从 SILICONFLOW_KEY 读，
  默认模型 Qwen/Qwen2.5-VL-72B-Instruct）。OpenAI 兼容格式，接入验证通（key 有效）。
- **受阻**：用户账号所有视觉模型返回 403 "Model disabled"（code 30003）——非代码问题，
  是账号未开通视觉模型调用权限。疑似需实名认证/账户余额，或代金券仅支持"网页在线体验"
  不支持 API。待用户在硅基流动后台确认开通后即可跑 72B 对照。
- 提醒用户：key 曾出现在对话中，建议后台重置。

## 2026-07-24 · 正文数据关联方向验证成功（小范围）

- 用户战略判断：正文已含精华数据，读图边际价值低，重心转向"吃透+关联正文数据"。
  图表数字化积木验证可用后搁置待命（深度分析时再启用）。
- 小范围验证：8篇用干净英文schema重抽（7成功），在其上做关联。
- **结论：干净数据→好关联，路通**。之前关联不出东西是数据脏（中英混杂+N/A），非方法问题。
  英文重抽后 bond 字段规范，关联模式清晰浮现：
  - 硼氧键(B-O)簇 → 性能全是冲击防护/热稳定，无高拉伸强度。
  - 氢键簇 → 出现 104.6 MPa 高拉伸、15 MPa 粘接、分子量数据。
  - 洞察：硼氧路线擅长冲击/热，氢键路线擅长强度/粘接；"给硼氧PBS引入氢键/相分离硬段
    做高强度"是潜在空白方向（单篇看不到、关联涌现的 idea）。
- 新增 docs/正文数据关联_方向说明.md（方向/路线/例子）。
- **下一步待用户定**：是否花钱全库英文重抽（154篇×DeepSeek，几块钱），拿到全量干净数据
  后做完整关联/知识图谱。小范围已证明值得。

## 2026-07-24 · 抽取加自我评估循环（借鉴 KnowMat，自己公理件实现）

**调研**：看了 LangExtract(Google)、KnowMat(材料专用) 等成熟抽取框架。
思路和我们一致（schema→LLM→JSON），但它们多了几个"提取更完全"的机制。

**已加：自我评估循环**（copy KnowMat 骨架，用 llm_client 实现，不依赖 LangGraph）：
- extract_with_eval：抽取后 _evaluate 对照原文查①漏抽②幻觉 → 有问题带反馈重抽（最多2轮）。
- 默认开，EXTRACT_NO_EVAL=1 可关（省钱）；本地模型跳过（评估不可靠）。
- 验证：CABSSMLA 跑通，key_properties=N/A 且评估未触发重抽=正确N/A（评估没为填满而幻觉）。
- 代价：每篇多1-3次LLM调用，较慢、稍贵，但换数据可信度。

**数据契约新增"可增量再提取原则"**（答用户问）：
- full.md=公理永久保留；提取结果=定理可重推。有新方法只需拿现成full.md重跑，不推倒重来。
- 靠 source/schema_ver/model 版本标记实现增量（只重抽旧版本、字段级增量）。

**待议的"提取更完全"两条路**（用户提出，尚未做）：
- A. 多趟抽取(multi-pass, 借鉴LangExtract)：分趟聚焦每趟抽2-3字段，比一次抽10字段更透。
- B. 类型自适应schema(用户直觉"特定类型特定注重点")：先判子类型(电池/力学/合成/表征)，
  用对应强化schema。现在一个通用schema抽全部，是表征类论文抽得少的根因。

## 2026-07-24 · watcher 开机自启（解决"打了标签没反应"）

**问题**：用户打「待精读」标签久无反应——watcher 没在跑（12:06后停了，非自启、无人启动）。

**解决**（做成开机自启，以后彻底不用管）：
1. 密钥永久化：setx DEEPSEEK_KEY / ZOTERO_API_KEY 为用户级环境变量（重启仍在），
   watcher 自启时能自读，根治#17密钥坑。
2. Windows 任务计划 ZoteroLiteratureWatcher：登录时自启 watchdog.py，由看门狗拉起并
   守护 watcher（一举两得：开机自启 + 卡死自动重启）。状态 Ready。
3. 确认 watchdog 用 env=dict(os.environ) 启 watcher，密钥继承链路通。

**效果**：以后开机自动跑，打「待精读」即自动精读，卡死自动重启。
当场验证：启动后立即捞到用户的 Mouthguards(X6VLD2ES)，MineRU解析完成、精读进行中。

**关联工单**：待办板"watcher加心跳+看门狗""deepread密钥.env"两条基本落地
（密钥改用永久环境变量，比.env更省事）。

## 2026-07-24 · 沉淀第7个公理件 pipelines/paper_discovery（找文献·方向补库）

- 用户提醒"找文献积木还没做"。其实有基础（find_papers.py，OpenAlex+去重），
  升级成干净公理件 pipelines/paper_discovery。
- 用途聚焦：方向补库。接口 search(query, limit)→结构化文献列表（title/doi/year/
  authors/venue/abstract/cited/is_oa/in_library），复用 zotero_client 查库里已有去重。
- 自测通过：真实搜"polyborosiloxane self-healing"返回5篇、字段完整、正确标记"库里已有1/5"。
- 数据源 OpenAlex（免费无key、覆盖广、含摘要引用OA状态）。
- 未来扩展（采购清单待办）：related(doi) 走 Semantic Scholar 引用网络（顺藤摸瓜）；
  自动导入 Zotero 需走 Web API（本地API只读#8）。
- 公理层达 7 块：zotero_client/pdf_parse/llm_client/embed/figure_crop/chart_digitize/paper_discovery。

## 2026-07-25 · 自启任务修正 + 看门狗自愈验证成功

**发现问题**：昨天建的自启任务从未运行（LastRunTime=1999，LastTaskResult=267011"尚未运行"）——
因为触发条件只有 AtLogOn，而用户一直没重新登录。今早跑的 watcher 其实是昨晚手动启的那个，
10:11 停了后无人接管。

**修正**：任务改为**双触发**——AtLogOn + 每小时重复一次（MultipleInstances=IgnoreNew，
已在跑则忽略新实例）。这样不重启电脑也能最多1小时内自愈。

**看门狗自愈完整验证成功**（日志实证）：
- 10:16:19 看门狗启动（阈值300s/检查60s）
- 10:17:19 检测"心跳已327s未更新(>300)→判定卡死，重启"→ 已重启 watcher ✓
- 三重保障：任务计划(登录+每小时) → 看门狗 → watcher；watcher挂最坏5分钟自愈，
  看门狗挂最坏1小时被任务计划拉起。

**用户那篇 Mouthguards(X6VLD2ES)**：精读早已完成，summary.html 3.5MB，标签已翻已精读。

## 2026-07-25 · 服务改为后台静默运行（不再弹黑窗口）

**问题**：用户看到弹出两个 python 黑窗口，想关掉——那正是看门狗和 watcher，关了服务就停。
但它们不该打扰用户（又丑又易误关），是设计疏忽。

**修正**：
- watchdog.py 启动 watcher：CREATE_NEW_CONSOLE → **CREATE_NO_WINDOW** + stdout/stderr DEVNULL。
- 任务计划执行程序：python.exe → **pythonw.exe**（无窗口版），看门狗自身也静默。
- 验证：看门狗静默在跑；20:39:56 自动判死并"已重启 watcher（后台无窗口）"，
  watcher 心跳恢复正常。全程无窗口弹出。

**用户须知**：以后不会再看到黑窗口；服务在后台默默跑，打「待精读」照常自动精读。

## 2026-07-25 · 问答失败排查：Ollama 未启动 + 环境变量未继承（踩坑#4变体）

**现象**：为验证"新对话能否直接用"，实测 ask.py 问答 → urllib 连接错误。

**根因（两层）**：
1. **Ollama 服务根本没在跑**（重启后无自启机制）——问答依赖它做向量检索。
2. 启动后模型列表为空（"失明"）：OLLAMA_MODELS 用户级/系统级都设对了
   （D:\02_AI\models\Ollama\models，实有4个模型），但**当前 PowerShell 会话未继承**
   （setx 设的变量需新会话才生效），Start-Process 启的 ollama 拿到空值 → 去默认路径找 → 空。

**解法**：
- 用 `cmd /c "set OLLAMA_MODELS=...&& ollama serve"` 显式注入再启动 → 4个模型全可见。
- 验证：问答恢复正常，答案质量高（硼酸酯键/硼氧六环两种动态机理+来源）。
- **根治**：新建自启任务 `OllamaService`（登录+每小时保活，固化正确 OLLAMA_MODELS）。

**CLAUDE.md 已加排查指引**：问答报错先查 Ollama 是否在跑、模型列表是否为空。

**教训**：验证"新对话能不能用"时必须实测端到端，否则用户会碰壁。依赖的本地服务
（Ollama）和平台服务（watcher）一样需要自启+保活。

## 2026-07-25 · 清理 Zotero 自动标签（用户：没用还多余）

**背景**：用户发现库里大量自动标签，认为多余，要求只留「待精读」「已精读」。
查证：autotag.py **并未在自动运行**（不在四个自启任务里，也无脚本调用），
现存标签是早先手动跑过一次的存量。

**执行**：
- 先备份全部标签 → workflow_data/backup/zotero_tags_backup.json（158条目）。
- 删除 690 种分类标签（type/149次、mechanism/393、topic/518、method/145、material/295）。
- 结果：707种/1539次 → **11种/31次，分类标签残留 0**，74篇全部成功、零失败。

**踩坑**：首次清理脚本用 `/items` 取条目，前100条全是附件/笔记 → 误判"0条需清理"、
静默空转。改用 **`/items/top`**（只取文献条目）后正确识别77篇。
教训：Zotero API 的 /items 含附件笔记，要文献条目必须用 /items/top。
另：后台脚本要加 `python -u` 关缓冲，否则日志不落盘、看不到进度。

**autotag.py 已标记弃用**（顶部加警告），防止将来误跑。

## 2026-07-25 · SI（补充材料）价值验证：值得挖，且对合成工作直接有用

**背景**：用户问"SI 重要吗、是否该有专门解析"。调研前沿（MOF/reticular materials 合成挖掘）
证实：**合成描述常分散在 SI 里**，需正文+SI 一起才构成完整数据；行业标准做法是
**先段落分类过滤、再精抽**（应对 SI 噪声大），MOF 上可达 90-99% 精确率。

**实测验证**（CABSSMLA 的 SI，2.4MB→MineRU 解析出 2.1万字符）：
- SI 结构：Materials / Synthesis / Measurements / 23张补充图 / 4张补充表。
- **确证 SI 含正文没有的关键合成细节**：
  「PDMS-OH (5 g, 1.19 mmol), DBA (0.058 g, 0.655 mmol), DMSO/IPA (2 mL, v/v=4:1)，
   室温搅拌6h → 80°C真空24h」+ **原料 Mw=4200 g/mol** + 各试剂供应商
  + 对照组设计逻辑（PBS-OH按羟基摩尔量等当、PBS-B按硼原子摩尔量等当）。
- 另有高价值定量数据：Table S2/S3 计算的 B-O 键能(eV)、完整流变主曲线/WLF位移因子、
  不同应变的应力松弛曲线、¹¹B NMR 四配位硼环境解析。
- 注意：本篇 SI 的合成段较简略（mmol仅出现2次），价值更多在表征数据与理论计算值。
  不同期刊 SI 详略差异大。

**库存量**：43 篇带 SI（占173篇的1/4），其中 PBS 方向 6 篇。title 多已规范为"SI"，识别可靠。
**格式注意**：有的 SI 是 .docx（如 adfm70535-sup-0001-suppmat.docx），MineRU 只吃 PDF，需另处理。

**顺带修复**：MINERU_TOKEN 未永久化导致解析失败 → 已 setx 永久化（补上之前只永久化
DEEPSEEK_KEY/ZOTERO_API_KEY 的遗漏）。

**结论/建议**：SI 该作为**独立的一条线**处理（不混入精读，避免污染），用专门 schema
抽合成细节（投料量/摩尔比/温度/时间/后处理/原料Mw）。待用户决定是否建这条线。

## 2026-07-25 · SI 处理线建成（第8个公理件 si_filter + SI精读 + 合并）

**用户定的设计**：生成独立、展示合并。
- 生成独立：正文精读与 SI 精读各自单独生成 → **后补 SI 时不必重跑正文精读**（用户关切点）。
- 展示合并：merge_summary.py 拼成一份 HTML（正文在前、SI在后），Zotero 一个附件看全。

**新增**：
- `domain/si_filter`（第8公理件）：SI 噪声过滤，三档 drop/core/brief/keep。
  用规则不用LLM（宪法·稳定的自己做），零成本可解释。对齐前沿的 paragraph classification。
- `scripts/si_deepread.py`：定位SI附件→MineRU解析→过滤→DeepSeek生成"实验细节精读"→裁补充图渲染HTML。
- `scripts/merge_summary.py`：合并正文+SI精读，回写 Zotero。

**踩坑（重要）**：si_filter 初版正则 `(\$\^?\{?\d+\}?\$.*){2,}` 判"作者行"过于粗暴，
**误杀了最关键内容**——`PDMS-OH, Mw = 4200 g/mol` 那段（试剂+分子量）、¹H/¹¹B NMR 方法段、
键能计算公式段（都含上标 $^{1}$ 被当成作者名单）。
修法：①作者行正则收紧为"人名 人名$^{n}$, 人名 人名"格式 ②单位行必须是"编号. XX College of"整段
③**加保护规则 _PROTECT_PAT**：含 Mw/Mn/purchased/定量/公式的段落绝不丢弃。
教训：**过滤规则宁可少丢，不可误杀**；每次改过滤必须回头验证关键信息还在。

**实测效果（CABSSMLA）**：SI 21473字符→过滤后19545（丢15段纯元信息，关键信息全保）。
生成4574字实验细节精读，含：所有试剂规格+供应商+分子量、精确投料
（PDMS-OH 5g/1.19mmol + DBA 0.058g/0.655mmol + DMSO/IPA 2mL 4:1，室温6h→80°C真空24h）、
各复合材料制备克数（0.5g CNT+10g PDBS；4g PDBS/50mL甲苯:IPA=9:1，浸渍3min，质量比1:2）、
对照组设计逻辑（PBS-OH按羟基等当/PBS-B按硼原子等当）。**这些正文完全没有。**
合并版 summary_full.html 15MB / 21图 / 章节：导读→引言→实验→讨论→总结→文献信息→SI实验细节。

**待办**：①合并版15MB偏大，可考虑压缩补充图 ②接入 watcher（打标签时自动带SI）
③库里43篇带SI，PBS方向6篇，可批量跑

## 2026-07-26 · 标签系统升级为状态机（用户设计）

**从"动作标签"升级为"状态标签"**：标签本身表明这篇处理到什么程度。

| 触发 | 检测 | 动作 | 完成后状态 |
|------|------|------|-----------|
| 打「待处理」| 只有正文 | 精读正文 | **正文精读** |
| | 只有SI（罕见备用）| 精读SI | **SI精读** |
| | 正文+SI 都没精读 | 都精读+合并 | **全文精读** |
| | 已有正文精读 + 有SI | **只补SI**（不重跑正文，省钱）| 正文精读 → **全文精读** |
| | 无任何PDF | 提示 | **无附件** |

**实现**：
- zotero_watcher.py：TRIGGER_TAG 改「待处理」；新增 TAG_MAIN/TAG_SI/TAG_FULL/TAG_NOPDF。
- `set_state_tag()` 替代 swap_tag：状态互斥（移除所有旧状态标签只留一个），保留用户其它标签。
- `has_si()` 检测SI附件；process_item 重写为状态机：分别判断正文/SI 是否已精读，只补缺的，
  最后按实际完成情况置标签，两者都有则调 merge_summary 合并。
- 旧标签迁移：已精读→正文精读、待精读→待处理，21篇全部成功。

**实测验证**（两个关键场景都通过）：
1. 全新文献：`正文PDF:有 SI:无 | 已精读 正文:否` → 精读→抽取→回写→`[状态] 待处理 → 正文精读` ✓
2. **已精读过的**：`已精读 正文:是` → `[跳过正文] 已有精读，不重跑` ✓（用户要的省钱逻辑）

**排查记录**：中途出现"待处理2篇却不处理"，原因是停旧watcher后看门狗尚在180s宽限期内、
新watcher未起，**非逻辑问题**。看门狗随后正常拉起。

## 2026-07-26 · 安全化与地基收口（据外部审计意见执行）

**背景**：用户请 GPT 审计了整个项目。其指出的问题大部分属实，本轮做"安全化+地基收口"，
优先于继续加功能。**风险等级修正**：审计建议按"已泄露"处理并清理 Git 历史，
但实测 `git remote -v` 为空——仓库**从未推送任何远端**，故按"预防性治理"执行，不重写历史
（重写历史本身是高风险操作，能免则免）。

### 1. 密钥去硬编码（最严重问题，已解决）
- 实测 23 处硬编码（14 py + 3 bat + 3 n8n备份json）。全部替换为从配置读取。
- **新增公理件 core/config**：`get_key()` 加载顺序 = 环境变量 → 项目根 `.env`。
  一举根治踩坑 #17/#19 反复三次的"setx 只对新进程生效、长驻进程拿不到密钥"问题。
- 生成 `.env`（已 gitignore）+ `.env.example`（进版本库供换机参考）。
- n8n 备份 json 里的密钥已脱敏为 `<XXX_REDACTED>`。
- 一次性改造脚本 `_desecret.py` **用完即删**（它必须含明文才能替换）。
- 验证：源码扫描 0 处明文密钥；59 个文件语法全通过；关键脚本 import 正常。

### 2. 文档漂移修正
- 说明性文档（README/架构总览/Zotero闭环使用说明/换设备重装指南/结构化抽取说明/CLAUDE.md）
  统一更新为新状态机标签：`待处理` → `正文精读`/`SI精读`/`全文精读`。残留 0 处。
- **历史文档（变更记录/踩坑记录）刻意不改**——它们是当时的事实记录，改了会失真。
- CLAUDE.md 更新：7 块公理件 → 9 块；密钥说明改为 .env 方案；加健康检查指引。

### 3. 依赖固化
- 探测出实际第三方依赖仅 3 个（chromadb / python-docx / PyMuPDF），其余全标准库
  ——印证"少依赖"原则。生成 `requirements.txt`（锁版本 + 用途说明 + 外部服务清单）。
- Python 3.13.12。

### 4. 测试补齐 + 一键体检
- 补上缺失的自测：`core/config/selftest.py`、`domain/si_filter/selftest.py`
  （后者含**踩坑回归测试**：验证含上标的科学内容不被误判为作者行而丢弃）。
- 修正 `llm_client` 自测：安全改造后 key 会从 .env 回退，"传空必报错"的旧断言已失效，
  改为测"密钥链路正常"。
- **新增 `scripts/health_check.py` 一键体检**：语法/密钥安全/配置/Zotero/Ollama/
  公理件自测/自测覆盖/数据资产/后台服务，共 9 项。慢测试（chart_digitize）默认跳过，
  `--full` 可跑；每个自测限时 60s 防卡死。
- **最终体检：9 通过 / 0 警告 / 0 失败。**

### 尚未做（明确记录，不假装完成）
- Git 历史里仍有旧密钥（因未推送远端，风险可控）。若将来要公开仓库，需先轮换密钥或清理历史。
- 密钥本身未轮换（用户可自行在各平台重置）。
- n8n docker-compose 仍用浮动 tag、允许任意外部 Node 模块——n8n 已非主链路（Zotero闭环直连），
  优先级低。
- 端到端流程测试（状态机/失败恢复/重复附件/SI合并）尚无自动化用例，目前靠 health_check 冒烟。

## 2026-07-31 · 全项目系统排查（用户反馈开机仍弹窗）

**触发**：修了 Ollama 弹窗后开机仍弹 → 用户要求全项目检查。

**A. 弹窗根治**（踩坑 #26）：任务 -Hidden 对控制台程序无效。
最终用 `powershell -WindowStyle Hidden -Command "Start-Process ... -WindowStyle Hidden"`
双层隐藏。四个任务现全部无窗口：pythonw(2) + powershell Hidden(1) + GUI(1)。
watchdog 加固：强制用 pythonw 启 watcher。

**B. 代码扫描**：无残留 CREATE_NEW_CONSOLE 等弹窗写法。

**C. 数据一致性**（全部干净）：
- library 31 篇全部有精读；有精读但缺结构化数据 0 篇
- SI 半成品（解析了没生成精读）0 篇；孤儿合并版 0 篇

**D. 清理**：8 个临时文件（_*.py / _*.out / _*.txt）已删，并加 .gitignore 规则防再犯。

**E. 磁盘**：library 722MB（精读HTML内嵌base64图，最占）/ 向量库 117MB / 结构化 0.2MB。

**最终体检：10 项通过 / 0 警告 / 0 失败。**


## 2026-08-04 · 修复精读质量崩塌（DeepSeek V4 思考模式）

**问题**：用户反馈「最近的文献精读质量特别差，有的还只是把图贴过来了」。

**根因**：V4 思考模式默认开启，推理链计入 max_tokens=8000，正文被挤没。详见踩坑 #27。

**改动**：
- `adapters/llm_client/__init__.py`：`chat()`/`_deepseek()` 加 `thinking` 开关；
  加截断检测（finish_reason=length 且正文过短 → 抛错而非静默返回）。
- `scripts/deepread_v4.py`：max_tokens 8000→32000、默认关思考、失败升级到 64000+开思考；
  输入截断 40000→150000；新增 MIN_OK=3000 底线校验，不达标直接失败退出不写盘。

**模型选型结论（回答用户「新模型是否更划算」）**：
维持 flash 做精读。官方定价 flash 输入 $0.14 / 输出 $0.28 每百万 token；
pro 输入 $0.435 / 输出 $0.87 —— pro 贵约 3 倍。精读是典型「输出重」任务，
flash 在正确的 max_tokens 下输出质量已达标，无需上 pro。
沿用宪法原则：输出少的活上 pro（结构化抽取），输出多的上 flash（精读）。
V4 真正的红利是 1M 上下文 —— 已用来取消输入截断，这对长文献的机理/结论部分是实质提升。


### 补充：全库体检与二次修复（同日）

**全库扫描**：54 份精读中，正文<3000字的废品 5 份（涉 4 篇文献：
HBCS74IC 536字、LNTAJLUB 2059字、4SPHXFL8 2378字、X6VLD2ES 2531字），
其余中位数 8401 字属正常。说明本 bug 影响面有限，只毁了最近处理的几篇。
经用户确认后用修好的流程重跑这 4 篇覆盖。

**验证**：HBCS74IC 重跑 536字 → 9932字 + 7 图，导读/引言/机理/结论/文献信息结构完整，
结尾不再截断（旧版是写到一半戛然而止）。

**顺带发现并修复**：验证期间 DeepSeek API 返回 503，暴露 llm_client **没有服务端抖动重试**。
已补：5xx/429 退避重试 4 次（5s/10s/20s），4xx 立即抛错不浪费时间。实测退避 3 次后成功。
这条对后续「几千几万篇」的批量处理是刚需 —— 长批量任务撞上一次 503 就会白跑一篇。


### 补充2：4 篇废品精读已修复并推回 Zotero

重跑结果（全部达标，图数不变）：
HBCS74IC 536->10132字 | LNTAJLUB 2059->9843字 | 4SPHXFL8 2378->9789字 | X6VLD2ES 2531->9669字
旧版均保留为 summary.html.bak，可随时还原。

新增 `scripts/refresh_summary_file.py`：把新版 summary.html 铺进 Zotero 本地 storage。
**只改文件内容、不动条目与版本号**，因此不触发同步冲突（沿用踩坑#18 的教训）。
4 篇均已刷新，用户在 Zotero 点开即最新精读。

体检：10 项全绿。
（过程中体检又抓到我一个失误：用 PowerShell Out-File -Encoding UTF8 写 py 文件会带 BOM，
导致语法检查失败。教训：写代码文件用 Write 工具，不要经 PowerShell 转手。）


## 2026-08-06 · 新增控制面板（scripts/panel.py + 控制面板.bat）

**动机**：用户不懂代码，此前「哪个服务挂了、进程在不在跑、想换模型/密钥」全靠来问我。
做成面板后这些日常运维他能自助完成。

**架构定位（重要）**：面板是**定理件/视图层，不含任何业务逻辑**。
状态直接复用 `health_check` 的检查函数，配置走 `core.config`，进程走系统查询。
一行判断逻辑都不重写 —— 否则就会出现「面板说正常、体检说异常」的两套真相。
任何新功能都应先做成积木、再由面板调用。

**能力边界（刻意划的线）**：
- 可做（可逆、零风险）：看状态/进程/日志/最近处理记录；改密钥与模型；重启后台服务。
- 不可做（不可逆或花钱）：删数据、全库重抽、改 Zotero 库、触发批量精读 —— 继续走人工确认。
  理由：面板上一个按钮点错的代价，远大于多问我一句的成本。

**顺带解决的老问题**：模型名散落在 6 个脚本里 hardcode，且部分用 `os.environ.get` 读 ——
而 `.env` 的值只喂给 `get_key()` 不进环境变量，**改了 .env 根本不生效**（隐蔽陷阱）。
现收敛到 `core.config.get_model()`，三级兜底（环境变量→.env→内置默认），
面板改一处即全局生效。已实测：面板改精读模型 → deepread_batch 立即读到新值。

**新增 core.config 能力**：`set_keys()`（原子替换+自动备份）、`get_model()`、`mask()`。

**安全**：只绑 127.0.0.1（外部机器访问不到）；密钥只显示后 4 位；
留空表示「不改动」而非「清空」（已实测：提交空密钥不会清掉已有配置）；
`.env.bak*` 已加入 .gitignore（备份含明文密钥，绝不进版本库）。

**验证**：6 个数据采集函数全通；HTTP 页面 200；写配置后下游脚本读到新值；体检 10 项全绿。

**面板上线即发现一个真实问题**：又出现 2 个 zotero_watcher 重复实例（当日第二次）。
已在面板里加「⚠重复实例」标记。根因（看门狗重启时旧进程未清干净）待修，记入待办。


## 2026-08-06 · 积木化规范：每个文件夹自带说明书，可被单独选中干活

**用户的真实诉求**（原话）：「我每次要改某个积木或项目时，可以精准选到那个项目，
而不是现在这样只能把所有文档的大文件夹选进去」。

也就是说，目录结构不只是给人看的分类，它决定了**LLM 的工作单元粒度**。
用户选中哪个文件夹，那个文件夹就是 LLM 的全部世界 —— 所以每个文件夹必须自足。

**做了三件事**：

1. **目录按功能重组**（见上一条）：7 个中文命名的工作流文件夹 + modules 积木层。
2. **每个文件夹配 `CLAUDE.md`**（共 17 份：7 个工作流 + 10 块积木）。内容包括：
   这块是什么、各文件职责、依赖哪些积木、**血泪教训**、改完怎么验证。
   并统一写明「要改别处请让用户改选那个文件夹」，避免 LLM 在错误的地方动手。
3. **新增 `项目导览.md`**（给人看的地图）+ 根 CLAUDE.md 增加目录说明与路径修正。

**面板新增两个视图**：
- 「项目组成」：列出各文件夹是什么、有几个脚本、说明书齐不齐 —— 用户据此决定选哪个文件夹
- 「积木」：10 块的能力一览，**每块可一键自测**（只读操作，已做路径穿越防护）

设计要点：面板显示的说明**直接读各文件夹的 CLAUDE.md 首段**，不另存一份。
否则会出现「面板上写的」和「LLM 读到的」两套说法。

**验证**：体检 10 项全绿；面板实测 7 个文件夹 + 10 块积木全部识别；
一键自测实测 proc_lock/config/si_filter 均 4/4 通过；非法名称被正确拒绝。


## 2026-08-06 · 新增 subproc 积木：根治弹窗与中文乱码

**起因**：用户反馈桌面周期性闪蓝色终端窗口。

**排查**：全项目 17 处子进程调用只有 1 处加了不弹窗标志；
周期性的两处正是面板（15 秒）与看门狗（60 秒）。

**做法**：新建公理件 `core/subproc`（第 11 块），统一「不弹窗 + 超时 + UTF-8」。
面板、看门狗、proc_lock、体检全部改走它；其余偶发调用补上静默标志。
看门狗顺带把已弃用的 `wmic` 换成 PowerShell CIM 查询。

**顺带揪出一个长期误诊**（踩坑 #32）：中文乱码不是「显示问题」，
是子进程编码问题，真的在损坏数据。已在 subproc 内统一注入 UTF-8 环境变量。
现在体检报告终于是中文的了。

**防复发**：体检新增「无弹窗」检查项，扫描全项目揪出裸 subprocess 调用。

**一处自我纠错**：改看门狗时我一度加了「重启前删锁文件」，
随即意识到若 taskkill 失败（旧进程仍活着），删锁会让新实例照样启动 →
又回到两份并存的老毛病。已移除，改为信任 proc_lock 的僵尸锁自动识别。

**验证**：体检 11 项全绿（新增无弹窗项）；11 块积木自测 10/10 通过；
subproc 自测在 python 与 pythonw 两种环境下均 5/5 通过。


## 2026-08-07 · 重启后排查：修复静默失败与依赖自愈

用户重启电脑后要求检查。发现 Zotero 未随开机保持运行，导致精读线静默停摆 19 分钟
（心跳正常但日志空白）。详见踩坑 #33。

**改动**：
- `zotero_watcher`：轮询异常写入日志（原来打到被丢弃的标准输出）+ 人话诊断
  + 连续失败计数提醒 + 恢复提示；单篇失败允许下轮重试
- `auto_sync`：新增 `_revive()`，依赖服务挂了主动拉起并复检，不再只是「跳过本轮」
- `panel`：新增故障告警栏，把「仍未恢复的失败」直接摆在最上面
- ZoteroApp 登录触发器加 1 分钟延迟

**验证**：实测关掉 Zotero → 日志有明确记录、面板出现告警；
auto_sync 7 秒内自动拉起；恢复后日志打印「已恢复」；体检 11 项全绿。


## 2026-08-09 · 密钥升级到系统凭据库（行业标准做法）

**用户问**：行业上密钥用什么方案安全保存。

**结论与分档**：
- ① 明文配置文件（.env + gitignore）—— 开发期最低要求，我们原来在这档
- ② **操作系统凭据库**（Windows 凭据管理器 / macOS 钥匙串）—— **桌面应用的行业标准，我们该在这档**
- ③ 云端密钥管理（Vault / AWS Secrets Manager）—— 服务器与团队场景，对单机个人项目是过度工程

**实施**（Python `keyring`，后端 WinVaultKeyring，已实测可用）：
- `config` 新增 `keyring_status/key_location/migrate_secrets_to_keyring`
- `get_key` 加载顺序改为：环境变量 → **系统凭据库** → .env
  （凭据库排在 .env 之前，迁移后 .env 里的明文可安全删除）
- `set_keys` 区分两类：**密钥进凭据库，模型/路径等普通配置进 .env**。
  两者要求本就不同 —— 密钥要保密且无需人读；配置要能被人看懂、跟项目走。
- 面板显示每个密钥「存在哪」（🔒凭据库 / ⚠明文 / 环境变量 / 未配置），
  有明文时给出「迁移到系统凭据库」按钮

**自己发现并修掉的一个漏洞**：`.env` 的备份机制是直接 copy，
于是**迁移那一刻反而生成了一份含明文密钥的备份**，正好抵消了整件事的意义。
改为脱敏备份（密钥写成 `<已脱敏>`）—— 备份的目的是还原配置，密钥在凭据库里不需要备份。

**体检新增「密钥存放」项**：检查是否已入库、是否有明文残留（含 .env.bak 等残留文件）。
实测它准确揪出了 5 个历史备份文件，已清理。

**实测验证**：迁移 3 个密钥 → .env 中密钥消失 → `get_key` 仍能读到 →
真实调用 DeepSeek API 成功 → 后台服务（另一进程）也正常读取。体检 12 项全绿。

**仍待办（需用户操作）**：这几个密钥曾以明文进过 Git 历史（3 个提交），
按安全惯例**应视为已泄露并重新生成**。换个更安全的柜子不等于旧钥匙就安全了。
用户重新生成后，在控制面板直接填入即可（会直接进凭据库，不再落明文）。


## 2026-08-09 · 清理 Git 历史中的旧密钥

**背景**：三个密钥曾以明文进过 Git 历史。用户已在各平台重新生成并存入系统凭据库，
旧密钥已作废，因此本次清理没有实际泄露风险，目的是让仓库具备公开条件
（历史里残留失效密钥会被 GitHub 的扫描器报警，也不专业）。

**做法**：`git filter-repo --replace-text`，把三个旧密钥值替换为 `***REMOVED***`。
选择保留历史而非重开，是因为 52 次提交的信息本身有价值
（每条 commit message 都记录了当时的判断与取舍）。

**操作顺序（不可逆操作的标准流程）**：
1. `git bundle create --all` 做完整备份（含全部分支与历史，可完整还原）
2. 精确提取历史中的密钥值（而非用正则猜），确认只有 3 个
3. 执行重写
4. **三重验证**：精确值搜索无命中、`sk-` 模式搜索无命中、替换标记出现 52 次
5. 验证工作区文件与目录结构完好、体检 12 项全绿

**结果**：52 个提交全部保留，历史中已无任何密钥痕迹。
备份文件在 `D:\02_AI\Docker\Projects\_repo_backup_*.bundle`，确认无误后可删。

**注意**：所有提交的哈希值已改变（历史重写的必然结果）。
本仓库无远程仓库，故无影响；若以后有协作者，需重新克隆。


## 2026-08-09 · 整理根目录，清出历史包袱

**起因**：用户说打开项目文件夹「感觉有点乱」，并问项目文件管理有没有行业规范。

**结论**：有惯例，但比模板更重要的是背后那条原则 ——
**根目录是门面，只放「进入这个项目必须先知道的东西」，实现细节一律往下沉一层。**
判据：陌生人打开根目录，应该十秒内知道这是什么、从哪开始。

**排查发现：根目录一半是历史包袱，不是放错位置**：
- `n8n_data` 148MB —— n8n 时代的数据库，平台早已改为纯 Python
- `wf_backup`、`docker-compose.yml` —— 同属 n8n 遗留
- `b` —— 空文件夹
- `zotero_backup_20260723_*` —— 一次性备份
- `启动Zotero闭环.bat` —— **已损坏**：还在提示用 setx 设密钥（密钥现已在凭据库）、
  还写死了 Ollama 路径；且 watcher 早已开机自启，不需要手动启动
- `启动精读服务.bat` —— 调用的是归档里的旧版脚本

以上全部移出到项目外 `_归档_文献平台旧版本/`（移动而非删除，可逆）。

**其余整理**：
- `重跑精读_PRO版.bat` 归入 `文献精读/`，并重写（去掉过时的密钥提示）
- `项目导览.md` 合并进 `README.md` —— README 是行业惯例的门面名，
  GitHub 会自动展示；两份定位重叠的说明文件迟早会各说各话
- 各处 CLAUDE.md 中的「项目导览.md」引用同步更新

**结果**：根目录从 20 项降到 17 项（10 个文件夹 + 7 个文件），且每一项都有存在理由。
体检 12 项全绿，控制面板正常。

**关于「屎山」的判断**（回答用户的疑问）：腐坏不是必然，但**是复利的** ——
每次「先放这儿吧」只花两秒，代价几个月后才显现。防住它靠三件事：
明确的放置规则、自动检查、定期清理。本项目前两条已具备
（各文件夹的 CLAUDE.md 是规则，health_check 是检查），这次是第三条。


## 2026-08-09 · 上线 GitHub 私有仓库 + 目录规范化搬迁

**1. GitHub 私有仓库**
`github.com/chen2994957404-droid/zotero-literature-platform`（PRIVATE）。
云端核验：118 个文件、`.env` 未上传、数据目录 0 文件、本地与远端同一提交。
意义：项目不再只存在于一块硬盘上。私有仓库在 GitHub 免费版即不限量。

**2. 目录搬迁**
`D:\02_AI\Docker\Projects\n8n-literature-workflow`
→ `D:\02_AI\Projects\zotero-literature-platform`

旧路径三层里有两层是历史包袱（Docker 早已不用；n8n 早已不用）。
遵循的惯例：**路径浅、语义准、无空格无中文、代码与数据分离**。

搬迁顺序（先推 GitHub 再搬，等于有了云端安全网）：
停服务 → 移动 → 更新 2 个自启任务路径 → 全面验证。
验证结果：体检 12 项全绿、Git 远程正常、密钥正常（在凭据库，与路径无关）、
看门狗/面板/watcher 均从新路径正常启动。

**3. 文档清理**
`docs/` 从 18 份精简到 12 份。归档 6 份已被各文件夹 CLAUDE.md 取代的旧说明
（含两份 n8n 时代的「使用说明」，还在教用户 `docker ps`）。
**过时文档比没有文档更糟** —— 它会让人和 AI 都读到错误信息。

留下的 12 份都是活的：架构宪法、变更记录、踩坑记录、待办、数据契约、
架构总览、演进规划、API 实测规格，以及几份调研参考。


## 2026-08-09 · 接入 Sciverse：新增「搜全球」与「问全世界」两项能力

**背景**：用户发现 MineRU 同厂（OpenDataLab）的 Sciverse ——
面向科研 Agent 的证据数据层，4.55 亿条记录、3000 万篇 AI-Ready 全文。

**先实测再决定**（未直接写代码）：拿用户真实方向验证覆盖质量 ——
`polyborosiloxane` 命中 250 篇且与库内已有文献对得上；
问「PBS 剪切硬化机理」直接返回原文片段 + 页码，相关度 0.97。**确认值得接。**

**新增第 12 块公理件 `adapters/sciverse`**（4 个接口 + 片段清洗 + 429/5xx 退避重试 + 自测）。
**为什么包一层**：Sciverse 尚处公测、接口会变，按宪法首要判据
「发展中的用现成，但把变化隔离在稳定接口背后」—— 上层只认本模块的函数。

**两个用户入口**：
- `找新文献/search_global.py`：搜全球并标出库里已有。
  实测搜 polyborosiloxane，12 篇里准确认出 4 篇库内已有、8 篇新的高被引文献。
  与既有 `find_papers.py`（走免费 OpenAlex）并存，没配密钥时仍可用后者。
- `库内问答/ask_world.py`：问全世界，返回带编号出处的中文回答。
  与 `ask.py` 互补：ask 答「我读过的文献怎么说」（深），ask_world 答「全世界怎么说」（广）。
  **约束模型只准根据取回的片段作答**，不许用自身知识补充 —— 可追溯是本工具的价值所在。

**实测中发现并修复的关键问题**：中文检索式会让召回质量崩塌（详见踩坑 #35）。
已改为「中文提问 → 转英文检索 → 中文作答」，并按相关度 0.60 过滤噪声。
修复后同一问题的证据相关度从「6/8 低于 0.6」变为「8/8 高于 0.89」。

**验证**：sciverse 自测 5/5；两个入口均用真实科研问题实测；体检 12 项全绿。


## 2026-08-09 · 找文献闭环：新增 lib_match 积木与 discover 主入口

**设计判断（本次的核心）**：用户原本设想「内部库」与「外部 API」是并列的两部分。
我提出的修正是：**两者不是并列，是闭环** ——

```
外部检索 → 【与我的库对照】 → 排序决策 → 入库 → 精读 → 回到对照层（参照系更准）
```

理由：外部检索谁都能调（OpenAlex/Sciverse 都是公开 API）。
**本平台唯一不可替代的是：只有它知道用户已有什么、读过什么、在做什么方向。**
如果外部结果不经过对照就直接呈现，它就退化成又一个 Google Scholar。
用户认同这一点：「最不易被替代的还是我们自己设计的适合我们个性化的这部分」。

**新增第 13 块公理件 `pipelines/lib_match`**：
- 回答两个不同问题：「这篇我有没有」（去重）与「这篇值不值得读」（相关度）
- 两层判别：精确层（DOI/标题）+ 语义层（标题摘要向量 vs 本地向量库）
- **核心主张：高被引 ≠ 对你有用。** rank() 权重 相关度0.6 / 被引0.25 / 新鲜度0.15，
  被引数还做开方压缩，避免超高被引一家独大
- 降级完备：Zotero 没开只丢精确层，Ollama 没跑只丢语义层，都不会让功能失效

**新增主入口 `找新文献/discover.py`**：搜全球 → 对照 → 排序 → 提示入库触发精读。

**实测基准**（改动后应对得上）：库内真实文献 relevance≈0.96 判为 have；
无关文献（拜占庭钱币冶金学）relevance≈0.54 判为 new。区分度足够。

**实测中发现并修复**：Sciverse 把补充材料当独立文献返回（详见踩坑 #36）。

**刻意没做的事**：没有做网页界面。命令行 + 控制面板已够用，
**过早做 UI 会锁死对流程的想象** —— 等用户实际用一两周，知道真正需要什么筛选再说。

**验证**：lib_match 自测 6/6、sciverse 自测 5/5、体检 12 项全绿（13 块积木）。


## 2026-08-09 · 找文献加入「决定层」：搜到 ≠ 要收，收了 ≠ 要读

**用户提出**：「中间还应该有个决定层，我决定导不导入，精不精读还是我自己来」。
这个要求是对的，而且暴露了原设计的两个问题。

**问题一（我的错）**：`import_by_doi.py` 里的 DOI 是**写死在代码里**的
（上一轮批量导入留下的一次性脚本），它根本不读命令行参数。
而我在 discover.py 的提示里写着「运行 import_by_doi.py <DOI>」—— 那条指令是无效的。
已重写为真正的通用工具（函数化 + 支持 argv + 可选标签）。

**问题二**：原流程要用户从列表里手抄 DOI 再逐篇跑命令。这不叫「决定权」，这叫麻烦。

**新增 `找新文献/collect.py`（决定层）**：
- discover 把结果存进 `workflow_data/_last_search.json`，用户按**编号**挑，不用抄 DOI
- 支持 `1,3,5-7` 这类范围表达式、`全部`、`--看` 回看列表
- **两个决定分开**：默认只收进库不打标签；加 `--精读` 才触发精读
  （精读要花钱，不该被顺手触发 —— 这正是用户强调的点）
- 执行前列出清单并要求确认；导入前实时查库，已有的跳过

**实测中发现并修复**：改 stdout 编码的写法有坑，导致导入成功却在打印时崩溃，
进而诱发重复导入（详见踩坑 #37）。已修正 11 个文件，并清理了因此产生的重复条目。

**验证**：编号解析、取消、防重复、真实导入四条路径全部实测通过；体检 12 项全绿。


## 2026-08-09 · 查询扩展：解决「搜不全面」的根本问题

**用户反馈**：「我在试我的平台的时候感觉还是有点局限不太全面」，
并问当前 AI 辅助文献检索的优秀框架是怎么设计的。

**调研结论**（PaperPilot / PaSa / SPAR 等前沿工作的共同设计）：
1. **查询分解与扩展** —— 把研究问题拆成领域概念、术语变体、约束，
   生成多个适配数据库的查询串，**而不是拿用户原话直接搜**
2. **引用图扩展** —— 前驱/后继/同辈/基准/综述 五个方向遍历引用网络
3. **多轮迭代** —— 保持状态、累积已用查询、用已得结果指导下一轮
4. **重排与集合运算** —— 多源结果做交并差后统一打分
5. 结果排序 ← 我们已有（且 lib_match 比通用工具更贴合本用户）

值得注意：那篇论文的对比表里，连 Elicit 这类商业工具在**引用图扩展**上也标为不支持。
所以这不是本平台落后，是该领域整体还在演进。

**诊断**：用户「不全面」的主因是**术语覆盖**。材料领域同一事物有大量别名
（polyborosiloxane / PBS / borosiloxane elastomer / shear stiffening gel / silly putty…），
单一检索式必然漏掉用其他叫法写的那批。

**新增第 14 块公理件 `pipelines/query_expand`**：
- `survey` 模式求全（术语变体/同义词/上下位概念）；`problem` 模式求准（现象/机理/解法/指标/体系）
- 两条硬约束：第一个检索式永远是原式（防扩展跑偏丢掉本意）；LLM 不可用时降级不失效
- 顺带消除了 ask_world 里重复实现的中译英逻辑

**discover.py 改为多式检索**：合并去重 + **各式新增贡献统计**。
后者直接回答「搜得够不够」——最后两式几乎无新增即判定「基本搜到底」，
反之提示「可能还没搜全，可加 --扩展 8」。这是系统综述里判断停止的标准做法。

**实测效果**：
- 「polyborosiloxane shear stiffening」：单查询约 20 篇 → 4 式合并 **45 篇**，
  每式新增 10~12 篇，**说明单查询漏了近 70%**
- 「材料常温流动怎么解决」（problem 模式）：拆出 cold flow / creep /
  shape retention additive 等角度 —— 这些是用户自己未必想得到的切入点

**验证**：query_expand 自测 5/5；两种模式各用真实问题实测；体检 12 项全绿（14 块积木）。

**待办**：引用图扩展（snowballing）尚未做入口，但 sciverse.paper_relations 已封装，
且实测用户库中 93% 文献 Sciverse 有元数据 —— 这是下一个高性价比改进。


## 2026-08-09 · 宪法新增【零号判据】：先看真实世界，别用记忆代替调研

**用户洞察**（原话）：
> 世界上那么多人，我能想到的各个方面很可能都是有人做的。所以做这些项目之前，
> 先看看有没有什么比较规范完备的准则，再看看目前有没有人做、做得好不好；
> 做得好的话我们做个性化部分再整合；做得不好或者没人做，再具体分析难度、意义、价值。
> **你下意识回答我的往往还是预训练的结果，我们还是要主动去看真实实时的世界是怎么样。**

**为什么它排在【首要判据】之前**：
首要判据回答「知道有什么之后，自己做还是用现成」；
零号判据回答「在此之前，世界上到底已经有什么」。**判断的前提是事实，不是记忆。**

**四步（互穿而非瀑布）**：找准则 → 找现有实现 → 评估边界与适用性 → 决定。
看了别人的实现常会改变对「自己要什么」的理解，允许回头修需求。

**对 LLM 的强制要求**：涉及外部世界现状、具体数字、外部 API 行为的判断，
**必须查/必须测**，不许凭记忆断言。

**立此条时的现场例证**（写进宪法，让后来者知道不是空话）：
- 反例：我曾断言「MineRU token 通常两周过期」。当场查证 —— 官方只说
  「有效期需关注平台通知，过期后重新获取」，**没有任何具体天数**。
  那个「两周」是编的：一个听起来精确、实则无据的数字。
- 正例：用户问「Sciverse 这类 API 自带查询扩展吗」，没有靠推理，
  而是读文档发现 `agentic-search` 有 `sub_queries`（默认 0），
  再实测 0 vs 4 —— 结论是「有但默认关，且本场景提升不明显（前五条结果完全相同、慢 60%）」。
  这个结论任何一方的记忆里都得不出，只能测出来。

**自查方法**（已写进最高指令）：说完一句判断，问自己 ——
这是**刚查到的**，还是我**本来就"知道"的**？后者一律打问号。

**落地**：宪法第三节新增【零号判据】完整章节；第八节「最高指令」新增第 0 条与第 6 条；
根 CLAUDE.md 顶部新增同名章节（因为新对话只自动加载 CLAUDE.md，
**写在宪法里却没人读到，等于没写**）。

**顺带修正**：根 CLAUDE.md 里密钥管理一节仍写着「.env 文件」，已更新为系统凭据库。


## 2026-08-09 · 雪球扩展：召回率从 50~95% 推到 90~100%

**按【零号判据】走的完整一轮，过程本身值得记录**：

**1. 找准则** —— 检索到系统综述领域的实证研究，给出了明确量化：
```
单个数据库检索              召回 13~35%
+ 优化检索式                召回 50~95%   ← query_expand 已做到
+ 一轮前后向雪球            召回 90~100%  ← 本次补上
```
另两条可操作结论：后向雪球提召回、前向雪球提精度（都要做）；
**雪球成败取决于种子集质量**，且必须与数据库检索混合使用，不能单用。

**2. 找现有实现并实测选型** —— 直觉是「既然用了 Sciverse 就一路用到底」，
实测推翻了它：
| | Sciverse meta-paper-relations | OpenAlex |
|---|---|---|
| 返回 | 只有 OpenAlex ID，标题为空、无 DOI | 标题/DOI/年份/被引俱全 |
| 49 条参考文献 | 需再查 49 次，约 3 分钟 | **一次批量请求 1.0 秒** |
| 限流 | 30 次/分钟 | 免费无密钥 |
按直觉做会产出一个「一篇种子要 3 分钟还拿不到标题」的废功能。

**3. 新增第 15 块公理件 `adapters/snowball`**（走 OpenAlex）：
前后向扩展、批量取元数据、摘要倒排索引还原、429/5xx 退避重试、自测 6/6。

**4. 种子集自动化 `lib_match.pick_seeds()`** —— 从用户库里挑与主题最相关的 N 篇。
**这一步是结构性优势**：别人做雪球得手工构造种子集，我们有现成的库和向量索引。

**实测效果**：「polyborosiloxane shear stiffening mechanism」
从 45 篇 → **182 篇**，其中雪球独贡献 131 篇关键词搜不到的。

**实测中发现并修复的关键缺陷**（详见踩坑 #38）：雪球一开，排序被高被引通用文献带偏。
已引入「贴题度」与贴题门槛，滤掉 79 篇跨方向文献后，前五名全部对口。

**同时写入宪法的推论**：创新性工作不要吝啬调研与验证的投入
（用户语：「如果产出无作用才是最大的浪费」），
但配了明确边界 —— **唯一停止判据：这次调研能不能改变接下来的做法**；
调研要瞄准决策的分叉点，而不是把一个领域读通。


## 2026-08-09 · 找文献搬进控制面板：与精读拉平「零命令行」体验

**用户点出的落差**（原话）：「文献精读现在我可以完全脱离 LLM 对话平台使用，
文献查询现在是怎样调用」。

对比很清楚：
| | 触发方式 | 摩擦 |
|---|---|---|
| 精读 | Zotero 打个标签 | 近乎为零（他本来就在 Zotero 里） |
| 查询 | 开终端 → cd → 敲中文路径命令 | 高，他多半宁愿来问我 |

**对不写代码的人来说，命令行不算「脱离对话平台」，只是换了个更难用的界面。**

**做法**：
1. 把 discover 的整条流程抽成 `run_discovery()` —— **命令行与面板共用同一份逻辑**。
   （早先 import_by_doi 就因为逻辑写死在脚本里无法复用，导致我给出过一条无效命令。）
2. 面板新增「找文献」卡片，放在最上面（这是他最常用的功能）：
   输入主题 + 模式选择 + 雪球种子数 → 后台任务 + 每 2 秒轮询进度 →
   结果表格（勾选框、相关度条、贴题/近库分数、引用源头/跟进工作标记、
   「与你库中某篇最接近」）→ 勾选后「收下」或「收下并精读」。
3. **两个决定分开**：收进库 vs 是否精读。后者要花钱，单独按钮 + 二次确认。
4. 长任务用「发起 → 轮询 → 取结果」，同一时间只允许一个检索任务（避免并发烧额度）。

**实测**：完整流程 55 秒（4 个检索式 → 36 篇 → 雪球 +131 篇 → 滤掉 74 篇跨方向
→ 最终 93 篇，其中新文献 73 篇）。
边界情况逐一验证：空选、库里已有（跳过不重复导入）、无效 DOI（报失败不崩溃）、
真实导入成功。体检 12 项全绿。

**关于「何时该做 UI」的回顾**：几轮前我建议「先别做界面，等用一阵知道真正需要什么」。
这次正是那个时机 —— 需求已经具体到「输入主题、看列表、勾选、收下」四件事。
**过早做 UI 会锁死对流程的想象；等流程稳定了再做，界面就是自然的。**


## 2026-08-09 · 修复用户搜「PBS」暴露的三个问题

一次真实使用翻出三个 bug（详见踩坑 #39）：

1. **崩溃**：`discover.py` 漏 `import re`。→ 体检新增「未导入的模块」检查（ast 交叉核对），
   已用故意造错验证能抓到。
2. **缩写歧义**：「PBS」被扩展成铅锡焊料 / 钪钽酸铅铁电体。
   → `query_expand` 新增 `context` 参数，由 discover 从用户库取 5 篇最相关标题作领域上下文。
   **这是本平台独有的信息** —— 我此前说「API 层不知道你是谁所以做不了术语扩展」，
   而修法正是把「我们知道你是谁」用上。
3. **贴题门槛杀光结果**：拿三字母缩写当贴题基准 → 全判不贴题 → 0 篇。
   → 基准改用全部扩展式拼接；并加兜底：门槛杀到不足 5 篇时自动逐级放宽并告知。
   **内部阈值不该让用户看到 0 篇**，那是最容易误导人的失败方式。

**验证**：搜「PBS」→ 88 篇（新 75 篇）全部对口；体检 13 项全绿。


## 2026-08-09 · 精读质量评测集：把主观判断变成系统能自动算的标准

**背景**：用户问「用 LLM 用得牛的人怎么用、我在哪个阶段」。
查证后（Gartner「context engineering is in, prompt engineering is out」、
规格驱动开发的 CLAUDE.md 实践）给出的判断是：他在上下文工程与系统化上做得扎实，
**最大的缺口是没有评测集** —— 所有自动检查都在验「能不能跑」，没有一项验「好不好」。

**用户的方案演进（他自己迭代出了更好的设计）**：
1. 最初想法：加「看完了」「正在看」标签 + 质量评价标签，评价用完自动消失
2. 我指出的风险：**三个维度不能混进同一个互斥状态机** ——
   若「读完」覆盖「全文精读」，系统会以为没精读过而重跑（花钱）
3. **他随即提出更好的**：「或者以另一个窗口的形式……那个窗口就可以塞下一个
   比较完美合适的评价体系，而不是只能局限标签」

最终设计正是第 3 条：**标签只做触发，面板做评价界面**。

```
Zotero 里只打：在读 / 读完        ← 干净，就两个，系统只读不改
控制面板「精读评价」：            ← 完整评价体系
   自动列出「读完但未评价」的 → 可直接打开精读 → 就地评价
```
评价**不回写 Zotero**，存本地 `workflow_data/evalset.json`。

**新增第 16 块公理件 `adapters/evalset`**：
- `snapshot()` 算客观指标：字数、图数、**数值密度**、章节完整性
- `save()` 记录评分时**连同当时的客观快照** —— 这是关键：
  没有快照，评分只是孤立分数；有了快照，才能回答「什么客观特征对应用户认为的好」
- `stats()` 对比好/差两组的客观指标差异，并判断样本是否够做校准

**面板新增「精读评价」卡片**：待评队列、二元评价 + 6 类可选差评原因 + 备注，
并提供 `/summary?key=` 直接在浏览器打开本地精读（带路径穿越防护）。

**实测**：给一篇已精读文献打「读完」→ 出现在待评队列（含快照 10508字/5图/27数值/6章节）
→ 提交评价 → 从队列消失、评测集 +1 → 确认「全文精读」标签**未被覆盖**（两标签共存）
→ 清理测试数据还原现场。非法评分被拒、路径穿越被拦截。体检 13 项全绿（16 块积木）。

**过程中的一个自我教训**：改完 panel.py 直接重启，导致面板启动失败
（在 `b''` 字节串里写了中文）。**这个错体检的语法检查本来就能抓到，我没跑就重启了。**
写了检查却不用，等于没有。


## 2026-08-10 · 交接文件：解决「换个对话框就丢记忆」

**用户痛点**：「我一直很苦恼现在只能在一个对话框，但是换个对话框又要丢失记忆」。

**查证后的标准做法**（不是我拍脑袋）：`CLAUDE.md` + `HANDOVER.md` 两件套 ——
前者给「这个项目是什么」，后者给「我们做到哪了、试过什么、卡在哪」。
另一条同源的警告：**CLAUDE.md 太长会导致指令被忽略**，
而本项目的 CLAUDE.md 已经 9.9 KB，接近风险线。

**诊断本项目的记忆现状**：
| | 大小 | 新对话能读到吗 |
|---|---|---|
| CLAUDE.md | 9.9 KB | ✅ 自动加载 |
| 变更记录.md | 75.6 KB | ❌ 太长，且不会自动读 |
| 踩坑记录.md | 47.5 KB | ❌ 同上 |
| 待办与需求.md | 5.7 KB | ❌ **而且已过时**（还列着早修好的 watcher 重复实例） |

结论：新对话知道「架构和规矩」，不知道「我们正在干什么」。

**关键设计决定：交接文件必须自动生成。**
理由是本项目已经三次证明「靠人（或 AI）记得更新的文档一定会过时」——
待办过时、CLAUDE.md 的密钥说明过时、docs 里留着教人跑 `docker ps` 的 n8n 教程。

所以 `平台管理/交接.py` 的内容**全部从系统真实状态抓取**：
git 提交历史、体检实际结果、评测集进展、待办、最近踩的坑标题、积木与工作流清单。
**没有一处需要人手写，也就没有过时的可能。**

**落地**：
- `平台管理/交接.py` → 生成根目录 `HANDOVER.md`
- CLAUDE.md 顶部新增「新对话第一件事：读 HANDOVER.md」
- 控制面板「精读评价」卡片底部加「生成交接文件」按钮（约 40 秒，含跑一次体检）

**验证**：命令行与面板按钮均生成成功；内容核对无误（13 项体检、10 条提交、
16 块积木、7 个工作流、5 条最近踩坑）；体检 13 项全绿。


## 2026-08-11 · 框架化第一步：代码规范 + CLI 积木 + 试点脚本（DeepSeek Harness 环境）

**用户目标**：功能已完善，现在做「框架化 + 代码优化」，给项目一个固定代码格式。
**工作环境变化**：迁移到 DeepSeek Harness 环境（开发副本机器）。体检发现该环境
**没有 Python、没有网络、写不进工作区之外**（winget 损坏、python.org 连不上、
本地代理端口全关），项目在真机的「自测→体检→实跑」验证闭环在此环境**无法执行**。
因此本步只做静态框架层，不做批量代码迁移（迁移见待办工单，须在有 Python 的环境执行）。

**体检（79 个 .py，框架化的依据）**：

| 项 | 数量 |
|---|---|
| `sys.path.insert`（塞项目根进 import 路径） | 78 处 / ~50 文件，写法 ≥10 种 |
| 手写 `sys.argv` 解析 | 97 处，argparse = 0，风格 ≥10 种 |
| `sys.stdout.reconfigure` 复制粘贴 | 15 处 |

**落地**：
1. 新增 `docs/代码规范_标准脚本模板.md` —— 项目唯一代码格式准绳：标准开头 9 行模板
   （向上找「含 modules/ 的目录」= 项目根，统一塞路径 + UTF-8）、统一 CLI（core/cli）、
   配置纪律、错误处理纪律、积木准入三件套、验证纪律、命名约定。
2. 新增第 17 块积木 `core/cli` —— `pos/flag/opt/opts/positionals`，只用标准库；
   不引入 argparse（符合宪法「能自己做的稳定部分自己做」）。附自测 6 组用例。
3. 试点 `找新文献/find_papers.py` 按模板重写 —— 函数化 + core/cli 取参 + 标准开头，
   去掉 `_sys/_os` 补丁式 import 与 lambda 降级；**行为与输出格式不变**；
   无参时由 IndexError 崩溃改为打印用法（改进）。
   实跑通过：`python 找新文献/find_papers.py "polyborosiloxane" 4`（OpenAlex 543 篇命中，输出正常）。
4. 根 CLAUDE.md 第二部分新增「代码规范」三条红线（改任何 .py 先读规范文档）。

**环境收尾（本机）**：这台 DSH 机器原本没装 Python、TUN 代理正常但沙箱禁 TLS（schannel 凭据被限制）。
已装 Python 3.13.15 到 `D:\Softwares\Python313`（用户批准 + 扩权安装）；pip 依赖装齐；
Python 自带 OpenSSL 不走 schannel，沙箱内 HTTPS 正常（实测 python.org 200）。

**验证结果（本机）**：
- `core/cli/selftest.py` **6 组全过**（首跑抓到 bug：`pos()` 把 `--limit 20` 的值 `20` 当位置参数；
  已修 —— 约定「位置参数必须在 `--` 选项之前」，规范文档同步更新）；
- `find_papers.py` 实跑 OpenAlex 正常；
- `health_check.py`：语法 81 文件全过，8 通过 / 4 警告（Zotero、Ollama、自启任务未配置，新机器预期）/ 1 失败（缺密钥，未配置）；
- `交接.py` 已刷新 CLAUDE.md 结构树（积木 16→17）与 HANDOVER.md。

**仍待办**：
- 批量迁移其余 ~40 个脚本（清单见 `docs/待办与需求.md`，本机已具备验证能力）；
- 真机（项目运行机）拉取后跑一次体检确认。


## 2026-08-11 · 框架化第二步：批量迁移 40+ 脚本到标准模板（全量验证通过）

**做法**：4 个安全文件夹（库内问答 / 数据抽取 / 找新文献 / 库房维护）由 4 个子代理并行迁移，
2 个敏感文件夹（文献精读 / 平台管理，含常驻服务）由主流程手工最小改动。
**只改写法、不改行为**；git 提交全部由主流程统一做（避免并发冲突）。

**迁移内容**（36 个文件，+1521 / -1029）：
- 全部换成「标准开头」9 行模板 —— 消灭 78 处手写 `sys.path.insert` 与 15 处 `sys.stdout.reconfigure` 复制；
- 命令行参数全部改走 `core/cli` —— 消灭 97 处手写 `sys.argv`；
  有兄弟脚本互相 import 的（collect/si_batch/refresh_summary_file/merge_summary/upload_summaries 等）
  在标准开头后保留 `sys.path.insert(0, SCRIPT_DIR)`；
- 密钥/模型/路径统一走 `core/config`（get_key/get_site/get_model/need_site），无新增 hardcode；
- 裸 `except` 全部补「为什么安全」注释；
- `deepread_v4` 整段流水线移入 `main()`（被 watcher/rerun_pro/deepread_batch 按固定参数调用，行为不变）；
- `zotero_watcher` / `watchdog` / `panel` / `health_check` / `交接` 等常驻/工具脚本只换开头与配置入口，逻辑一字未动。

**验证**（本机 `D:\Softwares\Python313`）：
- 71 个文件 `py_compile` 全过；
- `health_check`：语法 81 文件全过、未导入模块 0、无弹窗 0、运行时导入 7/7 ——
  **8 通过 / 4 警告 / 1 失败，与迁移前基线完全一致**（1 失败 = 缺密钥、4 警告 = 未配
  Zotero/Ollama/自启任务，均属新机器状态，与迁移无关）；
- 体检的「未导入模块」检查当场抓到 5 处「用了 os/sys 但没导入」（库房维护子代理漏了 import 行），已修并复检通过；
- `core/cli` 自测 6 组全过。

**教训**：py_compile 只验语法，验不出「用了但没导入」—— 这正是体检 `c_undefined_names`
（踩坑 #39 的产物）存在的意义。子代理产出必须过一遍体检才算完成。

**待办**：真机拉取后跑一次 health_check 确认；`modules/` 各 selftest 的标准开头统一为
walk-up 版属可选（现有写法正确）。

**教训**：标准开头**不能按 CLAUDE.md 找项目根** —— 每个子文件夹都有自己的 CLAUDE.md，
会找错层；必须按「含 `modules/` 的目录」找项目根（已在规范文档中写明）。

## 2026-08-24 · 运用侧

**在用户新电脑（本机）跑通 Zotero 验证**
- 确认 Zotero 9.0.6 安装在 D:\Softwares\Zotero（注册表 D:\Zotero 是残留），数据目录 D:\date\ZoteroData
- 用户重新配置同步后，附件从云端开始下载，363 个附件目录已有 165 个含真实 PDF
- 写入 .env：ZOTERO_USER_ID=0（本地 API 用 0 代表当前用户库）、ZOTERO_STORAGE=D:\date\ZoteroData\storage
- zotero_client 自测 3/3 通过（CABSSMLA/BMJEJ4ZY/2T6H4S3D 均正确选中正文、排除 SI）；get_fulltext 通
- 体检：Zotero 服务 OK；公理件自测 8/16→10/16（zotero_client、paper_discovery 过）
- 剩余失败与 Zotero 无关：embed/llm_client 等缺密钥或 Ollama；后台自启任务尚未建

## 2026-08-24 · 运用侧

**双机协作搭通（主仓库 chen2994957404-droid）**
- 起因：本库从 chen2994957404-droid 账户 clone，但本机 origin 曾指向 balabala-1314 仓库，两仓库并存
- 方案：主仓库定为 chen2994957404-droid/zotero-literature-platform；本机 origin 改指主仓库，原 balabala-1314 仓库保留为 backup remote
- 授权：在 GitHub 将 balabala-1314 添加为主仓库协作者（Write 权限），接受邀请后推送成功
- 已推送：e696c32..0490e48（含本机 Zotero 验证记录 + README 双机协作说明）
- README 新增「两台电脑协作」章节：pull→改→commit→push 流程、协作者授权方法、各机自配项（密钥/.env/workflow_data 不进库）


## 2026-08-25 · 运用侧

**llm_client 本地聊天修复（模型更新后 404 + 假卡死）**
- 起因：用户本地 Ollama 模型更新为 qwen3.5（+bge-m3），llm_client 默认还找 qwen2.5:7b-instruct → 404
- `adapters/llm_client/__init__.py`：`_cfg()` 与 `chat_vision()` 的模型名改走 `_cfg_get`（config 三级读取，
  .env 生效）；`_ollama()` options 固定 `think:False`（实测 qwen3.5 思考模式+中文 system 会卡 90s+）
- `.env`：新增 `OLLAMA_MODEL=qwen3.5:latest`（备份 .env.bak.20260825）
- `adapters/llm_client/selftest.py`：ollama 测试的 system 消息改英文，3/3 秒过（关联踩坑 #41）
- 验证：llm_client 自测 3/3；其余积木自测 6/8 通过（evalset/pdf_parse/proc_lock 为沙箱环境假失败，
  逻辑本身全过）
- 另：控制面板首次启动失败排查（用户双击.bat 后 127.0.0.1 拒绝访问）——面板代码实测可正常绑定
  HTTP 200（python 与 pythonw 两种方式都验证过），怀疑为用户会话内 pythonw 启动异常，待用户重试；
  发现两个僵死 pythonw 进程（PID 11556/21280，17:02/17:06 启动，0 CPU、不监听端口），身份待确认

- 控制面板打不开排查（用户双击.bat 后浏览器拒绝连接）：面板代码实测可正常绑定 HTTP 200
  （python 与 pythonw 两种方式），用户会话内 pythonw 启动即静默失败/挂起，原因不可见
  → 新增 `平台管理/panel_launch.py` 启动器：捕获 import 期与 main() 期一切异常写入
  `平台管理/panel_launch.log`；`控制面板.bat` 改为调用启动器（不再裸调 panel.py）
  → 下次打不开时日志直接给出 traceback。清理两个僵死 pythonw（PID 11556/21280，
  疑似失败的面板实例，用户已确认）

- 面板 pythonw 启动路径在用户会话不可靠（双击.bat 时 pythonw 未执行到启动器，连日志都没有；
  控制台 `python 平台管理/panel_launch.py` 一次成功）→ `控制面板.bat` 改用
  `start "" /min python`（最小化控制台），并加 `_launch_probe.log` 启动探针（ASCII 路径）
  区分「bat没跑」vs「python报错」；面板本身无代码改动（用户控制台运行已确认可用）
- 新增加速入口 `精读监听.bat`：`start /min python 文献精读/watchdog.py`（看门狗自动拉起
  watcher 并看护，心跳超300s自动重启）。起因：排查面板时误杀两个 pythonw（PID 11556/21280，
  实为正在轮询的 watcher，心跳停于 09:14:07；关联踩坑 #42）
- 待办升级：本机 4 个自启计划任务（ZoteroLiteratureWatcher/OllamaService/ZoteroApp/
  LiteratureAutoSync）均不存在，watcher 长期以手动进程方式运行——建议后续用任务计划
  注册（登录自启+每小时保活，符合架构），需用户确认

**更正（关于计划任务）**：上一条说「本机 4 个自启计划任务均不存在」是**误判**——
那是我的受限环境查不到任务计划（Get-ScheduledTask/CIM 被沙箱拦截）造成的假象。
实际 OllamaService / ZoteroApp / LiteratureAutoSync / ZoteroLiteratureWatcher 四个任务
一直在，且都是「登录 + 每小时保活」触发。watcher 09:37:37 正是被保活触发自动拉起、
检测到心跳停摆（1428s = 09:14 误杀时刻）后自愈恢复的 —— 保活机制是项目设计的核心。

**本次注册/调整**（用户已确认）：
- 新增 `ControlPanel` 计划任务：登录时 pythonw 启动 `平台管理/panel_launch.py`
  （面板现随登录自启，验证 HTTP 200 正常；pythonw 在任务上下文可用，坏的是 .bat 的
  cmd/start 链路）
- `ZoteroLiteratureWatcher` 被我用 Register-ScheduledTask -Force 覆盖注册过（命令相同：
  pythonw 文献精读/watchdog.py），随后**补回每小时保活触发器**（Once+重复1h），现与
  其他服务任务配置一致（登录 + 每小时）
- 清理：_launch_probe.log 探针机制废弃（启动器 panel_launch.log 自记录），探针行已从
  控制面板.bat 移除

## 2026-08-25 · 设计侧

**MCP 化开工：库房层 MCP 服务建成（MCP服务/）——调研结论 + 首个服务落地**
- 背景：用户提出把平台能力拆成 agent 可调用的 MCP。按零号判据调研 3 个现成 Zotero MCP
  （nealcaren/mcp-zotero、alisoroushmd/zotero-mcp、drxaibi/zotero-mcp，克隆暂存
  `.mcp-eval-tmp/` 待处理，未进 git）。结论：**库房层「借鉴重写」优于搬运**——
  我们已有 adapters/zotero_client 这块在真实库磨出来的公理件，MCP 只是薄壳；
  0-star 仓库搬运后要自己维护（fork 即自维护）；且写操作要与 watcher 标签状态机协调，
  外部 server 的写会绕过现有并发控制。**分析层（知识图谱/撤稿核查等）用户明确不做**。
- 新增 `MCP服务/`（宪法「界面层」，非公理件，故不入 modules/）：
  - `mcp_stdio.py`：手写 MCP stdio 协议层（JSON-RPC 2.0 + 换行分隔，**零第三方依赖**）。
    决策：不用官方 mcp SDK —— 平台「少依赖」宪法 + 协议已实测稳定（官方 SDK 的 ReadBuffer
    按 \n 切帧、序列化 = JSON+'\n'；部署版 types.js 支持协议版本含 2024-11-05，握手已对通）。
  - `zotero_server.py`：10 个只读 MCP 工具封装 adapters/zotero_client（ping/library_stats/
    search_items/get_item/find_pdf/get_fulltext/list_collections/get_collection_items/
    list_tags/get_recent_items）；工具面与本地 API 细节（q/qmode/tag/collection 参数、
    Total-Results 头）参考 nealcaren/mcp-zotero；`python MCP服务/zotero_server.py --list`
    给人看工具清单。
  - `selftest.py`（协议层 21 项离线自测）+ `CLAUDE.md`（含 v1 边界）。
- **v1 边界：只读**，不写 Zotero 任何数据、与 watcher 无并发冲突；写操作（打标签/改名/
  去重/清理）留 v2，届时 dry-run + 显式确认参数 + 与 watcher 状态机协调（已写进 MCP服务/CLAUDE.md）。
- 验证（全部实测）：
  - selftest 21/21 过；
  - health_check 13/13（85 文件语法过，16/16 公理件自测过，未碰坏任何现有模块）；
  - 真实库连通：188 顶层条目 / 181 合集 / **全库仅 2 个标签**（全文精读 26、正文精读 11，
    已直查本地 API 确认是真实数据）；
  - search 'polyborosiloxane' 返回 3 篇真实文献（含标签/DOI/作者）；get_item(7H8B5JIL)
    经 find_pdf 正确定位正文 PDF（I94A85YR，正确排除 SI docx 附件）；
  - 交接.py 已刷（结构树收录 MCP服务/）。
- 待办：接真实 MCP 客户端（DSH / Claude Code）端到端实测；写操作 v2；
  `.mcp-eval-tmp/` 三个克隆保留还是删除待用户定。

## 2026-08-25 · 设计侧（续）

**MCP 服务接入 DSH（HMR 热加载生效，无需重启）+ 清理调研克隆**
- 删除 `.mcp-eval-tmp/`（三个调研克隆，未进 git，评估已完成，用户拍板删除）。
- DSH 侧接入（改的是 **Harness 配置，不在本仓库**）：
  `C:\Users\Administrator\.dsh\profiles\web\cordis.patch.yml` 新增 mcp-client 行
  `mcp-zotero`（serverName=zotero；command = Python313\python.exe + MCP服务/zotero_server.py；
  toolCallTimeoutMs=120000；failOnStartupError=true）。web profile 启动器会**通过 HMR
  热加载 cordis.patch.yml**——实测确认补丁写入后无需重启，`Tool.listTools` 立即可见
  10 个 `mcp__zotero__*` 工具（中文描述完好，协议握手与工具同步成功）。
- 验证链路全通：`node ...\@deepseek-ai\dsh\lib\bin.js --profile web --dump-config`
  干跑合成正确 → 运行时热加载挂载 → 10 工具注册
  （ping/library_stats/search_items/get_item/find_pdf/get_fulltext/list_collections/
  get_collection_items/list_tags/get_recent_items）。
- 影响：DSH 所有 agent 会话可见 `mcp__zotero__*` 只读工具（复用平台 zotero_client），
  本会话下一轮起即可直接调用。写操作仍无（v1 只读），与 watcher 无冲突。
- 运维备忘：改 DSH 补丁后不生效时先确认 web profile 在跑、python 路径未变；
  dump-config 可随时干跑验证组合不启动进程。

## 2026-08-25 · 设计侧（续2）

**MCP服务/CLAUDE.md 补充其他 MCP 客户端接入示例（纯文档）**
- 用户问 Codex / Claude Code 能否直接用 → 能（标准 MCP stdio）。查证两方官方文档后，
  把接入写法存进 `MCP服务/CLAUDE.md`：Claude Code 用 `claude mcp add --scope user` 或
  项目根 `.mcp.json`；Codex 用 `~/.codex/config.toml` 的 `[mcp_servers.zotero]` 段。
- 结论要点：整套连接用**绝对路径**（脚本位置定位项目根、配置读项目根 .env），
  agent 在哪个文件夹跑都不影响查库；多客户端可同时连（各起独立进程，v1 只读无冲突）。
- 备忘：某客户端握手报协议版本不兼容 → 改 `mcp_stdio.py` 顶部 `PROTOCOL_VERSION` 一行。
- 验证：无代码改动；CLAUDE.md 已更新，git commit。


## 2026-08-26 · 全项目 bug 排查与修复（应用侧）

**背景**：用户反馈「前段时间用 deepseek harness 写了一段时间，效果不太好」，
要求盘一遍项目状况并查 bug。逐块审读 11.6k 行代码 + 实测验证。

**先澄清的假象**：本机（协作者机器）体检报 1 失败 4 警告，与交接文件的「13 通过 0 失败」不符。
逐个实测后确认，4 个自测失败（embed / llm_client / query_expand / zotero_client）
**全部是 `WinError 10061 连接被拒绝`**，即 Ollama 与 Zotero 没启动；
`workflow_data` 为空、密钥未配也都是本机环境差异。**这些不是 bug。**

**修的 7 处（每处都实测验证，不是「看着对」）**：

1. **MCP 服务中文全乱码**（踩坑 #43，最严重）——`mcp_stdio.serve()` 只设了 stdout 的
   UTF-8，stdin 跟随系统 GBK，中文参数被解码成乱码。
   验证：真实管道喂 `{"q":"聚硼硅氧烷"}`，修前得到乱码，修后逐码点比对 `完好=True`。

2. **单实例锁三处缺陷**（踩坑 #44）——`_pid_alive` 的 `default=''` 让「查不到」变成
   「已死」（与自身注释的契约相反）；裸子串匹配会被内存列 `45,678 K` 假匹配；
   `single_instance` 的「先删后建」有竞争窗口。
   验证：模拟 tasklist 失效 → 保守判活 True；pid=678 遇 `45,678 K` → False；自测 4/4。

3. **体检假绿灯**（踩坑 #45）——`密钥存放` 在一个密钥都没存时报「已存入系统凭据库」。
   改为按实际存入数量报告。现报：「凭据库可用，但一个密钥都没存 —— 5 个待填」。

4. **配置管道断裂 + 框架化漏网**（踩坑 #46）——`zotero_client` 读 `ZOTERO_LOCAL_API`，
   面板存 `ZOTERO_API_HOST`，键名对不上导致面板改地址永不生效，MCP 服务一并失效。
   连带修掉 10 个文件的硬编码地址，全部收口到 `get_site()`。
   **并给体检新增 `配置硬编码` 检查项**，让红线 #3 从此有人守（规则：文件若已向 config
   要该设置则放过，那里的字面量是兜底默认值）。
   验证：`ZOTERO_API_HOST=http://例子:9999` → `zotero_client.LOCAL_API` 随之改变。

5. **控制面板缺 CSRF 防护**（踩坑 #47）——加 `_guard()`：校验 Host / Origin /
   写操作强制 application/json。
   验证：起真实面板，2 条正常路径 200 放行；表单式 text/plain → 415、
   恶意 Origin → 403、伪造 Host → 403。

6. **两处小问题** —— `panel_launch.py` 文档字符串里的反斜杠 p 触发 SyntaxWarning
   （Python 未来版本会升级为硬错误），改用正斜杠；`deepread_v4.py` 用 `[0]` 取解析产物，
   缺文件时抛裸 `IndexError`，改为说清「缺什么、怎么办」的 SystemExit。
   验证：全库编译 0 警告；空目录跑 deepread 得到「解析结果不完整…删掉该目录重跑即可」。

7. **删除 watcher 中约 50 行死代码** —— `writeback` / `extract_text_summary` /
   `swap_tag` / `delete_old_summary` 已被「复用 summary 附件」方案取代且无任何调用点。
   其中 `delete_old_summary` 的「先删后传」正是踩坑 #28 反复弹同步冲突框的根因，
   留着有被误用的风险。已在 `set_state_tag` 上方留注说明去向。

**留给用户定夺的一项（未改）**：`zotero_watcher.py` 与 `deepread_batch.py` 把
DeepSeek 密钥作为**命令行第 5 参**传给 `deepread_v4.py`。Windows 上任何进程都能读到
别的进程的命令行，这与「把密钥搬进凭据库、硬盘不留明文」的设计是矛盾的。
经查 `llm_client._cfg(key=...)` 有 `key or _cfg_get('DEEPSEEK_KEY')` 的回退，
且 watcher 已经通过 `env=` 传了同一个密钥，**去掉第 5 参是安全的**。
但 `deepread_batch.py:54` 有一条注释称「靠命令行第5参传入，不读环境变量（认知2）」——
该说法现已不成立（回退路径存在）。因涉及一条有记录的设计决策，未擅自改动。

**整体判断**：项目底子好（积木划分、配置收敛、文档纪律都到位）。
反复出现的模式不是「不会写」，而是**大改动收尾差最后一公里**：
框架化迁移漏 10 个文件、自测绕开真实输入通道、体检项逻辑成立但实际测不到东西——
共同点是「看起来完成了，但没有闭环验证」。本次给体检补的硬编码检查项，
以及踩坑 #43/#45 记下的两条通用教训（自测不能绕开真实通道、警惕因为空所以合格），
就是针对这个模式的。


---

## 2026-08-26 · 架构重构 v2 · 阶段 0（安全网）+ 阶段 1（core/paths）

### 为什么做这件事

用户提出：项目边做边改、经手过几个 agent，想从软件工程角度做一次全局规划，
核心诉求是**各部分能各自升级而互不影响**。

通读全部代码（~11000 行、86 次提交、13 份文档）后的结论：
`modules/` 公理层方向是对的，问题全在它之上。而**根因只有一条** ——
没有安全网 → 改动有风险 → 每个接手者宁可新加脚本也不改旧的 → 脚本增殖、靠路径粘连。

所以第一步不是搬文件，是先造安全网。总体设计见 `docs/架构重构_v2总体设计.md`。

### 阶段 0 · 安全网

- **`pyproject.toml`**：项目成为可安装的 Python 包。`pip install -e . --no-deps` 之后，
  `import modules.x` / `from core import paths` 在任何目录、任何子进程里都能用。
- **引入 pytest**，建 `tests/`：
  - `test_core_paths.py` —— 数据契约的单元测试（纯离线、毫秒级）
  - `test_architecture.py` —— **架构守卫**：把宪法里「说了但没人执行」的规则变成会变红的测试
    · 数据路径只许在 core/paths.py 里拼装
    · 依赖只能从上往下（宪法铁律 2，此前无任何机制阻止违反）
    · domain 环不许有 I/O
    · 不许再往 sys.path 塞项目根
- **体检分两档**（`平台管理/health_check.py`）：
  - `--offline` 离线档：包安装 / 离线测试 / 语法 / 静态检查 —— **必须永远全绿**
  - 完整档：另加配置、Zotero、Ollama、公理件自测、数据、后台服务 —— 本机没配就会红，正常
  - 新增两项检查：`包安装`（没装成包会明确报出修复命令）、`离线测试`（pytest 是否全绿）
- **取得全绿基线**：离线档 9 通过 / 0 警告 / 0 失败。
  此前默认 8 通过 / 5 警告 / 1 失败，噪音（本机没配密钥、服务没开）盖住了真信号。

### 阶段 1 · 内核环 core/paths

- 新增 `core/paths.py`：**全系统唯一知道 workflow_data 长什么样的地方**。
  此前路径在 **53 处**手工拼装 —— `docs/数据契约.md` 写得再好也只是散文，
  契约随时可能被某个脚本悄悄违反而无人发现。
- 顺带把 item key 的校验（8 位字母数字）变成可执行的 `check_key()`，
  防止有人把标题、整条路径、甚至 `../..` 当 key 传进来。

### 迁移

一次性脚本改动 **66 个文件**：
- 删掉 40+ 处「沿 `__file__` 走查项目根 + `sys.path.insert`」补丁
- `workflow_data` 路径全部改走 `core.paths`
- `平台管理/panel.py` 里两处 `sys.path.insert('找新文献')` 是借用兄弟脚本，
  标了 `# paths-exempt`，阶段 4 迁入 `pipelines/` 后删除

### 验证（重点：证明行为中性）

| 项目 | 迁移前 | 迁移后 |
|---|---|---|
| 语法 | 89 文件通过 | 89 文件通过 |
| 静态未定义引用 | — | 0 |
| 公理件自测 | 12/16，失败 embed/llm_client/query_expand/zotero_client | **完全相同** |
| 完整档体检 | 8 通过 / 5 警告 / 1 失败 | 10 通过 / 5 警告 / 1 失败（多的 2 项是新增检查） |
| 警告与失败的**条目** | — | **逐条相同**（都是本机没配密钥/没开服务） |
| 离线测试 | 不存在 | 30 通过 1 跳过（跳过的是 domain 环，阶段 2 才建） |

`core/proc_lock` 自测从此包含子进程抢锁一项并通过 —— 顺带证明了
editable 安装在子进程里同样生效。

### 文档同步

- `docs/代码规范_标准脚本模板.md` 第二节重写：标准开头从 9 行降到 4 行，
  新增「二·B 数据路径一律走 core.paths」。**这一步必须做** ——
  否则下一个 agent 会照着旧规范把补丁写回来。
- `CLAUDE.md`：红线从三条变四条，补上安装步骤与三步验证顺序。
- `.gitignore`：忽略 `*.egg-info/`、`.pytest_cache/`、`平台管理/*.log`。

### 顺带发现（未修）

- `文献精读/rerun_pro.py` 引用 `workflow_data/zotero_work` 与 `workflow_data/summary`，
  这两个目录在现行布局下**都不存在** —— 该脚本疑似早已失效。迁移只做等价替换，
  没有改变它的行为，另记入待办。
- `文献精读/zotero_watcher.py` 里重复实现了 `zget` / `find_pdf` / `SUPP_PAT`，
  而 `adapters/zotero_client` 已有同名实现（违反宪法铁律 1）。属阶段 2 的活。
- `docs/架构总览.md` 描述的还是 `scripts/`、n8n、`workflow_data/summary/` 这套早已不存在的布局。
  属阶段 5：手写的地图必然过时，应改为自动生成。


## 2026-08-26 · 架构重构 v2 · 阶段 1 完成（core/log + core/errors）

### core/log.py —— 统一日志

改造前同一件事有三种写法：`zotero_watcher` **把内置 print 整个换掉**、
`watchdog` 和 `auto_sync` 各自写一个 `def log(msg)`。
劫持 print 尤其糟：读代码的人看到 `print('...')` 会以为只是打屏，实际在写文件；
而且这个 hack 无法 import 复用，只能复制粘贴。

三者还共有一个真问题：**没有任何一个会轮转**。`zotero_watcher` 是常驻服务，
日志只会一直长下去。

- 底层用标准库 `logging` + `RotatingFileHandler`（5MB × 3 份）——
  轮转这种稳定又琐碎的活用现成的，符合宪法【首要判据】
- 对外只暴露一个**像 print 一样的可调用对象**，所以 watcher 里几十处
  `print(...)` 调用**一行都不用改**，只把名字绑过去：
  `print = get_logger('zotero_watcher')`
- 三处调用点全部改用它，删掉各自的私有实现和已死的日志常量

### core/errors.py —— 异常分类

改造前一个步骤失败了，调用方没办法知道该怎么办：watcher 拉子进程只拿得到
退出码和一坨 stdout，积木抛的多是裸 Exception。
于是「MineRU 这一秒过载了」和「你传的 key 根本不合法」走同一条路。

分类维度刻意**不是「哪里出的错」，而是「该拿它怎么办」**：

| 类 | 该怎么办 |
|---|---|
| `BadInputError` | 调用方传错了，重试没用 |
| `ConfigError` | 用户没配好，让他去控制面板 |
| `DataError` | 我们自己的数据坏了，要人来看 |
| `ExternalServiceError`（含 `ServiceUnavailable`/`RateLimited`） | **可以重试** |
| `AuthError` | 密钥问题，重试一万次也不会好 |

调用方只需要 `errors.is_retryable(e)` 一个函数。
保守策略：不认识的异常一律不重试 —— 宁可让失败暴露，也不要对着一个
永远不会成功的调用反复烧钱。

已采用：`core.paths.BadKeyError` 归入 `BadInputError`（同时仍是 ValueError，
旧代码 `except ValueError` 照样接得住）。各 adapters 在阶段 2 逐个改造。

### 验证

- 离线测试 54 通过 1 跳过（新增 `tests/test_core_log_errors.py` 24 条）
- 离线档体检 9 通过 / 0 警告 / 0 失败
- 真实写日志验证：中文不乱码、时间戳格式正确、轮转配置生效（5MB × 3）
- 三个改造过的脚本（zotero_watcher / watchdog / auto_sync）逐个加载通过

### 踩坑

见 `docs/踩坑记录.md` 踩坑 #48：logging 的 Logger 是进程级全局单例，
判断「要不要挂 handler」不能只看「有没有」，要看「指向的是不是同一个文件」。
**这个坑是被新写的单元测试当场逼出来的** —— 在生产里它只会表现为
「日志偶尔写两遍」或「换了目录不生效」，几乎不可能靠肉眼发现。


## 2026-08-26 · 架构重构 v2 · 阶段 2 完成（拆开公理层）

### 17 块积木按「什么会让它需要改」分进四环

此前 `modules/` 里混着三种性质完全不同的东西，所以没法分别对待 ——
这正是「想换个模型却不知道要改几处」的直接原因。

| 环 | 判据 | 迁入 |
|---|---|---|
| `core/` | 几乎不会变的基础设施 | cli、config、proc_lock、subproc |
| `domain/` | **只有我们自己想法变了**才改 | figure_crop、si_filter |
| `adapters/` | **外部世界变了**就得改 | zotero_client、llm_client、embed、pdf_parse、sciverse、snowball、evalset |
| `pipelines/` | 需求一变就变（组合而成） | chart_digitize、query_expand、paper_discovery、lib_match |

后四块此前被当成「公理」，但都不满足公理的定义 ——
宪法铁律 1 写着「还能拆成『先做 A 再做 B』的就不是公理」，它们都能。

### 新增两块 adapter，消灭两类重复

**`adapters/vectordb`（Chroma）**：此前 `import chromadb` 出现在 **5 处**，
每处自己建 client、自己写集合名、自己解 Chroma 那套「每字段套一层 list」的返回：

```python
docs, metas = res['documents'][0], res['metadatas'][0]   # 这个 [0] 就是绑死的证据
```

换任何别的向量库，这 5 处都要改，而且改错了不报错、只静默返回空结果。
现在统一返回拆平的 Hit（`sim` 统一为「越大越像」），换库只改一个文件。

**`adapters/openalex`**：同一个 API 此前被**三处各实现一遍**
（snowball 有退避重试，paper_discovery 和 find_papers 是裸 urlopen）。
合并后行为一致、都有重试。

### 修掉一个只在特定路径下发作的真 bug

三份实现导致字段名各叫各的：`paper_discovery` 发 `cited_by`、
`discover.py` 读 `cited` —— **两边都不是 `citations`，也互相对不上**。
后果：走 OpenAlex 检索时引用数**永远是 0**，还连累了 `lib_match.rank`
按被引排序的打分（它读的是 `citations`）。全平台统一到 `citations`。

### 删掉 watcher 里重复实现的 Zotero 代码（宪法铁律 1 被违反的那处）

`zotero_watcher.py` 里另有一份 `zget` / `find_pdf` / `SUPP_PAT`，
与 `adapters/zotero_client` 的同名实现并存；`has_si` 里还藏着**第三份** SUPP_PAT
（而且写在循环体内，每个附件重新编译一次正则）。
全部删除改调适配器，`has_si` 收进 adapter。watcher 从 377 行减到 293 行。

### 架构守卫从 4 条加到 7 条

新增：
- **只有 adapters 环可以联网** ← 这条是「换掉 MineRU 只改一个文件」的全部保证
- 只有 adapters 可以用外部服务客户端（chromadb / keyring）
- domain 不许 import `core.paths`（**纯逻辑永远不知道文件放在哪**）
- 项目内的 import 必须指向真实存在的包（连函数体内的一起扫）
- 新增 `tests/test_no_undefined_names.py`：不许用没定义过的名字

### 验证

| 项 | 结果 |
|---|---|
| 离线测试 | 79 通过（新增 vectordb 18 条 + 未定义名 3 条 + 守卫 3 条）|
| 离线档体检 | 9 通过 / 0 警告 / 0 失败 |
| 完整档体检 | 与基线逐条相同（失败项仍只是本机没配密钥/没开服务）|
| 积木自测 | 19 块，通过/失败与基线完全一致；新增两块 8/8、7/7 全过 |
| 真实 API | `openalex` 自测真调了一次（546 篇命中）；snowball 6/6 也是真连 |
| 向量库 | 用临时目录建真 Chroma 库验证，不碰用户数据 |
| 控制面板 | 7 个数据采集函数逐个直调正常；积木列表 19 块正确 |

### 两个 bug 是被新守卫当场抓出来的

见踩坑 #49：`_LOG_DIR`（我在阶段 1 删漏的，会让 watcher 反复重启）
和 `lib_match` 里的 `sim`。两个都是「模块能 import 但函数一跑就炸」，
语法检查和导入检查都发现不了。


## 2026-08-26 · 两台机器的分工落地（机器角色守卫 + B 机操作面）

### 背景

用户定下一条约束：**Claude Code 只在编程端（A 机）跑**，主力机（B 机）只跑服务。
先把分工理清楚再继续重构。

查证后发现两件必须立刻处理的事：

1. **本地领先 origin 8 个提交** —— 阶段 0~2 的重构成果还没推上去，B 机拿不到。
2. **两台机器共用同一个 Zotero 账号**（A 机 storage 里有 397 个附件目录、
   228 个 PDF、38 份回写的 summary.html）。A 机做验证时一旦回写，
   污染的是真实文献库，而且立刻同步到 B。
   而现有防护只有 2 处 dry-run，会写 Zotero 的脚本却有 9 个。

**A 机现在写不了 Zotero 是巧合，不是设计**：它的 `ZOTERO_USER_ID` 是 `0`
（Zotero 本地 API 的约定值），拿这个 id 去写 `api.zotero.org` 会失败。
一旦有人为了让本地 API 表现一致而把它改成真实数字 id，
A 机立刻就获得改写真实文献库的能力，而且没有任何东西会拦。

### 做了什么

**`core/role.py` —— 机器角色**
- `.env` 里加 `ROLE`（`dev` 编程端 / `prod` 运行端），控制面板「本机设置」第一项可改
- `role.require_prod(动作, force=...)`：编程端调用直接抛 `WrongMachineError`
- **默认 `dev`（fail safe）**。两种默认的代价不对称：默认 prod 会让编程端在配置好
  之前毫无保护、静默污染真实数据；默认 dev 只会让主力机响亮地拒绝一次、立刻可修
- 报错是给不懂编程的人看的：说清是什么操作、去哪改、怎么越过、去哪看详情

**18 个守卫点**：9 个 Zotero 写入口 + 批量/全库/付费作业 + watcher + 看门狗。
全部加在**函数体里**而不是模块顶层 —— 写顶层会让 import 就抛错，
体检的「运行时导入」、pytest 收集、面板借用逻辑会一起挂。

**三条新架构守卫**（保证以后不会漏）
- 扫描所有出现 `api.zotero.org` 的文件，没调 `role.require_prod` 就变红
- 常驻服务必须有守卫
- 守卫不许写在模块顶层

**体检认识角色了**
- 新增「机器角色」检查项，排在最前（它决定后面几项该不该报警）
- 编程端不再报「缺 4 个自启任务」——那本来就不该有，是噪音不是问题
- 编程端的「数据资产」改为提示「没有金样本，纯逻辑改动无法做回归验证」

**B 机的操作面补齐到四个按钮**（B 上不需要敲任何命令）
- `更新平台.bat`：git pull → 重装包 → 离线体检（必须全绿）→ 完整体检
- `诊断报告.bat` → `平台管理/诊断报告.py`：一次收齐机器角色、代码版本、
  完整体检、数据统计、自启任务实况、进程、三份日志尾部、心跳距今多久，
  **自动复制到剪贴板**直接粘给 Claude。因为 Claude 看不见 B 机，
  这是唯一的窗口，所以设计成「一次给全，避免来回追问」

**常驻服务的友好失败**：watcher / 看门狗在 `__main__` 里接住 `WrongMachineError`，
打印人话而不是甩 traceback —— 主力机首次部署时这个失败**必然发生一次**
（ROLE 默认 dev）。

### 验证

- 离线测试 **100 通过**（新增 role 单元测试 16 条 + 架构守卫 3 条）
- 离线档体检 10 通过 / 0 警告 / 0 失败
- 完整档体检噪音从 5 条降到 4 条，且剩下的都是有意义的
  （缺密钥、Ollama 没跑、没有金样本、3 个需服务的自测）
- 实测在编程端启动 watcher：打印人话 + 退出码 2，不是 traceback
- `诊断报告.bat` 实跑通过（中文正常、剪贴板复制成功）
- 两个新 bat 的 BOM 与换行符对齐现有 bat（BOM 会让 cmd 把它当命令报错）

### 顺带记录的事实（查证得出，不是记忆）

- A 机 Zotero 已同步：397 附件目录 / 228 PDF / 2.0 GB / 38 份 summary.html
- **不走 Zotero 同步的**：`parsed/full.md`、`vector_db/`、`structured/*.json`
  （被 .gitignore 挡着，是可再生的派生层，见数据契约）
- **走 git 的**：`evalset.json`（用户人工评价，不可重建，理应保护）、
  `_last_search.json`、`backup/zotero_tags_backup.json`
- A 机硬件 MX450 / 2 GB —— 跑不动 bge-m3，所以 Ollama 必须留在 B


## 2026-08-26 · 修部署缺口：更新代码后重启常驻服务（踩坑 #50）

主力机反馈：pull 完、更新完、体检全绿，但控制面板里**看不到新加的「机器角色」**。
根因是面板常驻进程占着 8777 端口跑旧代码，新进程绑不上端口静默死掉。

- `平台管理/更新平台.py`（新）：6 步上线流程 —— git pull → 重装包 →
  **停掉旧面板** → **重启 watcher 计划任务**（仅运行端）→ 离线体检（必须全绿）→ 完整体检。
  逻辑放 Python 而非 .bat：Python 启动即把文件读进内存，可安全自更新。
- `更新平台.bat` 瘦成包装（并在 .py 缺失时提示先手动 pull 一次）。
- 控制面板顶部新增**代码版本行**：git 短哈希 + 提交标题 + 工作区是否干净 +
  面板进程启动时刻 + 本机角色。让「我看的是不是旧界面」一眼可辨。

验证：真起一个面板（HTTP 200）→ 调 `stop_panel()` → 再访问连不上，确认停掉。
`更新平台.py` 完整跑通，退出码 0，离线档 10 通过 / 0 警告 / 0 失败。


## 2026-08-26 · 修两机冲突根因：生成物移出版本库（踩坑 #51）

主力机 `git pull` 卡在 unmerged 状态。根因不是 git 用法，是设计：
版本库里放了**两台机器都会改写**的文件。

- `HANDOVER.md`、`workflow_data/_last_search.json` 移出版本库 + 进 gitignore
- `交接.py` 只在编程端重写 `CLAUDE.md` 的结构区块（运行端点按钮不再制造冲突）
- 新增两条守卫：禁止生成物被跟踪；**同时钉死 `evalset.json` 必须留在版本库**
  （用户人工评价，不可重建，版本库是唯一备份 —— 防止下次「清理」时误删）

判据记进宪法级的一句话：**进版本库的文件必须只有一个生产者。**

同日追加根治：新建 `.gitattributes`，给 `踩坑记录.md` / `变更记录.md` /
`待办与需求.md` / `.gitignore` 设 `merge=union` —— 这几个都是纯追加的流水账，
两边的行直接都保留即可，不该判成冲突。**代码绝不可用 union**（会缝出语法错误）。


## 2026-08-26 · 引导脚本加固（踩坑 #53、#52）

主力机双击「更新平台.bat」报 `ModuleNotFoundError: No module named 'core'` ——
它从没装过包，而这个脚本顶层就 import 了 core，可它要干的事情之一正是装包。

- `更新平台.py` / `诊断报告.py` 改成**引导脚本**：顶层只用标准库，
  `ROOT` 从 `__file__` 算，项目包延迟 import 并写好降级路径
- `诊断报告.py` 在包没装好时照样出报告，第一行直接写清病因与修法
- 新增守卫 `test_引导脚本不许在模块顶层import项目包`
- 顺带修 `诊断报告.py` 降级路径里两处仍在用 `paths.ROOT` / `paths.log` 的 bug
- 记录踩坑 #52：PowerShell 的 `$PWD` 不等于 .NET 的当前目录 ——
  给用户的命令必须先在同样的环境里真跑一遍

验证：造一个会抛 ImportError 的假 `core` 包（PYTHONPATH）顶掉真的，
真跑两个脚本确认降级路径正确，而非「看代码觉得没问题」。


## 2026-08-26 · 上线流程实战修三处（踩坑 #54~#56）

主力机第一次跑通「更新平台」，一次暴露三个真问题：

1. **PowerShell 输出乱码**（#54）：中文 Windows 上 PowerShell 默认输出 gb2312，
   我们按 UTF-8 解码。修在 `core/subproc.powershell()` —— **面板和体检也一直有这个病**，
   一并治好；两个引导脚本不能依赖 core，各内联一份。
2. **装完包当前进程仍 import 不到**（#55）：`.pth` 只在解释器启动时读一次。
   导致第 4 步「重启常驻服务」被跳过 —— 而那正是这个脚本存在的理由。
   加 `refresh_imports()`：invalidate_caches + site.main + 兜底 sys.path。
3. **`stop_panel` 把自己杀了**（#56）：`-like '*panel*'` 匹配太宽。
   收窄到 `panel.py`/`panel_launch` 并排除本进程 PID。

另外：「更新平台」现在会**自动装上 pytest** —— 第 5 步的离线体检是上线闸门，
闸门跑不了等于没有闸门（主力机就是没装 pytest，那一档直接跳过了）。

验证：真起一个面板 → HTTP 200 → 调 stop_panel → 中文正常、面板确实停掉、
测试进程自己没被误杀。


## 2026-08-26 · 控制面板改为「等它真起来再开浏览器」（踩坑 #57）

- 新增 `平台管理/打开面板.py`：轮询端口（最多 30 秒）而不是睡固定 2 秒；
  面板已在跑就提醒「可能是旧代码」；起不来则直接把 `panel_launch.log`
  尾巴和常见原因打在屏幕上。`控制面板.bat` 瘦成一行调用。
- 补回上一轮**静默失败**的改动：`更新平台.py` 第 2 步现在真的会
  自动装 pytest + 调 `refresh_imports()`（上次 `replace` 没加断言，
  锚点没匹配上，改动等于没做）。这次加了断言。
- `打开面板.py` 纳入引导脚本守卫（面板起不来时要靠它定位，它自己不能也起不来）。

验证：三条路径实跑 —— 冷启动 0.5 秒内起来并开浏览器、已在跑时给出旧代码提醒、
失败时日志尾巴正确摆出。全程用桩拦住 `webbrowser.open`，没真弹窗。


## 2026-08-27 · 修控制面板「本机设置存不进去」（踩坑 #58）

主力机把机器角色改成 prod、保存、刷新后又变回 dev，且无任何报错。

- **真 bug**：`action_save_config` 的白名单只有密钥和模型，
  **`SITE_SETTINGS` 整类被静默丢弃**。`ZOTERO_STORAGE`、`OLLAMA_MODELS`
  这些一直都存不进去，只是以前都直接改 `.env`，没人从面板存过。已补上。
- 面板那张卡分成三段小标题（密钥 / 本机设置 / 模型）——
  用户反馈「顶上没有本机设置」，其实是混在同一片输入框里找不着。
- **机器角色改成下拉框**：枚举值手打会拼错，而 `role.current()` 对非法值
  一律按 dev 处理，拼错就是**静默降级**。下拉框从源头消灭这个坑。
- `更新平台.py` 的 `git pull` 加 `--no-edit`：分叉过的分支拉取会产生合并提交，
  git 默认开编辑器让人写说明（主力机上弹出记事本，不关就一直卡着）。
  自动流程不该等人。

验证：真调 `action_save_config` 改一个无害设置项（`OLLAMA_HOST`），
确认 `.env` 真写进去再还原；ROLE 只做白名单静态检查，
**刻意不拿它做写入测试** —— 编程端哪怕一瞬间变成 prod，
也等于短暂解除了「不许写 Zotero」的保护。


## 2026-08-27 · 修面板配置区 `NaN` 回归（踩坑 #59）

加小标题时把新行插在了第一项前面，`+ ` 变成一元加号 → `innerHTML = NaN`，
整张配置卡片报废。改为「第一项不带 +，后续项才带」。

**验证方式改了**：这次用浏览器真渲染并读 DOM 断言 ——
确认无 NaN、三个小标题都在、`#k_ROLE` 是 `SELECT` 且选项为 dev/prod、
选成 prod 后表单确实会提交 `ROLE=prod`（提交前立刻还原，没真写进这台编程端）。

项目文档早就写着「改面板后务必真的打开页面点一遍」，这次是我没遵守。


## 2026-08-27 · 治「记事本反复弹出」的根因：本地提交没推送

主力机每次 `git pull` 都弹记事本。根因不是 git 配置，是**分支一直分叉**：
那次解冲突生成的合并提交从来没推上去，origin 上没有、本地有 ——
于是每次拉取都产生一个新的合并提交，git 每次都要人给它写说明。

- `更新平台.py` 末尾新增 `check_unpushed()`：检测「已提交未推送」和「未提交改动」，
  说清后果（这就是记事本的来源）并给出处理命令。
  **建议按实际情况给** —— 只有数据文件脏时才建议提交 evalset.json；
  改到代码时反而提示「运行端不该改代码，先发给 Claude」。
- `.gitignore` 加 `*.backup`（手工备份留本地，不进版本库）。

推一次之后两边合流，往后拉取都是快进，不再弹编辑器。


## 2026-08-27 · 诊断报告的剪贴板与 .bat 编码修复（踩坑 #60）

主力机部署已收口（角色 prod、watcher 已重启、4 个自启任务在、
离线档 10 通过 0 失败、完整档 16 通过 0 失败、工作区干净且不再分叉）。
最后两个小问题：

- **剪贴板全是乱码**：`clip.exe` 按当前控制台代码页读 stdin，喂 UTF-8 就乱。
  改走 `Get-Content -Encoding UTF8 -Raw | Set-Clipboard`，全程 Unicode。
  实测 138 行中文一字不差。
- **`诊断报告.bat` 报「不是内部或外部命令」**：里面几行 `echo ────────`
  用了 U+2500，cmd 解析批处理时对多字节字符失步。
  **.bat 里只留 ASCII**，所有中文提示交给 Python 打印。


## 2026-08-27 · 修「看门狗误杀正在干活的 watcher」（踩坑 #61）

从主力机诊断报告的看门狗日志查出来的真问题：一个月误杀约 20 次，
每次都白花一份 MineRU + DeepSeek，还在库里留下半成品。

- **新增 `core/heartbeat.py`**：把「还活着」和「有进展」拆成两个信号。
  报活由后台守护线程按固定节奏写，精读跑多久都不会被误判；
  进展在每完成一件实事时写，用来兜住「活着但卡住」。
  判断逻辑做成纯函数 `verdict(alive_age, progress_age)`，可直接测。
- `zotero_watcher` 改为 `heartbeat.start('watcher')` + 每轮/每篇 `progress()`。
- `watchdog` 改看两个信号：报活 300 秒、无进展 2700 秒（45 分钟，
  必须大于最慢一篇精读的耗时，否则等于把 bug 装回去 —— 有测试钉住这条）。
- 报活文件名保持 `watcher_heartbeat.txt` 不变，滚动升级时新旧版本不会对不上。
- **新增 `平台管理/查产物缺口.py`**（只读）：列出半成品文献、看它停在哪一步、
  给出重做建议（并说明解析结果还在、不会重复花 MineRU 的钱）。

验证：
- 单元测试 19 条（`core.heartbeat`）+ 接线测试 8 条（看门狗阈值与信号名一致性）
  + 缺口诊断 7 条，全绿
- **真进程模拟**：子进程「精读」15 秒不报进展、报活阈值压到 4 秒 ——
  全程判定「不动」（旧逻辑第一次检查就会杀它）；杀掉后 6 秒内正确判定需重启


## 2026-08-27 · 产物缺口诊断并进诊断报告（入口该有几个）

用户双击 `查产物缺口.py`，窗口一闪而过 —— 双击 .py 跑完即退出，没有 pause。
**这是设计问题不是用户问题**：项目的原则是「B 机上不需要敲任何命令」，
我却给了一个要在终端里跑的脚本。

- 产物清单收进 `core.paths.PAPER_ARTIFACTS` + `missing_artifacts()` ——
  它是数据契约的一部分，只该有一份
- 诊断结果**并进 `诊断报告.py`** 的「数据资产」段：直接列出每篇停在哪一步、
  有什么、缺什么、重做要不要再花 MineRU 的钱。不再需要单独的入口
- `查产物缺口.py` 保留（可单篇查），但不再是用户的必经路径
- 把「不要给 B 机用户要敲命令的工具」写进 `docs/两台机器的分工.md`
  和 `平台管理/CLAUDE.md`，并给出三个正确去处的优先级

验证：造了三篇假文献（完整 / 精读没做完 / 解析就没成），
确认报告里三种情况分别给出正确的停留阶段与建议。


## 2026-08-27 · 三处修正（踩坑 #62）

主力机第二份诊断报告推翻了两个判断：

1. **那 4 篇不是半成品**。`fulltext`/`layout`/`summary` 全有，只缺 `meta.json` ——
   精读做完了。而我给的建议是「重新打待处理标签」，**会让 4 篇整篇重跑 MineRU +
   DeepSeek，白花钱**。项目里本来就有 `库房维护/backfill_meta.py` 专补这个
   （读 Zotero 本地 API，不调大模型）。`diagnose()` 已加分支，
   并把「要花钱 / 不花钱」写进建议里。
2. **新代码没在 watcher 上生效**。计划任务拉起的是看门狗，watcher 是看门狗
   spawn 的孙子进程，停任务换不掉它（两份报告里 watcher PID 一模一样）。
   `更新平台.py` 新增 `stop_watcher()`：杀进程 + **清掉心跳/进度信号**
   （不清的话看门狗会以为它还活着，要等满 300 秒）。
3. **让「版本是否生效」看得见**：诊断报告改为分别显示报活与进展两个信号。
   旧版只写心跳不写进展 —— 进展缺失即「还在跑旧代码」，一眼可辨。
   这次正是因为看不见才漏掉的。

同时更正了踩坑 #61 里关于那 4 篇的推断，并注明「推断要标明是推断，
看到证据要立刻改口」。


## 2026-08-27 · 主力机部署全线收口 + query_expand 静默降级

**部署这条线关闭。** 主力机诊断报告逐条印证：
watcher PID 12292 → 27424（真换了）；看门狗日志出现
`报活信号缺失，watcher 可能没启动 → 重启`（正是新流程）；
两个信号都在 → `✓ watcher 跑的是新代码`；产物不全的文献 **0 篇**。

顺手处理最后一条黄灯 `query_expand`：

- **真问题是静默降级**：LLM 调不通时 `except Exception` 直接吞掉原因，
  只返回原始检索式。功能看起来「还能用、只是效果差点」，
  实际上一次都没调通，而且**没人查得出为什么**（踩坑 #33 同一个病）。
  改为降级仍降级，但把原因写进日志。
- **自测混淆了「环境没配」和「功能坏了」**：没有 `DEEPSEEK_KEY` 时报 FAIL。
  这会让体检长期挂一条查不出所以然的红 —— 而一个「有时候红是正常的」检查
  等于没有检查。改为缺密钥报 SKIP 并相应减少总数，
  只有「配了密钥却调不通」才算 FAIL。

编程端现在离线档 10 通过 / 0 警告 / 0 失败，19 块积木自测全绿。


## 2026-08-27 · 架构重构阶段 3（上半）：精读线搬进 pipelines + 建 core/jobs 状态库

**做了什么**

1. **`core/jobs.py`（新）**——SQLite 任务状态库，只用标准库。
   一行记录 = 某篇文献的某个步骤的一次执行（谁产的、哪个模型、哪版提示词、
   花了多少、失败原因）。设计文档第三节 A 的东西终于落地。
   两条约定写进了 `core/CLAUDE.md`：
   · **状态库是索引不是真相**（真相是硬盘上的产物；库删了能重建）；
   · **它坏了不许拖垮主流程**（所有读写兜底，记不上账可以，精读白做不行）。
   老数据没有记录但产物在 → 一律认它，避免上线即全库重跑。
2. **`pipelines/deepread/`（新）**——精读编排。`run(key, ...)` 一次调用完成
   解析 → 正文精读 → SI 精读 → 合并，返回 `Result`（事实：做成了什么）。
   watcher 里那**五个「脚本路径 + 参数顺序」的 subprocess 调用没了**。
   逻辑来自 `deepread_v4.py` / `si_deepread.py` / `merge_summary.py`，行为不变；
   只做了一处去重：正文精读不再自带一份裁图实现，改调 `domain.figure_crop`
   （两边算法与阈值本来逐字相同 —— 违反宪法铁律 1 已久）。
3. **三个老脚本变成命令行薄壳**（各 40 行左右），argv 契约一字未改 ——
   `重跑精读_PRO版.bat`、`si_batch.py`、体检的运行时导入检查全部照常。
4. **`deepread_batch.py` 改为直接调函数**，不再拉子进程：失败拿得到原因而不只是退出码，
   每次执行也进状态库。
5. 提示词文件 `_sys_prompt_v2.txt` 随逻辑搬进 `pipelines/deepread/`，
   并配 `PROMPT_VER`。**改了提示词范式就 +1**，
   于是「哪些精读该重跑」= `jobs.stale('main_summary', prompt_ver=N)`，
   不再靠人记得自己改过什么。

**为什么这么切**

原设计（第六节阶段 3 第 15 项）写的是「新旧并行、逐字节比对产物」。
实际做下来**这一步自动消解了**：老脚本现在是新函数的薄壳，
两条路调用的是同一段代码，比对没有意义。真正需要在主力机验证的是
「watcher 端到端还跑不跑得通」，而不是「两条路产物一不一样」。

**验证**

- 离线：`pytest` 110 条全绿（新增 `test_core_jobs.py` 12 条、
  `test_pipelines_deepread.py` 13 条）；`health_check --offline` 10 通过 0 警告 0 失败；
  `pipelines/deepread/selftest.py` 5/5。
- 编排测试用假的 MineRU/LLM 跑状态机：做过的不重跑、产物被删会补做、
  SI 失败不拖累正文、正文失败不写盘、提示词升版自动进重跑清单 —— 每条一个断言。
  **这类验证以前根本做不了**（要真花钱跑一篇），是这次搬家最直接的收益。
- 编程端跑不了真实精读（无金样本、不许写 Zotero）。**主力机需要实测一篇**：
  打「待处理」标签，看日志有没有走到 `[正文精读完成]`，
  以及 `workflow_data/state.db` 里有没有记录。

**一处顺手修的真 bug**：`deepread.run(pdf_path=None)` 原本会被当成「你没告诉我」
而去 Zotero 再查一次 —— 「没传」和「传了个空」必须分开，改用哨兵值。
测试里当场撞上，说明这类编排 bug 以前只会在运行时才暴露。

**还没搬的**（阶段 3 下半）：结构化抽取仍由 watcher 用子进程拉；
向量化/问答未动；回写 Zotero 该收进 `adapters/zotero_client`，
watcher 才能瘦成「看标签 → 入队 → 完事」。


## 2026-08-27 · 机器角色加第三档 `test`：编程端接独立 Zotero 账号

**起因**：用户在编程端登录了另一个 Zotero 账号（文件同步走坚果云 WebDAV），
专门用来测试。这解开的是此前 A 机**完全无法验证的那一半 —— 所有写操作**：
标签状态机、附件回写与复用（踩坑 #28 那类只在真实同步里才发作的问题）、
版本冲突、watcher 端到端。而且大多不花钱：喂一份假 summary.html 就能走完回写链路。

**改了什么**

1. `core/role.py` 加 `TEST` 档。放行条件**不是「角色写着 test」，
   而是「写回目标确实是测试库」**（`test_library_ok()`：配置里的写回 id
   必须等于测试账号 id，两个都得填）。角色是人填的一个字，指着哪个库才是客观事实，
   **闸门要挡在事实那一侧**。
2. **拆开 `ZOTERO_USER_ID`**（待办里挂了很久）：本地 API 认 `0`，
   Web API 必须真实数字 id，此前一个配置项在干两件事，于是编程端
   「读得动、写必失败」，而失败看起来像鉴权问题。
   现在多了 `ZOTERO_WEB_USER_ID`（写回用）与 `ZOTERO_TEST_USER_ID`（白名单）；
   没配则沿用旧值 —— **B 机一个字都不用改**。
3. 写回路径改用 `WEB_USER_ID`：`zotero_watcher.set_state_tag`、
   `zotero_upload_attachment`、`upload_summaries` 三处。
4. 文档：`docs/两台机器的分工.md` 加「第三档」一节，并在「两条铁律」上加例外说明 ——
   那两条的根因都是*共用同一账号*，账号不同就都不成立。
   不写清楚，下次接手的 LLM 会照着字面拒绝在 A 机跑 watcher。

**保护是两层，一层硬一层软**：硬的是编程端只持有测试账号的 API key，
带着它物理上写不到真实库；软的是上面那条守卫，挡「配置切回真实账号却忘了改角色」。

**验证**：新增 8 条测试（test 档放行/拒绝的四种组合、两个 id 的回落规则）；
pytest 全绿；离线体检 10/0/0。读的那半条链路已在测试库上实测通过 ——
`find_pdf` 能从测试账号的一篇 Nature hydrogel 论文里定位到本地 PDF。

**记一笔环境事故（有惊无险）**：换账号时用户是在**同一个 profile、同一个数据目录**里换的，
本地 2 GB 的真实库副本因此被清空。没造成损失，因为顺序对了 ——
**先退出账号、再清空本地**，登出状态下清本地不碰服务器。
顺序反了（登着旧账号做「重置」并选了「推上服务器」）就会清空真实库并同步到 B。
这条已写进分工文档。


## 2026-08-27 · 面板选不到 test 档（一处「同一份知识抄两遍」）

角色加了第三档，但控制面板的角色下拉是**在前端 JS 里写死的两项**，
于是用户在界面上根本选不到 `test`；更糟的是渲染逻辑写的是
`s.value!=='prod' ? dev选中`，也就是**保存一次会把 test 悄悄退回 dev**——
和踩坑 #58「界面上改得动、却存不进去」同一族。

**改法不是把第三项也抄进前端**，而是让选项从后端下发（`SITE_OPTIONS`，
取值直接引用 `core.role.DEV/PROD/TEST`），前端改成通用渲染：
有 options 就画下拉，没有就画输入框。以后再加档只改一处。

配套加了 `tests/test_panel_config.py`：断言面板的角色选项集合
**等于** `core.role.VALID`，以及每个本机设置项都在保存白名单里、都能在面板显示。
这类 bug 界面正常、点得动、不报错，只有结果不对，人工自查极难 ——
所以断言的不是「功能对不对」，是「两处一不一致」。

## 2026-08-27 · 测试端做成一等公民（不再被当成「残废的编程端」）

用户原话：「测试端本来就该有这些测试服务」。原先若干处按 `dev/prod` 二分，
`test` 会莫名其妙落进某一侧：

- 体检「数据资产」：会劝测试端「从主力机拷金样本」——错。测试端有自己的账号，
  数据是它自己跑出来的。改成单独一条：没有产物就提示「打个待处理跑一篇」。
- 体检「自启任务」：改成只有运行端才检查。测试端可以**手动**起 watcher 做端到端验证，
  但不注册自启任务 —— 那会跟着开机长跑白烧额度。
- 体检「机器角色」：test 档单独一条，并且**直接体检客观事实** ——
  写回目标不是测试库就报 FAIL 并说清怎么填。
  对测试端来说「指着哪个库」比「角色写着什么」重要得多。
- `交接.py`：判据从「是不是 dev」改成「是不是运行端」。
  测试端和编程端是同一台机器，照样该更新 CLAUDE.md 的结构区块。
- `更新平台.py`：不再对测试端喊「如果这台是主力机，去把角色改成 prod」。


## 2026-08-27 · 测试端第一次真跑回写链路，当场抓到踩坑 #64

**这就是测试端的价值证明**：搭好当天第一次真跑，就抓到一个上线以来一直存在、
但在主力机上不容易撞见的洞（详见踩坑 #64）——
`find_existing_summary` 查本地 API，而附件是刚传到云端的，本地还没同步，
于是「复用已有附件」失效、会重复建附件。

**改动**：
- `adapters/zotero_client` 新增 `zweb()`（云端只读 GET）+ `find_child_attachment()`，
  云端优先、失败退回本地。watcher 改用它。
- 架构守卫补一对：`_READONLY_WEB` 白名单（只读的云端封装免机器角色守卫）
  + 新守卫 `test_只读的云端封装必须保持只读`（该文件里不许出现写方法）。
  **开口子必须配一把锁**，否则以后谁往里加个 POST 就一路绿灯。

**已验证通过的（测试库，零成本）**：
1. 上传 summary 附件 → 云端可见
2. 第二次处理认出已有条目、**不重复建**（修复后）
3. 状态标签互斥：无 → 正文精读 → 全文精读，用户自己的标签（Gels and hydrogels）不受影响
4. 本地 storage 同步写入，点开即最新
5. 机器角色守卫在 test 档下按预期放行（`role.require_prod` 真实走了一遍）

**还差**：`MINERU_TOKEN` 没配，所以「真跑一篇精读」还做不了 —— 解析这一步会失败。


## 2026-08-27 · 测试端端到端真跑一篇：全线通过（193 秒）

用测试库那篇 Sun 2012 *Highly stretchable and tough hydrogels*（正文 + SI），
走 watcher 的 `process_item`，与用户打「待处理」标签触发的路径完全一致。

| 步骤 | 结果 |
|---|---|
| MineRU 解析正文 | 11s → `full.md` 26 KB / `layout.json` 394 KB |
| 正文精读 | 42s，7467 字，裁图 3 张（deepseek-v4-flash，提示词 v2）|
| SI 解析 + 实验细节精读 | 67s，5223 字，补充图 16 张 |
| 合并 | `summary_full.html` 10.9 MB |
| 结构化抽取 | 并入 `structured/compare.md`，字段抽得对（离子/共价双网络、~90% 水、>20 倍拉伸）|
| 回写 Zotero | **复用附件条目**（踩坑 #64 修复生效）、标签置「全文精读」、本地 storage 同步 |

**状态库第一次在真实数据上记账**：parse / main_summary / si_summary / merge
四条 `ok`，各自带 producer、model、prompt_ver、耗时。

**幂等验证（零成本）**：
- 原样再跑 → 正文与 SI 都跳过，一分钱不花
- 删掉 `summary.html` 再跑 → **只补正文**（41s，一次 LLM 调用）：
  解析复用、SI 一个字节没动。这正是 `core/jobs` 想要的「只补缺的部分」。

**至此路线图第一、二段完成**（`docs/测试端与阶段3路线.md`）。
编程端从此有了**可重复的端到端验证手段** —— 以前每次改精读线只能推到主力机赌一把。

### 顺带发现的两个小问题（未修，记入待办）

1. **合并每次都重做**：即使正文和 SI 都没变，`merge` 仍会重写一份 10.9 MB 的文件。
   本地操作不花钱，但没必要；该像别的步骤一样按「输入变没变」跳过。
2. **DOI 从解析文本里正则抓，抓不到就空着**：这篇的 DOI 在 Zotero 条目里明明有
   （10.1038/nature11409），但喂给模型的 `DOI:` 是空的，精读的「文献信息」栏因此可能缺 DOI。
   编排层手里就有 item，应该把标题/DOI 直接传给 `read_main`，而不是让它去正文里猜。


## 2026-08-27 · 补两个小洞（都在真实数据上复验过）

1. **合并步骤加幂等**：`summary_full.html` 比两个输入都新就跳过。
   合并不花钱，但每次要写十几 MB，还连带一次同样大小的 Zotero 回写。
   复验：改了正文 → 重新合并；原样再跑 → 「[复用] 合并版比两个输入都新」。
2. **标题/DOI 改用权威值**：编排层手里有 Zotero item，却让 `read_main` 用正则
   去正文里抓 DOI，抓不到就空着 —— 这篇的 DOI 在 Zotero 里明明有，
   精读的「文献信息」栏却缺。现在 item 的 title/DOI 优先，正则只作兜底。
   复验：重跑后正文里出现 `10.1038/nature11409`。

**这两个是同一类错误，也是踩坑 #64 的同一类**：
> **有权威源就别去猜。**猜出来的值不会报错，只会安静地少一块信息 —— 最难发现。

新增 8 条测试，其中一条专门盯「改对了函数却忘了在 `run()` 里传」这种回归。


## 2026-08-27 · 阶段 3 下半：抽取搬进 pipelines + Zotero 写操作收进 adapters

**至此 `zotero_watcher.process_item` 不再拉任何子进程。**

### 1. 结构化抽取拆成两块

- `domain/schema`（**纯逻辑**）：字段定义 `SCHEMA` + `SCHEMA_VER`、抽取/自检提示词、
  层次化取正文、综述分流、出对比表。**返回字符串，不写盘、不联网。**
- `pipelines/extract`（**编排**）：读 full.md → 调 LLM（带自我评估重抽循环）→
  写 `structured/<key>.json` → 并入 compare 表，全程记账。
- `数据抽取/extract_structured.py` 变成命令行薄壳；
  `extract_batch.py` / `extract_library.py` 改为从 domain/pipelines 取，
  不再 `from extract_structured import ...`（脚本既是程序又是库，是这次重构要根治的病）。

**收益**：加字段 = 改 `domain/schema` + `SCHEMA_VER` +1，
待重抽清单就是 `jobs.stale('extract', schema_ver=N)` —— 不用翻文件、不用记得改过什么。

### 2. Zotero 写操作收成一处

原来三份写实现（watcher 的 `set_state_tag`、`zotero_upload_attachment`、
`upload_summaries`）各自拼 URL、各自拼鉴权头、各自处理版本冲突。
**真正的代价不是重复，是机器角色守卫要在每一份里各写一遍 —— 漏一处闸门就等于不存在。**

现在 `adapters/zotero_client/_web.py` 是全项目唯一出现 api.zotero.org 的文件，
`replace_tags` / `upload_attachment` 开头各一行守卫。策略（哪些标签互斥）
留在 watcher —— 那是业务规则，不是通信细节。

顺手拆掉一颗地雷：`upload_summaries` 里还留着「先删旧附件再传新的」，
正是踩坑 #28 反复弹同步冲突框的根因。watcher 早改成复用了，这个批量脚本没跟上
（同一个 bug 的两份实现，修了一份忘了另一份 —— 又一条「收进适配层」的理由）。

### 3. 守卫自己也被修了一次

架构守卫「每个写 Zotero 的地方都要有机器角色守卫」是**文本扫描**，
于是「在文档字符串里解释这些写操作收到哪儿去了」也被判成违规。
同一天在体检的「无弹窗」检查上也踩过一次。

这次没有绕开它，而是给它加了 `_code_text()`：**把注释与文档字符串原地抹成空格再扫**。
不是重新拼源码 —— 重拼会让 `role.require_prod` 变成 `role .require_prod`，
守卫反而全线误判（真发生了，见提交历史）。
并补了一条**元测试**：放过散文，但绝不能放过代码。
> 一个「不会误报」但也不会真正报警的守卫，比没有守卫更糟：
> 它给人一种「有东西在看着」的错觉。

### 4. 真实验证

- SI 线的 token 额度太小（踩坑 #65），真跑时撞上并修掉
- 回写链路复验：复用附件条目、标签互斥，三条全过
- 那篇 hydrogel 全套产物重生成：正文 7837 字 / SI 9.8 MB / 合并 12 MB，Zotero 已更新
- 状态库把「上次失败的 SI」自动排进了重做 —— 修好额度后一次就成


## 2026-08-27 · 控制面板接上真进度（状态库终于被人看见）

面板此前只能看**日志尾巴** —— 它回答不了「现在做到第几篇」，
更回答不了「上次是哪一步失败的」。`core/jobs` 建好之后，这些都是一句查询。

新增「精读进度」卡片：
- 每个步骤的 成功 / 失败 / 在跑 计数（解析、正文精读、SI 精读、合并、结构化抽取）
- 最近 8 次执行：文献、步骤、结果、用时，**失败的直接显示原因**
- 卡住超过 30 分钟还挂着 running 的会红字提示（进程被杀或断电会留下这种记录）
- 「升级后该重跑」：提示词或字段升版之后，旧产物自动被算进清单

`core/jobs` 补了公开的 `recent(n)`（面板原来在用 `_rows` 私有函数）。

**验证方式值得记一笔**：这次没有只跑测试就算完 —— 另起一个端口把面板真打开、
读了一遍渲染结果。理由是踩坑 #59：改了面板却没渲染一遍，
配置区整块变成 `NaN`，测试全绿也发现不了。
顺带确认了几处 test 档的显示：角色标签、数据资产、自启任务三行都按测试端措辞走。

面板还顺手指出两块新积木缺说明书（`domain/schema`、`pipelines/extract`），已补。


## 2026-08-27 · 收尾：Zotero 写操作彻底收成一处 + watcher 作为服务真跑一次

### 全项目只剩一个文件碰 Zotero 写接口

剩下的 6 个脚本（`autotag` / `delete_junk` / `tag_to_nested` / `zotero_rename` /
`import_by_doi` / `zotero_add_thesis`）也改走 `adapters/zotero_client`。
适配层补了三个原语：`patch_item` / `create_items` / `delete_item`。

**顺带把重试也统一了**：这 4 个脚本各自带着 429（限流）与 412（版本冲突）的重试循环，
而**最该有它的 watcher 改标签一次重试都没有** —— 精读做完了、标签没换成，
用户只看到「怎么还是待处理」。现在 `_with_retry` 在适配层里，谁写谁自动拥有。

`--force` 现在一路传到适配层：否则顶层守卫放行、适配层却拦下，
**两道闸对同一个决定给出不同答案**。

删除原语单独做，并在文档里写死边界：**只用于用户明确要删的条目，
绝不用于「更新产物」**（踩坑 #28 的「先删后传」正是那么来的）。
删一个本来就不在的条目算成功（404 → True），批量清理才不会假失败。

**真验证**（在测试库上，不碰真实库）：用 `import_by_doi` 新建一个条目 →
`replace_tags` 改标签 → `delete_item` 删掉 → 再删一次确认幂等，四步全过。

### watcher 作为服务真跑了一次（此前只调过 process_item）

打「待处理」标签 → 起 watcher → 5 秒内闭环：

```
[心跳] 轮询正常，待处理 1 篇 → [发现] → 跳过正文/SI（已精读）→ [复用] 合并版
→ [跳过抽取]（schema v1）→ [复用附件条目] → [状态] 待处理 → 全文精读
```

**全程零花费**，因为每一步都被状态库判定为「做过了」。
顺带验了单实例锁（第二个实例立刻退出并报出持锁 PID）与心跳/进展两个信号。

### `rerun_pro.py` 是死代码，重写了

它读的 `workflow_data/zotero_work`、`workflow_data/summary` 在现行数据契约下
**根本不存在**，一运行就抛 FileNotFoundError —— 而根目录的
`重跑精读_PRO版.bat` 还在正常地把用户引过来，说明里写的输出位置也和代码对不上。

改为走 `core.paths` + `pipelines.deepread`：列出「解析结果还在」的文献
（标题从 meta.json 读，不再需要 Zotero 开着），重跑前把旧版备份成 `.bak`，
并记进状态库（model=pro）。
