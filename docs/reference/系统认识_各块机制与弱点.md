# 系统认识：各块的机制与弱点

<!-- 由 host/codegen/mechanisms.py 生成，**别手改**。改源：各块的文档字符串 / 踩坑记录 / git -->

给 agent 的地图：**每块大概怎么运转、靠什么、容易在哪坏**。不必记细节，出问题时先来这里定位。
「容易在哪坏」不靠人写 —— 一块的外部依赖 + 它踩过的坑，就是它的弱点清单。

生成于 2026-09-15 22:15。共 51 块。


## 工具（tools）

### `tools/ask`
库内问答：向**我自己的**文献库提问，中文作答并附来源。
- **靠什么**：外接口 embed、llm_client、vectordb、zotero_client
- **碰哪些数据**：`VECTOR_DB`、`all_keys`、`fulltext`、`meta`、`si_fulltext`
- **踩过的坑**（弱点在这里）：#82 施工文件的「映射表」和它自己的「硬规则」打架，以规则为准；#84 标准开头写在文档字符串**前面**，`__doc__` 就是 None；#89 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了；#93 走子进程绕过 import，不叫「合规」；#126 粗层把精层挡在门外 —— 判重问的是「这篇有没有块」，该问「这一档有没有」；#154 用嵌套 heredoc 写代码，`
- **最近改动**：2026-09-13 拆冗余 + 根目录准入（重写历史，抹掉误提交的个人文件） ｜ 2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本

### `tools/askworld`
问全世界：向全球文献提一个科学问题，拿到**带原文出处**的中文回答。
- **靠什么**：外接口 llm_client、query_expand、sciverse、zotero_client
- **踩过的坑**（弱点在这里）：#82 施工文件的「映射表」和它自己的「硬规则」打架，以规则为准；#154 用嵌套 heredoc 写代码，`
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-10 把研究真正需要的能力补成 tool（外包第 2 步） ｜ 2026-09-02 花钱的入口放开三个给模型，但每次强制弹窗 —— 判据从「花不花钱」改成「代价多大」

### `tools/curate`
库房维护：让 Zotero 库和平台产物保持整齐、一致、不掉队。
- **靠什么**：外接口 llm_client、openalex、zotero_client
- **碰哪些数据**：`ABSTRACTS`、`CURATED`、`SERVING`、`STRUCTURED`、`all_keys`、`journals`、`junk_list`、`meta`
- **踩过的坑**（弱点在这里）：#93 走子进程绕过 import，不叫「合规」
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-07 期刊分级也从结构化记录里取 DOI，不只看 meta.json

### `tools/deepread`
精读一篇文献 → 一份中文图文精读报告。
- **靠什么**：外接口 llm_client、pdf_parse、zotero_client；纯逻辑 figure_crop、schema
- **碰哪些数据**：`all_keys`、`check_key`、`find_local_si`、`layout`、`local_pdf`、`meta`、`paper_dir`、`parsed_dir`、`si_fulltext`、`si_parsed_dir`、`si_summary`、`summary`、`summary_full`
- **踩过的坑**（弱点在这里）：#81 按文件名找脚本 / 找进程的地方，搬家时会静默失效；#84 标准开头写在文档字符串**前面**，`__doc__` 就是 None；#86 花钱的能力如果暴露成 MCP `tool`，等于把钱包交给模型；#105 写死在下游的默认值，会让上游刚加的能力**静默失效**；#120 附件传上去了，用户点开却「找不到附件」——而这个坑平台早解决过
- **最近改动**：2026-09-15 精读 HTML 渲染转义 < > &（裸 < 吞掉后半段）；评分器反转义；变更记录 ｜ 2026-09-15 读了高分/低分各一篇：深解段漂到下一张图就截断、半句没翻的词也抓、作者行清掉上标；fig/exp 提示词 @v3（数值一个不落、步骤写到成品 ｜ 2026-09-15 综述图多时深解下限再降一档；范式文档补汉字口径与四轮评测数据

### `tools/digitize`
图表数字化：论文图里的曲线/散点/柱状图 → 可用的 X-Y 数值。
- **靠什么**：外接口 llm_client；纯逻辑 figure_crop
- **碰哪些数据**：`curves`、`parsed_dir`
- **踩过的坑**（弱点在这里）：#139 写死的视觉模型被下线，看图这条线整条哑了一天
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-07 看图这条线修好：模型名从写死改成面板上能换的配置项

### `tools/direction`
方向地图：一批种子论文 → 这个领域长什么样（谁做了什么、有哪些簇）
- **靠什么**：外接口 embed、llm_client、openalex、vectordb、wechat_seed、zotero_client；第三方 numpy；纯逻辑 schema
- **碰哪些数据**：`VECTOR_DB`、`abstract_record`、`all_work_ids`、`check_work_id`、`direction_bands`、`direction_db`、`direction_dir`、`direction_spec`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-07 摘要抽取：一个模型的免费额度用完就换下一个

### `tools/discover`
找新文献：搜全球 → 对照我的库 → 按「跟我多相关」排序 → 收进库。
- **靠什么**：外接口 crossref、embed、openalex、query_expand、sciverse、snowball、vectordb、zotero_client；纯逻辑 libmatch
- **碰哪些数据**：`last_search`、`search_record`
- **踩过的坑**（弱点在这里）：#39 用户搜「PBS」，一次暴露三个 bug（缩写歧义 / 漏导入 / 阈值杀光结果）；#82 施工文件的「映射表」和它自己的「硬规则」打架，以规则为准；#149 A 机上跑 discover，排序会安静地退化成「按被引量排」；#157 常驻进程抱着 chromadb 连接不放，另一个进程写过之后再写 = 向量库坏掉
- **最近改动**：2026-09-14 真实任务暴露的 7 个问题：第二条作业通道、OpenAlex 限流退 Sciverse、检索留档、书章节标记/过滤、watcher 自适应批 ｜ 2026-09-13 按方法论对照做四项：雪球起始集多样性（Wohlin）、检索留档（PRISMA-S 子集）、空白分类（Miles）、概念矩阵（Webster  ｜ 2026-09-13 新方向模式：雪球种子改从本次命中里挑，不再从库里挑

### `tools/extract`
结构化抽取：一篇文献 → 对齐的机器可读字段 → 横向对比表。
- **靠什么**：外接口 llm_client、pdf_parse、zotero_client；纯逻辑 schema
- **碰哪些数据**：`STRUCTURED`、`all_keys`、`check_key`、`chunk_measurements`、`compare`、`fulltext`、`log`、`meta`、`parsed_dir`、`si_fulltext`、`structured`、`structured_backup`
- **踩过的坑**（弱点在这里）：#85 没人认识的命令行参数会被当成「没给参数」，于是走进最贵那条路；#86 花钱的能力如果暴露成 MCP `tool`，等于把钱包交给模型；#89 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了；#93 走子进程绕过 import，不叫「合规」；#136 「故意破坏守卫再还原」会留下过期的 .pyc，测试红得莫名其妙；#138 `--stale --list` 不存在，于是它直接走进了全库抽取那条路
- **最近改动**：2026-09-13 拆冗余 + 根目录准入（重写历史，抹掉误提交的个人文件） ｜ 2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本

### `tools/getpdf`
一批 DOI → 一批 PDF 落地（本工具的全部业务逻辑都在这个文件）
- **靠什么**：外接口 crossref、pdf_fetch、pdf_parse、zotero_client；纯逻辑 schema
- **碰哪些数据**：`INCOMING`、`ROOT`、`check_key`、`find_local_si`、`fulltext`、`is_zotero_key`、`local_pdf`、`local_si`、`outline`、`paper_id_from_doi`、`parsed_dir`、`runtime`、`si_fulltext`
- **踩过的坑**（弱点在这里）：#115 把「所有候选各试一趟」写成两轮，结果 SI 冒充了正文；#117 遥控工具超时后换地址重跑，命令被执行了两遍；#120 附件传上去了，用户点开却「找不到附件」——而这个坑平台早解决过；#151 工具往 stdout 打的日志，直接混进了 MCP 的报文流；#153 外部 agent 写中文文档，`	` `` `
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-15 存储去重：取件区文件落地时搬走而不是复制；MineRU 的 origin.pdf 换成指向正本的硬链接；host/deploy/dedupe ｜ 2026-09-14 取件提速：fetch_both 一次落地取正文+SI、链接一出现就走、浏览器连接复用、SI 滚动加快；金标 --两路 并发开关（上限 2，按

### `tools/journalwatch`
盯新刊：好期刊一出新文章就列出来（2026-09-15 建）
- **靠什么**：外接口 crossref
- **碰哪些数据**：`journal_watch`、`journal_watch_seen`、`last_search`、`radar_db`
- **踩过的坑**（弱点在这里）：#160 Crossref 按刊名搜 ISSN 会给错刊
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-15 盯新刊/回填：证据库 DOI 表扫一次（catalog.find 每篇重扫全库，回填卡死）；踩坑 #161 ｜ 2026-09-15 0 级文献雷达：journalwatch 入雷达库（SQLite，题目/摘要/参考文献）、三年回填（断点续跑）、监听服务每天巡逻一次；cro

### `tools/library`
查我的 Zotero 库：搜条目、看合集标签、拿正文 PDF 与全文。
- **靠什么**：外接口 embed、vectordb、zotero_client；纯逻辑 schema
- **碰哪些数据**：`fulltext`、`outline`、`si_fulltext`
- **踩过的坑**（弱点在这里）：#84 标准开头写在文档字符串**前面**，`__doc__` 就是 None；#86 花钱的能力如果暴露成 MCP `tool`，等于把钱包交给模型；#89 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了
- **最近改动**：2026-09-14 骨架：图注挤在上一段后面（MineRU 图片行两空格换行、无空行）也认得出 —— 7 图论文原来只认出 4 张 ｜ 2026-09-14 骨架：结尾那块的段地址不再固定叫 p1（真实任务里看到两个 s12.p1） ｜ 2026-09-14 library_refs：中文题名归一成空串导致全部参考文献被标「已在库」（真实任务里发现）

### `tools/litsearch`
对抗式检索的**原料通道**：精确检索 / 取摘要 / 雪球 / 我有没有。
- **靠什么**：外接口 openalex、sciverse、snowball、zotero_client；纯逻辑 libmatch
- **碰哪些数据**：`fulltext`、`search_record`
- **踩过的坑**（弱点在这里）：#150 MCP 的 handler 返回裸字符串，报出来是「内部错误」且不带工具名
- **最近改动**：2026-09-14 真实任务暴露的 7 个问题：第二条作业通道、OpenAlex 限流退 Sciverse、检索留档、书章节标记/过滤、watcher 自适应批 ｜ 2026-09-13 给模型「自己用文献」补五样：库内检索 library_retrieve、引文 library_refs、检索结果标可读、取全文带菜单、dee ｜ 2026-09-10 检索结果带出版商与付费状态：防 LLM 偷懒，不是过滤 MDPI

### `tools/paperdb`
文献查询库：一堆结构化 JSON → 一个能筛、能分组、能比大小的库。
- **靠什么**：纯逻辑 schema
- **碰哪些数据**：`ABSTRACTS`、`STRUCTURED`、`all_keys`、`chunk_measurements`、`compare`、`curves`、`fulltext`、`is_zotero_key`、`journals`、`papers_db`
- **踩过的坑**（弱点在这里）：#83 重命名目录之后，架构守卫会**静默地不守任何东西**；#86 花钱的能力如果暴露成 MCP `tool`，等于把钱包交给模型；#131 同一个隔离疏忽栽了三次 —— 「加一个数据源」等于「三份测试各漏一处」；#140 抠出第一张图的当天，paperdb 自测在主力机上红了
- **最近改动**：2026-09-13 按方法论对照做四项：雪球起始集多样性（Wohlin）、检索留档（PRISMA-S 子集）、空白分类（Miles）、概念矩阵（Webster  ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-07 paperdb 自测漏隔离 CURATED：抠出第一张图的当天就红了


## 平台自身（host）

### `host/autosync`
每小时一次的「新文献自动入库」。
- **靠什么**：外接口 embed、zotero_client
- **踩过的坑**（弱点在这里）：#93 走子进程绕过 import，不叫「合规」
- **最近改动**：2026-09-13 拆冗余 + 根目录准入（重写历史，抹掉误提交的个人文件） ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零

### `host/codegen`
所有生成器。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`CODE_RINGS`、`NOISE_DIRS`、`NON_WORKFLOW_DIRS`、`ROOT`、`block_dirs`、`xxx`
- **踩过的坑**（弱点在这里）：#80 自动区块的标记里写了生成器自己的路径，生成器一搬家就多出一份；#87 `git rm -r` 删掉一个包之后，结构树里它还在；#91 坑号撞车 —— 同一个编号被两条完全不同的记录占着；#135 Bash 工具的 heredoc 会把 `\\` 吞成 `\`
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-07 分级守卫上线，零号判据收敛成一个源 ｜ 2026-09-07 宪法分级：把「不许质疑」的范围缩到该锁死的那一小块

### `host/deploy`
上线：把代码拉到本机，并**重启跑着旧代码的常驻服务**。
- **靠什么**：外接口 pdf_parse
- **碰哪些数据**：`DATA`、`INCOMING`、`ROOT`、`find_local_si`、`local_pdf`、`parsed_dir`、`si_parsed_dir`
- **踩过的坑**（弱点在这里）：#81 按文件名找脚本 / 找进程的地方，搬家时会静默失效；#97 连不上主力机的四种原因，报错长得完全不一样但都像「连不上」；#101 「密钥读不出来」的真凶不是权限，是**登录会话的种类**
- **最近改动**：2026-09-15 origin.pdf 去重判据改为页面几何一致（MineRU 重存过字节不同；裁图实测像素相同） ｜ 2026-09-15 存储去重：取件区文件落地时搬走而不是复制；MineRU 的 origin.pdf 换成指向正本的硬链接；host/deploy/dedupe ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本

### `host/doctor`
体检与诊断：平台自己给自己看病。
- **靠什么**：外接口 llm_client、pdf_parse、vectordb、zotero_client
- **碰哪些数据**：`CURATED`、`LOGS`、`NOISE_DIRS`、`NON_WORKFLOW_DIRS`、`ROOT`、`SCANNED_ROOTS`、`STRUCTURED`、`VECTOR_DB`、`all_keys`、`block_dirs`、`log`、`missing_artifacts`、`paper_dir`、`paper_raw_dir`
- **踩过的坑**（弱点在这里）：#90 为验证守卫「故意违反一次」，还原时用 `git checkout --` 把本窗未暂存的
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-10 服务端花钱闸：因为 confirm 那道闸换个客户端就没了

### `host/ingest`
落地即自动：正本一到，**不花大模型钱的那几步**自己做完。
- **靠什么**：外接口 pdf_parse、vectordb
- **碰哪些数据**：`check_key`、`find_local_si`、`fulltext`、`local_pdf`、`outline`、`parsed_dir`、`si_fulltext`、`si_parsed_dir`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-13 向量库：常驻进程写完就放连接（踩坑 #157：抱着旧连接写把库盖坏） ｜ 2026-09-13 ingest 测试对齐「失败隔天再试」

### `host/mcp`
MCP 协议层：把平台的能力暴露给 agent。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`ROOT`、`runtime`
- **踩过的坑**（弱点在这里）：#84 标准开头写在文档字符串**前面**，`__doc__` 就是 None；#86 花钱的能力如果暴露成 MCP `tool`，等于把钱包交给模型；#150 MCP 的 handler 返回裸字符串，报出来是「内部错误」且不带工具名；#151 工具往 stdout 打的日志，直接混进了 MCP 的报文流；#152 模块起名叫 `http.py`，把标准库整个顶掉了
- **最近改动**：2026-09-10 把研究真正需要的能力补成 tool（外包第 2 步） ｜ 2026-09-09 MCP 长出第二条腿：Streamable HTTP ｜ 2026-09-09 stdout 是协议通道，不是日志通道

### `host/panel`
控制面板：平台的仪表盘与开关（用户唯一的图形入口）。
- **靠什么**：外接口 llm_client、pdf_parse、zotero_client；纯逻辑 schema
- **碰哪些数据**：`CURATED`、`NON_WORKFLOW_DIRS`、`block_dir`、`block_dirs`、`log`、`runtime`、`summary`
- **踩过的坑**（弱点在这里）：#146 改名失败，真凶是一个没有窗口的进程
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-14 launcher：去掉重复的 import sys ｜ 2026-09-14 控制面板：启动器不再自己开浏览器（open_panel 已经开了一次 → 每次弹两个页面）

### `host/watcher`
「打个标签就全自动」的那条常驻服务。
- **靠什么**：外接口 llm_client、pdf_parse、zotero_client
- **碰哪些数据**：`CURATED`、`find_local_si`、`local_pdf`、`reference`、`summary`
- **踩过的坑**（弱点在这里）：#66 主力机的密钥早就失效了，而体检一直是绿的；#67 用户重新打标签，watcher 看得见却一动不动；#71 面板的「重启服务」按钮从来没生效过；#81 按文件名找脚本 / 找进程的地方，搬家时会静默失效；#146 改名失败，真凶是一个没有窗口的进程；#148 一次失败的上传留下三个空附件，用户看到「三个 summary 都打不开」
- **最近改动**：2026-09-15 0 级文献雷达：journalwatch 入雷达库（SQLite，题目/摘要/参考文献）、三年回填（断点续跑）、监听服务每天巡逻一次；cro ｜ 2026-09-15 打标签的条目按 DOI 认出证据库里的同一篇（不再开第二个目录）；有公众号范文的推文当正文精读、只补 SI ｜ 2026-09-14 真实任务暴露的 7 个问题：第二条作业通道、OpenAlex 限流退 Sciverse、检索留档、书章节标记/过滤、watcher 自适应批

### `host/wechat_import`
公众号推送 → Zotero 条目 + 一份现成的正文精读
- **靠什么**：外接口 wechat_seed、zotero_client；第三方 PyMuPDF（读 PDF）
- **碰哪些数据**：`check_key`、`find_local_si`、`golden_index`、`local_pdf`、`local_si`、`paper_dir`、`paper_raw_dir`、`reference`、`summary`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录 ｜ 2026-09-15 打标签的条目按 DOI 认出证据库里的同一篇（不再开第二个目录）；有公众号范文的推文当正文精读、只补 SI ｜ 2026-09-14 取件提速：fetch_both 一次落地取正文+SI、链接一出现就走、浏览器连接复用、SI 滚动加快；金标 --两路 并发开关（上限 2，按


## 外接口（shared/adapters）

### `shared/adapters/crossref`
DOI 元数据基础件（原子能力：一个 DOI → 这篇文献的书目信息）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-15 0 级文献雷达：journalwatch 入雷达库（SQLite，题目/摘要/参考文献）、三年回填（断点续跑）、监听服务每天巡逻一次；cro ｜ 2026-09-15 crossref.normalize：标题里的 <i> 标签去掉 ｜ 2026-09-15 盯新刊：tools/journalwatch 按 ISSN + Crossref 登记日列好刊新论文、标库里有没有（只列不取）；crossr

### `shared/adapters/embed`
文本向量化基础件（原子能力：文本 → 向量 + 文本预处理原子操作）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#95 一条规则同时管着差三个数量级的两件事；#116 出版商的「PDF 链接」常常不是 PDF，而且有三种花样；#120 附件传上去了，用户点开却「找不到附件」——而这个坑平台早解决过；#126 粗层把精层挡在门外 —— 判重问的是「这篇有没有块」，该问「这一档有没有」；#129 Chroma 的 `add` 遇到已有 id 会**一声不吭地丢掉** —— 「重新向量
- **最近改动**：2026-09-11 向量化纳入路由表，且明确「不许有备用通道」 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零

### `shared/adapters/llm_client`
LLM 调用基础件（原子能力：文本 → LLM → 文本/JSON）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#105 写死在下游的默认值，会让上游刚加的能力**静默失效**；#106 Gemini 3 的思考**关不掉**，而且文档列的合法值它不认；#111 给提示词加「字数下限」＝ 给模型发了编造许可证；#130 `thinking=False` 对百炼一直是空话 —— 漏翻译不报错，只是安静地一直开；#139 写死的视觉模型被下线，看图这条线整条哑了一天；#155 Ollama 的「关思考」写在 options 里，等于没关
- **最近改动**：2026-09-12 百炼实测能通；修「关思考」在路由路径上静默失效的 bug ｜ 2026-09-11 Ollama 的 think 提到顶层：本地模型快 20 倍（踩坑 #155） ｜ 2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本

### `shared/adapters/openalex`
OpenAlex 学术检索 API（免费、无需密钥）。
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#149 A 机上跑 discover，排序会安静地退化成「按被引量排」
- **最近改动**：2026-09-10 检索结果带出版商与付费状态：防 LLM 偷懒，不是过滤 MDPI ｜ 2026-09-10 把「替调用方判断」的三处拆掉：递事实 vs 替判断 ｜ 2026-09-08 改名 literature-platform：核心是证据库，不是 Zotero

### `shared/adapters/pdf_fetch`
出版商全文基础件（原子能力：一个 DOI → 这篇文献的 PDF 字节）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#112 9222 被一个**普通** Chrome 占着，浏览器起得来但永远连不上；#159 取件提速后 ScienceDirect 篇篇「异常」，一晚漏了 46 个标签把 Edge
- **最近改动**：2026-09-14 pdf_fetch：清扫不动首页标签、最后一个标签不关（Edge 关掉最后一个标签就退出，整批连不上） ｜ 2026-09-14 pdf_fetch：页面内 JS 全部走 CDP Runtime.evaluate 带硬超时（Wiley 阅读器页把 evaluate 卡了 ｜ 2026-09-14 pdf_fetch：轮询时容忍 ScienceDirect 的二次跳转；先关标签再丢连接（一晚漏了 46 个标签把 Edge 拖死）；连上时

### `shared/adapters/pdf_parse`
PDF 解析基础件（原子能力：PDF → 结构化文本+图坐标）
- **靠什么**：第三方 PyMuPDF（读 PDF）、python-docx
- **踩过的坑**（弱点在这里）：#34 体检脚本自己在项目里造垃圾，还偷偷调用付费 API；#61 看门狗一直在杀**正在干活**的 watcher；#66 主力机的密钥早就失效了，而体检一直是绿的；#126 粗层把精层挡在门外 —— 判重问的是「这篇有没有块」，该问「这一档有没有」；#129 Chroma 的 `add` 遇到已有 id 会**一声不吭地丢掉** —— 「重新向量；#134 把「名字里有两个数字」当判据，误伤了写着测试条件的表头
- **最近改动**：2026-09-15 origin.pdf 去重判据改为页面几何一致（MineRU 重存过字节不同；裁图实测像素相同） ｜ 2026-09-15 存储去重：取件区文件落地时搬走而不是复制；MineRU 的 origin.pdf 换成指向正本的硬链接；host/deploy/dedupe ｜ 2026-09-13 拆冗余 + 根目录准入（重写历史，抹掉误提交的个人文件）

### `shared/adapters/query_expand`
检索式扩展基础件（原子能力：一个研究问题 → 多个互补的英文检索式）
- **靠什么**：外接口 llm_client
- **踩过的坑**（弱点在这里）：#82 施工文件的「映射表」和它自己的「硬规则」打架，以规则为准
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零

### `shared/adapters/sciverse`
全球文献库基础件（原子能力：查询 → 全球科学文献的结构化结果）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零 ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

### `shared/adapters/snowball`
引用雪球基础件（原子能力：种子文献 → 沿引用网络扩展出的相关文献）
- **靠什么**：外接口 openalex
- **踩过的坑**（弱点在这里）：#82 施工文件的「映射表」和它自己的「硬规则」打架，以规则为准
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-01 收尾三：补 snowball 的说明书与三件套守卫 · 新增《项目结构导览》 ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

### `shared/adapters/vectordb`
向量库（当前实现：Chroma）。
- **靠什么**：第三方 Chroma 向量库
- **碰哪些数据**：`VECTOR_DB`
- **踩过的坑**（弱点在这里）：#126 粗层把精层挡在门外 —— 判重问的是「这篇有没有块」，该问「这一档有没有」；#129 Chroma 的 `add` 遇到已有 id 会**一声不吭地丢掉** —— 「重新向量；#157 常驻进程抱着 chromadb 连接不放，另一个进程写过之后再写 = 向量库坏掉
- **最近改动**：2026-09-13 向量库：常驻进程写完就放连接（踩坑 #157：抱着旧连接写把库盖坏） ｜ 2026-09-06 向量库的 add 必须是同 id 覆盖 —— Chroma 的 add 会静默丢掉 ｜ 2026-09-06 精读全文终于进得了问答库：判重按档次算，不按「这篇有没有块」

### `shared/adapters/wechat_seed`
公众号推送 → 种子文献（外接口：把「别人的品味」变成一批 DOI）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-14 公众号推送解析：空行/残行是段界（踩坑 #158）；金标 --重写范文；范式实测数据入档 ｜ 2026-09-06 反过来用公众号：推文当正文精读，平台只补 SI ｜ 2026-08-30 R3 窗：切出 ask / askworld / discover / direction / curate

### `shared/adapters/zotero_client`
Zotero 接口基础件（可独立成 GitHub 项目的候选）
- **靠什么**：外接口 zotero_client
- **踩过的坑**（弱点在这里）：#46 面板存 `ZOTERO_API_HOST`，积木读 `ZOTERO_LOCAL_API`；#79 给模块名加前缀的正则，会把已经加过前缀的再加一遍；#120 附件传上去了，用户点开却「找不到附件」——而这个坑平台早解决过；#148 一次失败的上传留下三个空附件，用户看到「三个 summary 都打不开」
- **最近改动**：2026-09-13 Zotero 降级：证据库是全集，收文献不再写 Zotero ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-09-08 不再往 Zotero 官方存储传附件：那 300MB 一直在白烧


## 纯逻辑（shared/domain）

### `shared/domain/figure_crop`
裁图基础件（原子能力：解析结果 → 完整 Figure 图片）
- **靠什么**：第三方 PyMuPDF（读 PDF）
- **踩过的坑**（弱点在这里）：#61 看门狗一直在杀**正在干活**的 watcher；#124 云端视觉模型也会编数据 —— 主曲线读得准，短曲线被外推成直线
- **最近改动**：2026-09-14 金标第一轮暴露：Wiley 全大写 FIGURE 让裁图图注全空（不分大小写）；子图行不另算图注；没图注时按顺序对应；深解预算收到 3000 ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零

### `shared/domain/libmatch`
「这篇跟我的库是什么关系」——**纯判断，不联网、不知道文件在哪**。
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-14 真实任务暴露的 7 个问题：第二条作业通道、OpenAlex 限流退 Sciverse、检索留档、书章节标记/过滤、watcher 自适应批 ｜ 2026-09-13 按方法论对照做四项：雪球起始集多样性（Wohlin）、检索留档（PRISMA-S 子集）、空白分类（Miles）、概念矩阵（Webster  ｜ 2026-09-09 拆「找文献」：对抗式检索单独成一条路，不再跟排序捆在一起

### `shared/domain/schema`
结构化抽取的字段定义与文本处理（纯逻辑，原子能力）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#125 给模型留了「main text」这个选项，于是「有出处率」永远 100%；#132 提示词里的示例数字被小模型原样抄出来
- **最近改动**：2026-09-14 金标第一轮暴露：Wiley 全大写 FIGURE 让裁图图注全空（不分大小写）；子图行不另算图注；没图注时按顺序对应；深解预算收到 3000 ｜ 2026-09-14 骨架：图注挤在上一段后面（MineRU 图片行两空格换行、无空行）也认得出 —— 7 图论文原来只认出 4 张 ｜ 2026-09-14 骨架：结尾那块的段地址不再固定叫 p1（真实任务里看到两个 s12.p1）


## 基础设施（shared/kernel）

### `shared/kernel/budget.py`
每天花多少的账本与闸门 —— **不依赖客户端，装在服务端**。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`runtime`
- **踩过的坑**（弱点在这里）：#47 「只绑 127.0.0.1」挡得住别的机器，挡不住你自己浏览器里的网页；#65 SI 精读的 token 额度是正文线的 1/5，于是「有时成有时败」；#77 OpenAlex 从 2026-02 起按量计费，「免费无密钥」这条旧认知已经过期；#106 Gemini 3 的思考**关不掉**，而且文档列的合法值它不认；#110 Gemini 免费档的 503 跟请求大小无关，是**型号级**的排队；#130 `thinking=False` 对百炼一直是空话 —— 漏翻译不报错，只是安静地一直开
- **最近改动**：2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本 ｜ 2026-09-10 服务端花钱闸：因为 confirm 那道闸换个客户端就没了

### `shared/kernel/catalog.py`
证据库目录：本地到底有哪些文献、每篇手上有什么。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`CURATED`、`RAW`、`check_key`、`find_local_si`、`fulltext`、`is_zotero_key`、`local_pdf`、`meta`、`paper_dir`、`si_fulltext`、`structured`、`summary`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-13 拆冗余 + 根目录准入（重写历史，抹掉误提交的个人文件） ｜ 2026-09-13 Zotero 降级：证据库是全集，收文献不再写 Zotero

### `shared/kernel/cli`
命令行参数解析基础件（原子能力：全项目只有一种取参数的方式）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#85 没人认识的命令行参数会被当成「没给参数」，于是走进最贵那条路；#138 `--stale --list` 不存在，于是它直接走进了全库抽取那条路
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零 ｜ 2026-08-31 R4 窗：十个工具补齐五件套 + MCP 从手写清单改成读 tool.toml 聚合

### `shared/kernel/config`
配置/密钥加载基础件（原子能力：统一、可靠地拿到密钥）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#78 一整包代码搬深一层，`__file__` 数上来的 ROOT 会静默指错
- **最近改动**：2026-09-12 百炼实测能通；修「关思考」在路由路径上静默失效的 bug ｜ 2026-09-11 向量化纳入路由表，且明确「不许有备用通道」 ｜ 2026-09-11 大模型管理拆成三段：通道 / 用途 / 账本

### `shared/kernel/errors.py`
异常分类。
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 中段：docs 按用途归类 · .claude 改成全生成 · 坑按工具切片 ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

### `shared/kernel/heartbeat.py`
常驻服务的两种「我还好」信号。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`runtime`
- **踩过的坑**（弱点在这里）：#61 看门狗一直在杀**正在干活**的 watcher；#62 重启计划任务换不掉 watcher —— 它是**孙子进程**；#63 面板上改配置，**没点保存就被自动刷新冲回去了**；#66 主力机的密钥早就失效了，而体检一直是绿的；#67 用户重新打标签，watcher 看得见却一动不动；#71 面板的「重启服务」按钮从来没生效过
- **最近改动**：2026-09-15 heartbeat.overview：信号文件读不出时跳过（主力机上有旧格式文件） ｜ 2026-09-15 heartbeat 后台线程被注销后自行退出（测试的 'w' 心跳曾漏进真实目录） ｜ 2026-09-15 卡住了要有人知道：heartbeat.done/overview，取件/落地/金标/回填写进展，面板与体检报卡住；测试心跳走临时目录

### `shared/kernel/jobs.py`
任务状态库（SQLite，标准库，零新依赖）。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`check_key`、`has`、`state_db`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-08-31 R7 中段：docs 按用途归类 · .claude 改成全生成 · 坑按工具切片 ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

### `shared/kernel/log.py`
统一的日志落点。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`log`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-09 主力机时不时弹 python 窗口：计划任务用的是 python.exe ｜ 2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 中段：docs 按用途归类 · .claude 改成全生成 · 坑按工具切片

### `shared/kernel/mcp_prompt.py`
把「花钱/有副作用的能力」拼成一段给模型看的话。
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R4 窗：十个工具补齐五件套 + MCP 从手写清单改成读 tool.toml 聚合

### `shared/kernel/paths.py`
数据契约的唯一实现。
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`fulltext`、`has`、`paper_dir`、`paper_id_from_doi`
- **踩过的坑**（弱点在这里）：#78 一整包代码搬深一层，`__file__` 数上来的 ROOT 会静默指错；#89 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了
- **最近改动**：2026-09-15 0 级文献雷达：journalwatch 入雷达库（SQLite，题目/摘要/参考文献）、三年回填（断点续跑）、监听服务每天巡逻一次；cro ｜ 2026-09-15 盯新刊：tools/journalwatch 按 ISSN + Crossref 登记日列好刊新论文、标库里有没有（只列不取）；crossr ｜ 2026-09-14 金标评测器：对着范文自动打分（骨架/图两段/篇幅比/数字覆盖/术语覆盖/两问/通俗理解），--金标评测 一轮一个 tag

### `shared/kernel/proc_lock`
单实例锁基础件（原子能力：保证同一个程序同时只跑一份）
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`LOGS`
- **踩过的坑**（弱点在这里）：#78 一整包代码搬深一层，`__file__` 数上来的 ROOT 会静默指错
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零 ｜ 2026-08-31 R7 上半：三条硬规则的违规清零 —— watcher 归 host、索引自刷新、单用户块下放

### `shared/kernel/prompts`
提示词的唯一读取口（原子能力：一段提示词 = 一个只增不改的版本文件）
- **靠什么**：只靠标准库与 kernel（不联网）
- **碰哪些数据**：`ROOT`
- **踩过的坑**：还没有记录在案的
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R5 上半：提示词全部归位到 <拥有者>/prompts/<名>_v<N>.txt，只增不改

### `shared/kernel/role.py`
这台机器是什么角色。
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#153 外部 agent 写中文文档，`	` `` `；#154 用嵌套 heredoc 写代码，`
- **最近改动**：2026-08-31 R7 中段：docs 按用途归类 · .claude 改成全生成 · 坑按工具切片 ｜ 2026-08-31 R6 窗：workflow_data/ → 五层 data/，7 个 .bat 进 launch/ ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

### `shared/kernel/subproc`
子进程调用基础件（原子能力：跑一条外部命令，安静、带超时、编码正确）
- **靠什么**：只靠标准库与 kernel（不联网）
- **踩过的坑**（弱点在这里）：#145 并进主仓库才发现，toolforge 生成的骨架自带一个编码 bug
- **最近改动**：2026-09-09 术语去口语化：当初为了让我看懂的比喻，现在成了翻译成本 ｜ 2026-08-31 R7 收尾：AGENTS.md 成正本 · 定时同步归 host · docs 与散文里的旧路径清零 ｜ 2026-08-30 R1 窗：开分支 + 建骨架 + core/domain/adapters → shared/，平台管理+MCP服务 → host/

