# 坑的总目录（自动生成，别手改）

源：各 `tools/<工具>/INCIDENTS.md`。生成器：`host/codegen/incidents.py`。

**全文在 [踩坑记录.md](踩坑记录.md)**（101 条，按时间排，唯一真相）。
这里只回答一个问题：**我要改的这个工具，前人在哪些地方栽过？**

## 按工具

### ask（2 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #89 | 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了 | 向量化的输入来自 raw，产物在 serving —— 两个目录 |
| #49 | 「模块能 import 成功」远不等于「代码没问题」 | 本工具的自测被这个骗过一次 |

### askworld（2 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #35 | 中文检索式让全球文献召回质量崩塌 | 问全世界一律用英文检索式 |
| #36 | Sciverse 把补充材料当独立文献返回，9 条重复挤爆结果 | 结果要去重，否则一个 SI 能占满半页 |

### curate（3 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #20 | Zotero「找不到文件」：附件条目记录的文件名与实际写入的不一致 | 改名这件事本身就是这条坑的产物 |
| #23 | 同一文献出现两个 summary 附件 | 去重要认得出「同一份东西的两个条目」 |
| #10 | Zotero API 限流 429 + 重复处理 | 全库遍历改标签最容易撞限流 |

### deepread（13 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #7 | 图被 MineRU 打散 / 摘要图混入 / 图没截全 | **别自己重写裁图**，用 `shared.domain.figure_crop` —— 那一条的全部智慧固化在那儿 |
| #1 #2 | MineRU PUT 上传 403；轮询字段结构与旧版完全不同 | 解析这一环只许经 `shared.adapters.pdf_parse`，别在工具里另写一份 |
| #21 #22 | Elsevier 的 SI 是 .docx 不是 PDF，导致漏检；docx 的 SI 当初没取图 | `has_si` 与 SI 线都要认 .docx |
| #23 | 同一文献出现两个 summary 附件 | 回写要**复用已有附件条目**，只覆盖文件内容 |
| #18 | Zotero 每篇都弹「冲突解决」框 | 根因是「先删后传」的删除动作进了同步链 —— 绝不删附件条目 |
| #20 | Zotero「找不到文件」：附件条目记录的文件名与实际写入的不一致 | 附件条目记录的文件名必须与实际写入的一致 |
| #27 | 精读只剩图没有字 | DeepSeek V4 思考模式会吃光 max_tokens |
| #65 | SI 精读「有时成有时败」 | SI 线的 token 额度曾是正文线的 1/5 |
| #29 | 用户打了标签没反应 | 改触发标签名 = 要求用户记住新词，等于没改 |
| #3 | 3060 跑精读版太慢，不实用 | 精读是长输出，本地卡跑不动 —— 走云端 flash |
| #17 #19 | 批量精读 401；子进程拿不到 setx 设的 MINERU_TOKEN | 密钥一律走 `shared.kernel.config.get_key`，别指望环境继承 |
| #64 | 问本地 API「我刚传上去的附件在不在」，答案永远是「不在」 | 那是本地 API 的固有行为（滞后于同步），别怀疑上传失败 |
| #88 | 换用 `paths.summary(key)` 顺手引入了一处会抛异常的新行为 | 搬家式改动也要看被调函数的失败语义 |

### digitize（1 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #41 | 本地 qwen3.5「卡死」 | 中文 system 消息极慢 + 思考模式 + 模型名没对齐，三件事叠在一起 |

### direction（3 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #75 | 种子被「太重要」踢出了聚类 | 主键覆盖把 120 篇降级 |
| #76 | 批量取用被限流，30% 的数据静默丢失 | 建图前先确认取全了 |
| #77 | OpenAlex 从 2026-02 起按量计费 | 建图会打很多次 OpenAlex，注意配额 |

### discover（6 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #35 | 中文检索式让全球文献召回质量崩塌 | 检索式必须是英文 —— 这是 `query_expand` 存在的理由 |
| #39 | 用户搜「PBS」一次暴露三个 bug | 缩写歧义 / 漏导入 / 阈值杀光结果 |
| #38 | 只看「跟我的库像不像」不够，雪球一开就被高被引通用文献带偏 | 排序要同时看相关性与「是不是通用大热门」 |
| #36 | Sciverse 把补充材料当独立文献返回，9 条重复挤爆结果 | 结果要去重 |
| #76 | 批量取用被限流，30% 的数据静默丢失 | **静默**是关键：限流不报错，只是少给 |
| #77 | OpenAlex 从 2026-02 起按量计费 | 「免费无密钥」这条旧认知已经过期 |

### extract（4 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #12 | 性能数值列大面积 N/A | 根因是 schema 字段太窄，不是模型不行 |
| #68 | 合成条件全是 N/A | 因为抽取从来没打开过补充材料 |
| #13 | 本地 7B vs 云端 DeepSeek | 抽取吃精度，7B 不够稳 —— 默认走云端 |
| #89 | 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了 | raw 与 curated 是两个目录了，建一个不等于建另一个 |

### library（3 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #8 | Zotero 本地 API 只读，不能写 | 本工具是只读的，这不是限制而是定位 |
| #10 | Zotero API 限流 429 + 重复处理 | 批量查要限速 |
| #9 | 本地 API 请求被 PowerShell 拒 / curl 中文乱码 | 别用 shell 拼请求，走 `shared.adapters.zotero_client` |

### paperdb（1 条）

| 坑号 | 现象 | 对这个工具意味着什么 |
|---|---|---|
| #89 | 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了 | 库在 `state/`，源 JSON 在 `serving/` —— 两层，别假设同生共死 |

## 咬过不止一个工具的

这些值得特别小心 —— 它们不是某个工具的毛病，是**一类做法**的毛病。

| 坑号 | 现象 | 咬过谁 |
|---|---|---|
| #10 | Zotero API 限流 429 + 重复处理 | curate、library |
| #20 | Zotero「找不到文件」：附件条目记录的文件名与实际写入的不一致 | curate、deepread |
| #23 | 同一文献出现两个 summary 附件 | curate、deepread |
| #35 | 中文检索式让全球文献召回质量崩塌 | askworld、discover |
| #36 | Sciverse 把补充材料当独立文献返回，9 条重复挤爆结果 | askworld、discover |
| #76 | 批量取用被限流，30% 的数据静默丢失 | direction、discover |
| #77 | OpenAlex 从 2026-02 起按量计费 | direction、discover |
| #89 | 数据分层把「建了 A 就等于建了 B」这个隐含保证悄悄拆了 | ask、extract、paperdb |

## 平台自身的坑不在这里

面板 / 体检 / 部署 / MCP / 常驻服务踩的坑（编码、弹窗、进程、部署、
SSH、版本库冲突…）没有按工具切片 —— 它们本来就是平台级的，
直接查总账，或看 `troubleshoot` skill 的「现象 → 坑号」速查表。
