# 归档_旧版本 · 给 LLM 的说明

这里放的是**已经废弃、但留着备查**的旧版脚本。

## ⚠ 不要在这里开发

- 不要修改这里的文件
- 不要参考这里的写法（它们代表的是被淘汰的思路）
- 不要把这里的代码复制到新功能里

如果发现现役功能里有和这里雷同的代码，说明现役那份可能也该更新了。

## 里面是什么

| 文件 | 为什么废弃 |
|---|---|
| `watcher.py` | 早期的精读监听器，已被 `文献精读/zotero_watcher.py` 取代（状态机、SI 支持、看门狗、单实例锁都在新版） |
| `build_deepread_workflow.py` | n8n 工作流生成器。平台早期基于 n8n，后来改为纯 Python 脚本，不再需要 |
| `archive/` | 更早的历史版本 |

## 什么时候可以真删

确认现役功能稳定运行一段时间、且用户不再需要回溯对比之后。
删之前问用户一句 —— 删除不可逆。
