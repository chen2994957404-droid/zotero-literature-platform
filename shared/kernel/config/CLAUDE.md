# config · 配置与密钥

> 你可能是被单独选中这个积木文件夹打开的，看不到项目其他部分。本文件是你的全部上下文。

## 这是什么

**一块「公理件」** —— 平台的最小可复用单元。整个项目的架构是：
公理件（原子能力）→ 定理（工作流）→ 组合。你现在在最底层。

**公理特征：只做一件不可再分的事。** 如果你想在这里加一个「顺便还做 XX」的功能，
那说明 XX 属于上层，不属于这里。保持这块的纯粹，是整个体系不腐坏的前提。

用户是材料方向研究者（聚硼硅氧烷/动态键弹性体），**不懂编程**。
技术决策你自己拿主意，跟他汇报用大白话。

## 职责

统一、可靠地拿到配置。加载顺序：**进程环境变量 → 项目根 `.env` 文件**。

## 对外接口

| 函数 | 用途 |
|---|---|
| `get_key(name, required=False)` | 取密钥（DEEPSEEK_KEY 等） |
| `get_model(name)` | 取某环节该用的模型（DEEPREAD_MODEL / EXTRACT_MODEL / ASK_MODEL…） |
| `get_site(name)` | 取本机设置（Zotero 用户ID、附件目录、Ollama 地址…） |
| `need_site(name)` | 同上，但没配就**明确报错并告知怎么配** |
| `set_keys({...})` | 写回 `.env`（原子替换 + 自动备份），控制面板用它 |
| `mask(v)` | 脱敏显示，只留后 4 位 |
| `site_missing()` | 哪些必填项还没配（新电脑装机时提示用） |

## 为什么存在（血泪）

密钥只放环境变量时，`setx` 设的值**只对之后新建的进程生效**，长驻进程拿不到，
导致 401、"未设置 token" 等静默失败 —— 这个坑反复踩了三次才收敛到这里。

## 铁律

1. **源码里绝不出现明文密钥**，一律走本模块。体检有专门一项在把关。
2. **不要给必填项留「读不到就用默认值」的兜底**。那会把开发者本人的 Zotero 用户ID
   留在源码里（传 GitHub 即泄露），而且别人装上会静默连到陌生人的库。
   **静默用错值，比直接报错危险得多。**
3. `.env` 与 `.env.bak*` 已在 .gitignore，永远不要提交。

## 谁在用它

几乎所有脚本和积木。**这是依赖最广的一块，改动要格外小心。**

改这里的对外接口 = 可能弄坏上面所有调用者。**改签名前先想清楚兼容性。**

## 改完必须做

```
python selftest.py                       # 本块自测，必须全过
python ../../host/doctor/health_check.py     # 全局体检，确认没碰坏别人
```
自测不过就是没改完。**没有自测覆盖的新功能，等于没写。**
