# -*- coding: utf-8 -*-
"""shared.adapters —— 外接口环：每一块**包住一个**外部系统或持久化存储。

判据：**「外部世界变了就得改它」**（API 换版本、模型换代、第三方库换掉、
存储格式变了）。宪法【首要判据】说的「发展中的东西用现成的，但包一层
自己的接口」，这一环就是那层接口住的地方。

**只有这一环允许联网、允许 import 第三方 SDK。**
这条规则就是「换掉 MineRU 只需改一个文件」的全部保证 ——
如果别处也能直接发 HTTP 请求，那个承诺立刻作废。

每块的写法：
    · 对外暴露**我们自己定义的**稳定函数名（多年不变）
    · 对内是随时可以整块换掉的实现
    · 失败时抛 `shared.kernel.errors` 里的分类异常，让调用方知道该不该重试
    · 依赖只能向下（shared.kernel），不许 import shared.domain / tools / host

`llm_client` 是范例：外层 `chat` / `chat_json` 没变过，
内层实现已经换过 DeepSeek → Ollama → 硅基流动三家。
"""
