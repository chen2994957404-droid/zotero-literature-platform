# -*- coding: utf-8 -*-
"""shared —— 共用件：被 **≥2 个工具**用到的东西才允许住这里。

只有一个工具用的代码留在那个工具自己的文件夹里（`tools/<name>/`）。
这条「下沉规则」防的是：`shared/` 一年后变成整个仓库最烂的地方 ——
什么都往里塞，谁也不敢动，因为不知道还有谁在用。

三层，依赖只能从下往上被引用：

    kernel/     基础设施：谁都依赖它，它不依赖任何人，也不联网
    domain/     纯逻辑：不联网、**不知道文件放在哪**（路径由调用方传进来）
    adapters/   外部世界：**唯一允许联网**的一层

「只有 adapters 能联网」就是「换掉 MineRU 只改一个文件」的全部保证。
守卫见 `tests/test_architecture.py`。
"""
