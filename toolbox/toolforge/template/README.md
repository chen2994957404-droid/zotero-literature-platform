# {{NAME}}

{{DESC}}

## 装

```bash
python install.py
```

把这份工具的说明装到 `~/.claude/skills/{{NAME}}/`，之后在**任何**项目里都能用。

然后照着 `config.example.toml` 抄一份到 `~/.{{NAME}}/config.toml`，填上你自己的东西。
**这份配置在仓库之外，不会被提交上去。**

需要 Python 3.11+。

## 用

```bash
python {{NAME_MOD}}.py --help
```

<!-- 填：三五条最常用的命令，每条后面写清楚它到底会做什么 -->

## 它替你记着的事

<!-- 填：这个工具存在的理由。不是功能列表，是那些「不知道就会踩」的细节 ——
     每一条都该是真踩过的，别写听起来很对但没验过的 -->

## 边界

<!-- 填：它**不做**什么、什么情况下别用它。
     这段比功能列表更能救人：让不该用的人早点走开，比事后解释便宜得多 -->

## 测试

```bash
python -m pytest tests -q
```

## 许可

MIT（见 LICENSE）。
