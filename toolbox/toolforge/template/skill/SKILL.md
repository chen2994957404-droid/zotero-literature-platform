---
name: {{NAME}}
description: <填：什么时候该读这份。写「用户说了什么 / 遇到什么情况」，不要写「本工具功能强大」——这段是给模型判断要不要加载用的，写虚了它就不会加载。>
---

# {{NAME}}

{{DESC}}

```bash
python D:/dev/{{NAME}}/{{NAME_MOD}}.py --help
```

| 想干什么 | 命令 |
|---|---|
| <填> | `{{NAME_MOD}}.py <子命令>` |

配置在 `~/.{{NAME}}/config.toml`。

## ⚠ 用之前要知道的

<!-- 填：真踩过的坑。每条写「现象 → 根因 → 怎么办」，
     别写没验过的注意事项 —— 假的注意事项会让人查错方向，比没有更贵 -->

## 什么时候**别**用我

<!-- 填：更便宜的做法是什么、哪些情况它根本帮不上忙。
     这一段能省掉大量白跑 -->
