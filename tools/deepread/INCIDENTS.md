# deepread · 这个工具特有的坑

> **全文在总账 `docs/incidents/踩坑记录.md`**（按编号找）。这里只留「跟本工具有关的那些」，
> 每条一句「对我意味着什么」——**改这个工具之前扫一眼，能省掉一整轮重新踩。**

| 坑号 | 现象 | 对本工具意味着什么 |
|---|---|---|
| #7 | 图被 MineRU 打散 / 摘要图混入 / 图没截全 | **别自己重写裁图**，用 `shared.domain.figure_crop` —— 那一条的全部智慧固化在那儿 |
| #1 #2 | MineRU PUT 上传 403；轮询字段结构与旧版完全不同 | 解析这一环只许经 `shared.adapters.pdf_parse`，别在工具里另写一份 |
| #15（第一个 15，精读线那条）| find_pdf 误选补充材料去精读（Springer 的 MOESM/ESM 逃逸了正则） | 选正文 PDF 的判据在 `zotero_client.find_pdf`，改判据要连这条一起想 |
| #21 #22 | Elsevier 的 SI 是 .docx 不是 PDF，导致漏检；docx 的 SI 当初没取图 | `has_si` 与 SI 线都要认 .docx |
| #23 | 同一文献出现两个 summary 附件 | 回写要**复用已有附件条目**，只覆盖文件内容 |
| #18 | Zotero 每篇都弹「冲突解决」框 | 根因是「先删后传」的删除动作进了同步链 —— 绝不删附件条目 |
| #20 | Zotero「找不到文件」：附件条目记录的文件名与实际写入的不一致 | 附件条目记录的文件名必须与实际写入的一致 |
| #27 | 精读只剩图没有字 | DeepSeek V4 思考模式会吃光 max_tokens |
| #65 | SI 精读「有时成有时败」 | SI 线的 token 额度曾是正文线的 1/5 |
| #29 | 用户打了标签没反应 | 改触发标签名 = 要求用户记住新词，等于没改 |
| #43（第一个 43）| 本地 qwen3.5 跑精读失败 | num_ctx 默认 16k 装不下全文（另：【图 N】的空格不匹配） |
| #3 | 3060 跑精读版太慢，不实用 | 精读是长输出，本地卡跑不动 —— 走云端 flash |
| #17 #19 | 批量精读 401；子进程拿不到 setx 设的 MINERU_TOKEN | 密钥一律走 `shared.kernel.config.get_key`，别指望环境继承 |
| #64 | 问本地 API「我刚传上去的附件在不在」，答案永远是「不在」 | 那是本地 API 的固有行为（滞后于同步），别怀疑上传失败 |
| #88 | 换用 `paths.summary(key)` 顺手引入了一处会抛异常的新行为 | 搬家式改动也要看被调函数的失败语义 |

## 再踩到新的怎么办

1. 当场往 `docs/incidents/踩坑记录.md` 追加一条（编号 + 现象 + 根因 + 解法，三段齐全）
2. 如果它只跟本工具有关，同时在上面这张表里加一行
3. 写中文用 Python `io.open(..., encoding='utf-8')` 追加 —— 别用 PowerShell 重定向（GBK 乱码）

`docs/incidents/README.md` 是由各工具的本文件汇总生成的
（`python host/codegen/incidents.py`），别手改那一份。
