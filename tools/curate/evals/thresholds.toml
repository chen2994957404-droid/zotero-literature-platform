# curate 评测阈值 —— **数字只写在这里**。

[thresholds]
# 1.0 = 一条都不许挂。
#
# 别的工具或许能容忍「八成通过」，这里不行：curate 做的是
# **改附件名 / 删条目 / 改标签**，全是写用户真实 Zotero 库的不可逆操作。
# 判错一条，用户就得人工收拾一条。而且这些判断全是纯函数，
# 没有模型、没有网络、没有随机性 —— 挂了就是真的坏了。
min_pass_rate = 1.0
