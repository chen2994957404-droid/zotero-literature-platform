# extract 评测阈值 —— **数字只写在这里**。

[thresholds]
# 1.0：这一组验的是抽完之后那套**确定性的理解**（是不是综述、哪一档、
# 哪些算有值、性能怎么拆成数），全是纯函数，挂了就是真坏了。
min_pass_rate = 1.0

# 「模型抽得准不准」是另一回事，需要人工核对过的真实记录当金标，
# 而且要花钱重抽才能比 —— 那一半还没做，见 README。
