# discover 评测阈值 —— **数字只写在这里**。

[thresholds]
# 1.0 = 一条都不许挂。这一组验的是**排序规则本身**（纯函数，输入固定），
# 不是「检索召回质量」那种有模糊空间的东西 —— 挂了就是规则被改坏了。
#
# 真正的召回质量要拿用户「后来实际导入了哪几篇」当金标，那是天然的、
# 但只有主力机上才有的数据。见 evals/README.md。
min_pass_rate = 1.0
