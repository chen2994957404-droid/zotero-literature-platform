# library 评测阈值 —— **数字只写在这里**。

[thresholds]
# 抽几篇来验「用自己的标题能搜到自己」。
# 太少了偶然性大，太多了每次都要打好几次 Zotero API（免费但慢）。
sample_items = 5

# 允许挂一篇：标题里带特殊符号（下标、希腊字母、书名号）的条目，
# Zotero 的 titleCreatorYear 分词偶尔搜不回自己。这是它的行为，不是我们的 bug。
# **但不许挂两篇** —— 那就不是个例了。
min_pass_rate = 0.8
