# digitize · 这个工具特有的坑

> **全文在总账 `docs/incidents/踩坑记录.md`**（按编号找）。这里只留「跟本工具有关的那些」，
> 每条一句「对我意味着什么」——**改这个工具之前扫一眼，能省掉一整轮重新踩。**

| 坑号 | 现象 | 对本工具意味着什么 |
|---|---|---|
| #15 #16（**第二组** 15/16，图表数字化那两条 —— 总账里这两个号各用过两次）| 本地 7B 视觉模型会编造「等差数列」假数据（严重） | **必须用云端大模型**。这条是本工具存在方式的前提，不是优化项 |
| #41 | 本地 qwen3.5「卡死」 | 中文 system 消息极慢 + 思考模式 + 模型名没对齐，三件事叠在一起 |

## 再踩到新的怎么办

1. 当场往 `docs/incidents/踩坑记录.md` 追加一条（编号 + 现象 + 根因 + 解法，三段齐全）
2. 如果它只跟本工具有关，同时在上面这张表里加一行
3. 写中文用 Python `io.open(..., encoding='utf-8')` 追加 —— 别用 PowerShell 重定向（GBK 乱码）

`docs/incidents/README.md` 是由各工具的本文件汇总生成的
（`python host/codegen/incidents.py`），别手改那一份。

## 云端模型也会编：短曲线被外推成直线（2026-09-06，总编号 #124）

qwen3-vl-plus 读六联图里的 a 图：横跨全图的主曲线读得基本准，
两条早断的短曲线被编成等差数列、一路铺到坐标轴末端，而 `confidence` 报 **high**。

`README` 里那句「本地 7B 会编假数据」需要扩大范围：**云端大模型同样会编**，
只是编的位置更隐蔽 —— 它编的是次要曲线，主曲线是对的，所以整份结果「看起来很对」。

**用本工具的人必须知道**：
- 结果**不能只看 confidence**
- 「终点恰好落在坐标轴最大值上」是造假的强信号，入库前先查这一条
- 目前只有横跨全图的主曲线值得信；短曲线、早断曲线要人工核对原图

详见 `docs/incidents/踩坑记录.md` #124。

### 续：单独裁子图能救回主曲线，救不回被量程压扁的曲线（2026-09-06）

单裁 a 图 + 放大 3 倍 + 提示词禁止外推之后：主曲线**分毫不差**，
置信度从 high 降到诚实的 medium，等差数列式编造消失。
但同图里数量级小 10 倍的两条曲线仍偏大 2~4 倍 —— 它们在图上只占几个像素。

**给本工具的两条改进方向**（未做）：
- `crop_figures` 目前把整张多联图当一张喂出去，应当按子图（a/b/c…）拆开
- 提示词加入「不要外推到坐标轴末端；看不清就说看不清」——已验证有效

**给使用者的边界**：同一张图里数量级差 10 倍以上时，小的那条不要入库。


## 踩坑 #139：写死的视觉模型被下线，看图这条线整条哑了一天（2026-09-07）

**现象**：`digitize` 抠图全部失败，唯一的信息是 `视觉模型调用失败: HTTP Error 400`。
主力机上 42 篇能裁出 **292 张图**，`curves.json` **一个都没有** —— 从来没成功过。

**根因**（问服务端问出来的，不是猜的）：`shared/adapters/llm_client` 的
`PROVIDERS['deepseek']` 第 4 位（看图默认模型）写着 `deepseek-vl2`，而服务端原话是：

> The supported API model names are deepseek-v4-pro, deepseek-v4-flash,
> and deepseek-v4-flash-vision-exp, but you passed deepseek-vl2.

**真正的毛病不是模型名过期**（那是外部世界变了，本来就会发生），
**是它没有配置项**：`MODEL_SETTINGS` 里精读/抽取/问答/打标签/构想/方向层各有一项，
唯独看图没有。于是模型一下线，不懂编程的用户在控制面板上**找不到任何地方可以改**，
只能看见一个 400。架构准则第二条（可理解性）踩在这里。

顺带查出第二处：`chat_vision` 里 `provider = os.environ.get('VISION_PROVIDER', ...)`
**只读环境变量**，而面板把设置写进 `.env` —— 就算加了这一项，用户改了也不生效（强制规范 #3 的老坑）。

**解法**：
1. `MODEL_SETTINGS` 加 `DIGITIZE_MODEL`（默认 `deepseek-v4-flash-vision-exp`），面板自动出现这一项。
2. `PROVIDERS['deepseek']` 的看图默认值换成服务端确认存在的名字。
3. `chat_vision` 取 provider 改走 `_cfg_get`（三级加载）。
4. `tools/digitize` 不指定模型时从 `get_model('DIGITIZE_MODEL')` 取。
5. 守卫：`tools/digitize/tests/test_model_from_config.py`（3 条）钉住「模型名是配置项」。

**判据（值得记住）**：**凡是外部世界会变的东西，都必须有一个用户够得着的开关。**
模型名、端点、限额都属于这一类。写死它 = 把「外面变了」变成「平台坏了」。
