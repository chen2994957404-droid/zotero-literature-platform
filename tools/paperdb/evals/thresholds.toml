# paperdb 评测阈值 —— **数字只写在这里**，别在 .py 里再抄一遍。

[thresholds]
# 1.0 = 一条都不许挂。
#
# 为什么是满分而不是「八成就行」：这套金标全是**确定性查询**
# （固定的记录 + 固定的 SQL），没有模型、没有网络、没有随机性。
# 这种东西挂一条就是真的坏了一条，容不下「通过率」这种说法。
# 需要容错的是那些要调模型的评测（deepread / digitize），不是这里。
min_pass_rate = 1.0
