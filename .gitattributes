# ===== 合并策略 =====
# 追加型文件：两台机器各自往文件末尾追加内容，谁也没改谁的部分。
# 默认的合并算法看不懂这一点，会判成冲突 —— 2026-08-26 主力机就是这么卡住的
# （踩坑 #51）。union 策略的意思是「两边的行都留下」，正是这类文件想要的。
#
# ⚠ 只对**纯追加**的清单/流水账用 union。
#   代码绝不能用：union 会把两边的行都保留，产生语法错误的缝合怪。
docs/踩坑记录.md      merge=union
docs/变更记录.md      merge=union
docs/待办与需求.md    merge=union
.gitignore            merge=union
